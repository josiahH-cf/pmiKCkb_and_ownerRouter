# Legacy And Superseded Context

This directory holds historical planning material that should not route new work unless
an active product doc links to it.

## Current Legacy Items

- `owner-router-separate-repo.md`: superseded plan for keeping Owner Router in a
  separate repository. Gmail Inbox 0 is now the active client-facing product lane in
  this monorepo.

## Rules

- Do not delete preserved history merely because the product direction changed.
- Do not let legacy "KB-only" or "separate Owner Router repo" language override
  `docs/north-star.md`, `docs/products/`, or `AGENTS.md`.
- When useful setup details are reused, copy the current decision into an active product
  doc and cite the legacy file as historical source material.
