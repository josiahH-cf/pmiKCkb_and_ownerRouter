# PMI KC current status

Last updated: 2026-09-10.

## Serving and candidate

Production serves d243911cb20ffb01773072c0e27c723648eeea34 as
pmi-kc-app-rmtkmhj1z-8855e4c6dbfb, 100% traffic, at
https://pmi-kc-app-kq6wuvpiva-uc.a.run.app. The prior zero-traffic candidate is
6e77d18f6d9916ba94078550d4b5ba73751e86c8 / pmi-kc-app-rmtt039q1-c1463245a94c.
It remains unpromoted; its assurance, promotion and observation never passed.
No S113 commit, push or deployment has run. Both exact supplied v2 internal templates are published.

September 10 compact Cloud Run readback confirms Production + Live, the managed runtime identity,
Sheet switch on, RentVine/RentCast bindings and RentCast allowance 50. The exact new release must
reread all runtime configuration, eleven Spaces, authorized domains and monitoring.

## Verified implementation

S113 F1-F5 is implemented locally. Five visible dashboard sections retain source truth and desk
context. Typed corrections support the approved exact Sheet and RentVine operations. RentCast
comp/trend preparation is visible and persisted. Supplied v2 messages retain rich/plain formatting,
copy fallback and governed unsent Gmail draft creation/recovery. Audited manual activity progresses
through staff-recorded completion, which remains distinct from provider-verified effects.
Persistent insurance, renewal information and seven legal-form location fields accept blanks.
Only resource-dependent output waits for a real verified value or approved legal content.

Full units pass 6,454 tests with four existing skips. All 201 backend tests pass through actual
mounted controls, routes, Firestore, claims, receipts/readbacks and the independent desk oracle.
Deterministic adapters provide external effects; no live customer record, draft or send proves this.
Core HTTP E2E passes 31 tests; 18 Firestore-dependent cases are intentionally skipped in that group
and covered separately by backend verification. Canonical verify phases pass. R19 repeats all 201
backend tests and the production build after the history correction; 85 assurance-boundary tests
and the separate 17-test existing canary request boundary pass.

The compiled guide passes 42 semantic steps, including visible correction controls, all required
resource fields and retained return view. Navbar, Dashboard assistant, theme, maintenance blockers
and maintenance intake pass. The final full desk passes sorting/filtering, inspection refusal,
active workspace/section navigation, exact return and Back, term parity and layout checks. Final
read scheduling passes 45 affected tests, all 201 backend tests and the R28 production build.
All 21 in-scope review findings are closed; deadlines and source freshness remain unchanged.
See docs/evidence/s113-implementation-review-2026-09-10.md for findings, evidence and limits.

## Downstream handoff and resources

Normal S106/S34 packet controls, preparation/approval, exact S21 bytes, S20 queue/ledger execution
and own-receipt recovery pass backend acceptance. Actual forms/catalog and mappings, managed
connection/consent/selection and the two closed exact keys retain their gates. No signature-send/
status API or legal content is invented. These inputs do not block the S113 manual lane or release.
B-S100/B-MNT1 and S36's complete-S100 prerequisite remain separate.

All six private supplied-template pages were reviewed and preserved outside Git. The exact v2
internal publications were published and read back approved at 20:08:16–17 UTC September 10, with
matching immutable hashes and the existing Admin approver; old review-only versions remain unchanged.
No live Gmail draft or send has run. The three-page operator guide and one-page meeting brief were
visually checked. Current control evidence is docs/evidence/renewal-training-control-review-2026-09-09.md.
The brief awaits final release-result alignment.

## Authentication and release

September 10 CLI/ADC refresh and GitHub pass with the existing WSL store and approved account.
The existing native Linux Cloud SDK also passes the same refresh checks. Canonical owner Admin
browser enrollment passed as josiah@pmikcmetro.com. The owner explicitly accepts Admin-only browser
assurance on both exact origins; Editor is not_run and backend role restrictions remain unchanged.
No account, IAM or claim changed. The separate 24-hour authentication longevity proof is unverified.

The previous watcher was stopped at its unpromoted assurance checkpoint to serialize this release.
Final desk/review and 217-file mechanical delivery checks pass. Exact-main-SHA CI, candidate/configuration/domain
assurance, Admin on both origins, independent source reconciliation, receipt-bound promotion and
300,000 ms observation remain mandatory, followed by serving and backend readback. The existing
watcher resumes after verified release. Documentation-only closure does not deploy.
