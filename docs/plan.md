# PMI KC current plan

Updated: 2026-08-27.

## Outcome

Ship the dependency-independent closure slice, leave every coupled feature at one exact external
seam, prove the release and rollback, obtain eight real owner litmus verdicts, and publish the
customer readout deck. No live RentVine record, operating Sheet cell, or client-facing message may be
changed.

## Completed implementation

1. Triaged all three live support reports and added verified managed-user reporter display.
2. Corrected RentVine Registry metadata without changing `production_allowed:false`.
3. Built auditable Admin rehearsal-Sheet configuration and saved-config proof resolution.
4. Built discrepancy dispositions and kept correction execution source-contract closed.
5. Proved the four-lease baseline/evidence/report machinery and documented its exact run sequence.
6. Retired Gmail watch infrastructure; built manual refresh, source-backed follow-up truth, and S75
   versioned policy surfaces with unset-safe behavior.
7. Completed the fixed S36 provider lifecycle and bounded S37 operational page builder.
8. Completed dependency-independent Dotloop, LeadSimple, and RentVine resident-channel lifecycles.
9. Applied and behavior-tested the smallest dependency overrides; production audit is zero.

## Current release sequence

1. Reconcile all active documents and the completion/blocker HTML to the implementation and live
   readback.
2. Run focused falsification, the complete 524-file unit lane, Firestore lane, all static/policy
   gates, dependency audit, and production build.
3. Audit the diff for authority changes, secrets, PII, live-effect reachability, and stale docs.
4. Commit and push the green tree to `main`; wait for the aggregate CI result.
5. Deploy a zero-traffic candidate, prove exact commit/revision and bounded behavior, then promote and
   read back 100% traffic.
6. Roll back to the captured version-aware predecessor, smoke, restore the new revision, and smoke
   again. Restore immediately on any failure.
7. Run the eight-row human litmus with the owner; record real dated PASS/FAIL observations and repair
   any FAIL.
8. Create and visually verify the editable 8–10 slide customer readout from final deployed truth.

## Completion rule

Internal closure is complete only when local and remote gates are green, the served revision and
rollback are proven, the human litmus has no blank verdict, the deck is delivered, and the blocker
report contains external owners/inputs only.
