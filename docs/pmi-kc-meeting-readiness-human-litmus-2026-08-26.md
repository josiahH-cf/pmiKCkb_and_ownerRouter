# PMI KC consolidated human litmus - current 2026-08-30

This checklist preserves the original eight meeting checks and adds one named entry for every renewal
stabilization suite, with separate S63 process and number/safety reviews. Model verdicts summarize
deterministic evidence only. Human verdicts intentionally remain blank until the owner performs the
check, records a real date, and writes a short observation. Never infer or model-fill a human PASS.

For a blocked external effect, test the implemented refusal and boundary now; record the live portion
only after its exact external prerequisite and authority exist.

## Original meeting checks

### Safe RentVine renewal proof boundary

**If this was built correctly:** The tracked S30 templates cannot execute. With the exact action
closed, an Admin can prepare only a secure dry review for one exact lease `endDate`; recurring
charges, a second record, and every generic operation remain unreachable.

- Model verdict: PASS - Tracked/unsafe packets, closed/suspended/unreadable state, wrong actor/account,
  stale inputs, unsupported fields, recurring-charge execution, and second targets refuse before
  writer construction. Production action readback is non-executable. No live target or write ran.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Rehearsal spreadsheet connection

**If this was built correctly:** Admin shows separate links for the operating renewal Sheet and its
rehearsal copy, refuses to treat the operating Sheet as the copy, and a confirmed copy-only probe
writes a synthetic marker, reads it back, removes it, and confirms the cell is restored.

- Model verdict: PASS - Admin canonicalizes and saves a distinct rehearsal URL/id without running a
  proof. Deterministic tests refuse an operating-Sheet alias and prove blank-cell compare-and-set,
  readback, exact clear, and final blank restoration against a fixture. No actual copy is configured,
  so the operational round-trip remains correctly unavailable.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Dual-source discrepancy language

**If this was built correctly:** A nontechnical teammate can distinguish agree, conflict, one source,
both missing, intentionally different meanings, stale, or ambiguous lease matching without seeing a
client value in the example.

- Model verdict: PASS - One classifier renders the full state set with a plain-language next step. An
  append-only disposition records exact source identity, reason, evidence, owner, and status without
  changing either source.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Honest current-rent confidence

**If this was built correctly:** A rent involved in an open conflict, stale read, or ambiguous record
never wears a Verified badge in the workspace or draft; fresh agreement or one exact current
resolution does, with its read date.

- Model verdict: PASS - Agreement or a current exact resolution can verify. Conflict, missing, stale,
  expired, ambiguous, or unavailable evidence fails closed to Needs Verification.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Human-friendly currency entry

**If this was built correctly:** A teammate may type `$1,500.25` or `1500`, sees malformed or
negative amounts rejected, and reviews the same numeric value in the exact draft preview.

- Model verdict: PASS - Shared S77 parsing preserves positive numeric offer values and rejects malformed
  grouping, text, non-finite, zero/negative, or inverted values before preview/create.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Work details and materials

**If this was built correctly:** When assigning work, a teammate can record the job location,
materials still needed, and materials already bought, then see those details on the task card.

- Model verdict: PASS - Location, materials needed, and materials bought persist and render through
  the work-accountability store; validation and round-trip tests pass.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Clear product navigation and exact build identity

**If this was built correctly:** The compatibility-only Workflow Communications Space is absent from
the directory, and `/api/version` identifies the exact deployed commit/revision without exposing
configuration or secrets.

- Model verdict: PASS - Directory filtering hides the compatibility-only Space and the bodyless
  version route uses `no-store`. Production reports commit
  `1d68c7fb0a4f3138b9d0ba410d221b44bfb5534c` and revision
  `pmi-kc-app-rmtg73suu-fe8734d35330`.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### Client action center and meeting materials

**If this was built correctly:** The client can use one action checklist, one presenter agenda, this
litmus, and an editable 16:9 readout that separates delivered outcomes from external decisions.

- Model verdict: PASS - The current HTML action center/agenda, client checklist, litmus, and editable
  10-slide PPTX plus inspected PDF exist. Copy/redaction checks still apply to the final bundle.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

## Renewal stabilization suite checks

### S77 - exact preview, confirmation, and recovery

**If this was built correctly:** Preview never accepts a boolean as create authority. Create requires
the same execution id and preview hash; changing offer/channel/server facts invalidates it. An
uncertain create cannot be retried as new and offers only exact Message-ID reconciliation.

- Model verdict: PASS - Browser, route, service, ledger, composer, and send-boundary tests use one
  strict contract and prove exact input binding, one-attempt behavior, and read-only reconciliation.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S59 - defensible RentCast reference evidence

**If this was built correctly:** The operator can explain the exact RentVine facts, query policy,
omitted property type, cache/quota state, subject/comparable evidence, and why the result never sets
the offer.

- Model verdict: PASS - The server derives one complete query, requests the approved two-mile/15-count
  policy, exposes provenance/cache/quota/provider order, and preserves contractual base rent as a
  distinct authoritative fact.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S80 - role, Space, and exact-effect governance

**If this was built correctly:** A Renewals Editor can do ordinary app work, while reconciliation,
pricing/source approval, Admin configuration, and provider effects still require their own role and
exact action conditions. Direct routes and APIs refuse consistently.

- Model verdict: PASS - One inventory-backed matrix covers every renewal page, exported API method,
  and rendered control; role never substitutes for an exact key, runtime, quota, or confirmation.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S72 - six-step process and evidence

**If this was built correctly:** The workspace shows six ordered steps with useful substeps, roles,
evidence, blockers, branches, and reopening. A click, note, draft, or legacy stage cannot forge
completion, and missing downstream inputs block only their exact substep.

- Model verdict: PASS - Immutable `renewal-v1`, legacy compatibility, stable ids, exact evidence
  projection, branch/invalidation/reopening rules, and effect separation pass focused and canonical
  tests.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S75 - waiting, last contact, and due truth

**If this was built correctly:** Waiting party and last verified contact agree across the desk,
workspace, evidence, and attention surfaces. Unset policy displays as unset and produces no due item,
draft, or send; one manual linked-thread refresh updates evidence monotonically.

- Model verdict: PASS - One provider-free exact-identity projection, most-specific policy resolution,
  incomplete/stale refusal, manual read-only refresh, and audited dismiss/reopen behavior are shared
  across all surfaces.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S78 - canonical renewal desk

**If this was built correctly:** The operator can find current, upcoming, and tracked-incomplete work;
search and URL-backed filters/sorts round-trip predictably; nulls and ties are stable; and every row
opens the same source-backed lease workspace.

- Model verdict: PASS - One worklist projection owns identity, cohort/retention, conflict, process,
  follow-up, role-consistent next action, deterministic query rules, and the legacy redirect.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S74 - copy, locked facts, and channel truth

**If this was built correctly:** Owner and tenant copy are separately versioned. Recipients, dates,
values, terms, and mandatory facts cannot be altered by the browser or model. Current review-only
copy visibly refuses tailoring/draft creation, and a draft never claims contact or delivery.

- Model verdict: PASS - Server envelope/hash binding, editable-region validation, deterministic
  fallback, review-only provider refusal, and separate intent/preview/draft/contact/delivery/reply
  states pass. Client-approved copy remains external.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S79 - one receipt-bound screenshot attachment

**If this was built correctly:** Only one current same-Space/lease delivered receipt can contribute
one allowlisted image to the exact draft preview. Forged/cross-record refs, multiple/inline/HTML
attachments, mismatched bytes, and sends refuse. Text-only drafts remain compatible.

- Model verdict: PASS - Receipt ownership, byte/hash/MIME binding, deterministic multipart encoding,
  raw Gmail draft readback, uncertainty reconciliation, and rollback honesty pass. The live Drive
  action/folder remains closed/unconfigured, so a real attachment effect is NOT EVALUATED.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S81 - task-oriented Connections and Admin navigation

**If this was built correctly:** Staff can find connection status by task and Admins can reach the
right existing control through stable anchors/back-links. The screen distinguishes disconnected,
closed, and ready states, and navigation never grants permission or provider authority.

- Model verdict: PASS - One capability-validated manifest, role/direct-route guards, status-only
  RentCast behavior, keyboard/focus order, active state, and narrow-layout checks pass.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S63 - four-case process behavior

**If this was built correctly:** For each of four securely designated real read-only cases, the report
shows which six-step process evidence is present, missing, or not evaluated without changing a source
or allowing one case to substitute for another.

- Model verdict: PASS for machinery; NOT EVALUATED for fresh cases - Secure exact-four packets,
  exact Sheet-row-to-RentVine-lease linkage, create-only baselines, append-only evidence, and
  independent process verdicts are implemented/deployed. No secure packets or fresh report were
  supplied.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S63 - four-case numbers, evidence, and read-only safety

**If this was built correctly:** Each case separately shows base-rent/recurring-charge separation,
RentCast query/evidence, source conflict/currency, and safety outcomes. Missing inputs say not
evaluated. Terminal/report surfaces reveal no client values and no writer/draft/sender is reachable.

- Model verdict: PASS for machinery; NOT EVALUATED for fresh cases - Independent number/evidence and
  safety families, value-free commands, bodyless refs/counts, source-read-only inventory, and
  fail-closed missing-input behavior pass. Real reviewer observations remain absent.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

### S30 - prove and reverse one exact lease end-date

**If this was built correctly:** After separate owner authorization, one managed Admin sees one exact
lease before/change/rollback, confirms forward once, sees its receipt/readback, separately confirms
rollback, sees the original value restored, and sees the exact action closed. No other record, field,
recurring charge, or operation is selectable.

- Model verdict: PASS for closed implementation; NOT EVALUATED for live effect - Secure input,
  managed-actor proof, two fresh reads, just-in-time authority/confirmation checks, durable at-most-one
  forward/rollback, ambiguity-without-retry, exact readback, and closed-state closeout pass. No target
  or protected direction was supplied; no writer or live mutation ran.
- Human verdict (YYYY-MM-DD, owner): PASS | FAIL - observation:

## Completion rule

Every human row requires an actual dated observation. A FAIL creates a repair item and must be rerun.
A blocked external effect remains blank or explicitly not evaluated; it never becomes PASS from model
evidence, deployment, source-read success, or informal meeting intent.
