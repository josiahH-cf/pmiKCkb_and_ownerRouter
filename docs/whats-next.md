# What is next

Updated: 2026-08-29.

## Internal closure artifacts

The dependency-independent product implementation, verification, commit/push/CI, zero-traffic
deployment, promotion, and version-aware rollback/restoration rehearsal are complete. The only
remaining internal closure artifacts are eight real owner litmus verdicts and the final editable,
visually inspected readout deck. They are acceptance evidence, not client product decisions.

## Client, owner, and provider inputs

The renewal process, base-rent meaning, two-mile/15-request RentCast policy, ordinary Editor-work
matrix, constrained-AI scope, screenshot attachment presentation, and narrow Admin/Connections scope
were recorded on 2026-08-29. Remaining external inputs are:

1. Create/share a distinct rehearsal Sheet copy and select one blank proof cell.
2. Designate one unmistakable RentVine test lease/owner/field for a separately reviewed reversible
   proof; ordinary workflow-testing availability is not write authorization.
3. Provide the move-out walkthrough and exact lease behind the wrong-resident report.
4. Approve owner/tenant wording, mandatory/forbidden copy, channel-evidence rules, waiting-on/timing/
   override policy, and the exact end-of-September scope.
5. Approve the S66 lease packet catalog and supply Dotloop OAuth/exact mappings.
6. Supply one selected LeadSimple account action/contract/credential.
7. Supply the official RentVine resident invitation/reply/webhook/correction/rollback contract.
8. Supply one S36 owner-approved pilot packet if a twelfth Space should be proven.
9. Decide separately whether S64 per-person approval authority is authorized; it remains closed.

## Safe next action after each input

- Enter policy through its versioned Admin surface, then rerun the affected deterministic tests.
- Run the Sheet proof only on the distinct copy: blank → marker → readback → clear → blank.
- Keep RentVine write closed until the exact designated-record preview, rollback, and protected gate
  review are complete.
- Run the four-lease packet in `docs/evidence/four-lease-proof-readiness-2026-08-27.md` under the
  approved process/base-rent/two-mile/15-request criteria; missing lease evidence remains
  `not evaluated`.
- Configure only the official provider adapter references; do not change schemas or invent values to
  simulate readiness.

Every missing external input blocks only its named capability. The rest of PMI KC remains usable.
