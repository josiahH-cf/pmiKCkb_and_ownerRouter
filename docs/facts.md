# Solidified Context — Fact Ledger

Tier-0 spine. Read this with `docs/loop-state.md` before acting. This is the one authoritative
ledger of what is **Verified**, what is an **Assumption**, and what is **Open**. It references the
other docs as evidence rather than restating them. `npm run verify:context-freshness` gates this
file: every `Verified` row needs evidence and an ISO date, evidence paths must exist, supersedes
must resolve, no superseded rule may still read as active, and `docs/loop-state.md` must stay a
short, current pointer.

Conventions: `F-` verified fact · `A-` assumption · `Q-` open question. `status` is one of
`Verified` / `Assumption` / `Open`. Dates are ISO (`YYYY-MM-DD`). `supersedes` points at the row id
this replaced, or `—`. `review-by` is the date after which a `Verified` row must be re-checked (a
non-failing staleness warning), or `—`.

## Fact Ledger

| id                  | claim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | status     | evidence                                                                                                                                                                                                                                       | verified-on | supersedes          | review-by  |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------- | ---------- |
| F-IDENTITY-1        | The project always authenticates as a `pmikcmetro.com` identity; the personal `josiah.abernathy@gmail.com` account must never appear in any auth path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verified   | `AGENTS.md` Identity Rules                                                                                                                                                                                                                     | 2026-06-25  | —                   | 2026-12-31 |
| F-BUDGET-1          | The cloud budget is a hard ~$10 total cap enforced by a real kill switch (budget → Pub/Sub → a Cloud Function that disables billing), not alerts alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Verified   | `docs/budget-and-cost-policy.md`; `scripts/check-budget-guard.mjs`                                                                                                                                                                             | 2026-06-25  | —                   | 2026-12-31 |
| F-AWAY-MODE         | Remote Away Mode is INACTIVE; normal owner-present governance and the approval gates apply.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verified   | `docs/away-mode.md`                                                                                                                                                                                                                            | 2026-06-25  | —                   | 2026-12-31 |
| F-SEND-AUTHORIZED   | Owner granted send/draft authorization across the board (in-session directive 2026-07-19): the product is LIVE and the default posture is ship-to-production, not preview-only-pending. Activating a built action's `production_allowed` is a routine reviewed code change (seed edit plus its pinned tests), never an owner-decision blocker; an action stays gated ONLY for a genuine named external dependency (a missing provider write endpoint or credential, e.g. a RentVine write endpoint or QuickBooks OAuth), never as a governance default. Retained safety invariants PERMIT go-live and never block it: every client-facing send stays human-initiated and exact-confirmed (no autonomous, scheduled, bulk, or model-triggered client-facing agent send; internal-staff transactional notifications may auto-send per `D-AUTOMATION-LINE` / `F-ROADMAP-BUILD-AUTHORIZED`); secrets stay in Secret Manager, never git; a Live system-of-record write uses its S25/S26 preview, confirm, receipt, and rollback contract; sample/test data never becomes a real draft or send; staff/cloud/build identities stay `pmikcmetro.com`/service. The authorized Gmail notice draft-into-Gmail actions (renewal owner/tenant, maintenance owner) are cleared to activate; each activation is a reviewed seed change.                                                                                                                                                                                                                   | Verified   | `AGENTS.md`; `lib/integrations/action-registry-seed.ts`; `lib/integrations/action-gate.ts`; `docs/loop-state.md`                                                                                                                               | 2026-07-19  | —                   | 2026-12-31 |
| F-RENTVINE-AUTH     | RentVine is read-only today over HTTP Basic to the tenant host; one live `GET /leases/export` returned 25 real leases mapped to 25 candidates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Verified   | `lib/integrations/rentvine/client.ts`; `docs/status.md`                                                                                                                                                                                        | 2026-06-24  | —                   | 2026-07-24 |
| F-SHEET-DWD         | The renewal sheet is read keyless via domain-wide delegation (a service account signs a JWT impersonating a `pmikcmetro.com` user); read-only scope; the managed domain blocks a bare service account.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verified   | `lib/google-sheets/read-client.ts`                                                                                                                                                                                                             | 2026-06-24  | —                   | 2026-07-24 |
| F-SHEET-TAB         | The real renewal tab is "Lease Renewal" (519×31); the sheet is a freeform live worklog reconciled against RentVine, not a defect list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verified   | `lib/lease-renewal/headers.ts`; `lib/lease-renewal/pipeline.ts`                                                                                                                                                                                | 2026-06-24  | —                   | 2026-09-30 |
| F-MAINT-FIRST-WRITE | Maintenance Work Order Intake (RentVine work-order create) is the documented first executable write; lease-renewal writeback is last and gated (no documented endpoint).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Verified   | `docs/integration-architecture.md`; `docs/plan.md`                                                                                                                                                                                             | 2026-06-25  | —                   | 2026-12-31 |
| F-GMAIL-LANE        | Workflow Communications is the active Gmail lane: Gmail remains the message system of record, while PMI KC owns bodyless workflow linkage, reviewed proposals/facts, attention, approvals, and audit context for authorized renewal and maintenance communications. Compatibility filenames and routes retain Gmail Inbox 0 / Gmail Hub names only to avoid breaking links.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verified   | `docs/north-star.md`; `docs/products/README.md`; `docs/products/gmail-inbox-zero.md`; `docs/feature-suites/gmail-live-per-user.md`                                                                                                             | 2026-07-14  | F-GMAIL-LIVE-ACTIVE | 2026-12-31 |
| F-LOCALMODEL-SEAM   | A model-provider seam exists: a free local model stands in for Gemini via the same answer path, selected by config and fenced from prod (NODE_ENV).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Verified   | `lib/llm/model-provider.ts`; `lib/llm/answer.ts`                                                                                                                                                                                               | 2026-06-25  | —                   | 2026-12-31 |
| F-VOICE             | Client-facing copy follows `docs/voice-and-audience.md`; the S2 Connection Center pass removed internal jargon, the dead next-release control, and the not-live verification over-claim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Verified   | `docs/voice-and-audience.md`; `docs/feature-suites/voice-copy.md`                                                                                                                                                                              | 2026-06-25  | —                   | 2026-12-31 |
| F-VOICE-2           | Copy-voice v2 (extends F-VOICE) adds three durable rules enforced by `npm run verify:copy-voice`: no em dashes in user-facing copy, the app calls itself "the app" (never "PMI handles it"), and every description says what it does in one concrete sentence. The S13 A-slice fixed the two operator-quoted strings, applied the simulation→"test run" lexicon across the Console/Ask/workflow surfaces, and split RentVine's display label from its byte-identical internal source id. The gate hard-fails on jargon everywhere and on em dashes in both client-facing drafts and (since the 2026-07-19 operator-UI purge cleared all 60) the operator UI (app/components), and now runs inside scripts/verify.sh so it cannot silently regress.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verified   | `docs/voice-and-audience.md`; `scripts/check-copy-voice.mjs`; `lib/lease-renewal/source-display.ts`                                                                                                                                            | 2026-07-02  | —                   | 2026-12-31 |
| F-PRECUST-WAVE1     | S13 Wave 1 is BUILT and locally verified (branch `s13-wave1-precustomer`, 970 tests): B — the Approval Queue defaults to the unified value-free "Needs your decision" inbox, queued write-backs get bulk approve/return ON the run page (multi-select + one shared mandatory reason, Admin-only, per-item Activity row, per-item failure reporting), decided items collapse to counts-only sections, and one shared needs-decision gather feeds the Console approvals answer + the lease-renewal Space card's "N waiting on you"; C — persisted reconcile direct_links resolve via a redirect route + ?flag= highlight/scroll, app-state items carry per-item deep links (per-connector anchors), the Console shows live counts + a "start with" link, and reconcile queue items dedupe by source_trigger_key so one decision is one count everywhere; D — the built RentVine/Sheets live probes feed verifiedIds (~10-min cache) so working connectors show "Connected", Sheets/Drive requiredConfig names the DWD pair, Dotloop/LeadSimple/Gmail-sender get honest cards + env seams, /connections is read-only for non-Admins with an Admin fresh-verify button, and the stale Drive-DWD note is corrected. S14 adds one narrow exception to the original read-only triage posture: a safe Low/Medium, Ready, actor-eligible, non-self-assigned `queue_item` may approve inline in the unified inbox and Console. Renewal-flag and write-back rows plus the Spaces card remain deep-link/read-only; no SoR write; hard gates unchanged. | Verified   | `git log 72bb7a7..ca3c9bf`; `lib/approval/needs-decision-gather.ts`; `lib/approval/needs-decision-inbox.ts`; `lib/connections/verification.ts`; `tests/unit/needs-decision-inbox.test.ts`; `tests/unit/lease-renewal-run-client-bulk.test.tsx` | 2026-07-10  | —                   | 2026-12-31 |
| A-DATA-GOV          | Real previously-vetted client data may be read/used as test/training input inside the `pmikcmetro.com` boundary, kept out of git and out of user/model output without approval; it is never emitted or acted on autonomously.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Assumption | `AGENTS.md` Security Rules (applied 2026-06-20)                                                                                                                                                                                                | 2026-06-25  | —                   | 2026-09-30 |
| A-IA-V2             | Target IA recalibrated (owner-directed 2026-06-30): the Console is the home/front door; Spaces ⊇ Processes (each process surfaced alongside its Space via sub-tabs, the standalone Processes tab retired, the process-definition ENGINE kept); Space cards are fully clickable and reflect real process/connection state; Maintenance folds into sub-tabs; built operator-first for a non-technical user. Evolves F-OPS-CONSOLE-IA, which still describes the CURRENT built IA until the rework ships.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Assumption | `docs/feature-suites/ui-ia.md`; `docs/feature-suites/console-app-state.md`; `docs/feature-suites/space-teeth.md`; `docs/feature-suites/dev-prod-parity.md`; `docs/products/v1-process-qa.md`                                                   | 2026-06-30  | —                   | 2026-12-31 |
| Q-ABC-1             | The term "ABC" used in prior discussion is undefined in the repo; the owner must define it before anything relies on it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Open       | owner to clarify; no occurrence in code or docs                                                                                                                                                                                                | —           | —                   | —          |
| Q-PREC-1            | Reconciliation precedence is fully settled: the DEFAULT is owner-confirmed (F-RECON-PRECEDENCE) and the per-case MANUAL override is CONFIRMED (owner 2026-07-01) — a reviewer may override a genuine conflict via the existing resolve flow (pick source / enter corrected value / "sheet is already right"); High-severity overrides need an Admin; an audited plain-English reason is mandatory + logged; never auto-applied. Round 2 (2026-07-14) permits an Admin to self-approve at every risk; R01 still determines whether an internal Editor may directly execute a High/Blocked instance. D1 scopes the input method only: a tapped reason code supplies that persisted plain-English reason for a Low/Medium exact suggested-source acceptance, while High/Blocked and every manual override still require free text. Write-back execution remains gated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Verified   | `docs/products/v1-process-qa.md`; `docs/feature-suites/approval-queue-mobile.md`; `lib/firestore/lease-renewal-resolutions.ts`; `docs/v1-readiness-audit-round-3-2026-07-14.html`                                                              | 2026-07-14  | —                   | —          |
| Q-WRITEBACK-METHOD  | Lease-renewal write-back method DECIDED (owner 2026-07-01): (a) append-only proposal column — write proposals to a NEW append-only column, never mutate existing cells (zero risk to the team's data, earns trust first); graduate to (b) cell-anchored compare-and-set only after Phase-1 flag accuracy is proven; (c) RentVine-first stays deferred (vendor-gated write endpoint, OQ-RV-1). Decision recorded; the write-back is not built (gated Phase-2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Verified   | `docs/products/v1-process-qa.md` (owner decision 2026-07-01)                                                                                                                                                                                   | 2026-07-01  | —                   | —          |
| Q-MAINT-STORAGE     | Storage DECIDED: Google Drive in-boundary; the Drive adapter + photo capture are built and the keyless Drive sync was round-trip-verified. The current Action Registry deliberately gates photo execution; any later gate-open deploy must set `MAINTENANCE_PHOTO_DRIVE_FOLDER_ID`, which the cutover preflight enforces. This is resolved capability/configuration, not an open V1 product question.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Verified   | `lib/google-drive/drive-dwd.ts`; `lib/maintenance/photo-action.ts`; `scripts/ensure-maintenance-drive-folder.ts`; `scripts/preflight-production-cutover.mjs`                                                                                   | 2026-07-14  | —                   | 2026-12-31 |
| Q-MAINT-PHOTO-INDEX | Binding rule CONFIRMED (owner 2026-07-01): tenant maintenance photos are kept out of ANY indexed/searchable corpus — the photo folder (MAINTENANCE_PHOTO_DRIVE_FOLDER_ID) has NO Vertex data store; maintenance SOPs index only from a SEPARATE approved low-sensitivity source; tenant photos are NEVER co-located in an indexed source folder. The photo folder is already decoupled from the KB-source map in config.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Verified   | `docs/products/v1-process-qa.md` (owner decision 2026-07-01); `lib/config/server.ts`                                                                                                                                                           | 2026-07-01  | —                   | —          |
| F-IA-CONSOLE-HOME   | IA is Console-first (A-IA-V2, supersedes F-OPS-CONSOLE-IA): the Console is the app home — the home route `/` and the preserved `/ask` both render the shared `ConsoleView` (the card-launcher home was retired). The standalone Processes nav tab is retired; the `/processes` routes and the process-definition engine are preserved and reached via deep links (e.g. the Renewal Desk). The Spaces directory shows fully-clickable cards reflecting real state (connections-needed / needs-a-process / process-ready via `computeSpaceCardState`); each Space that carries a process surfaces it beside the Space via a read-only Process sub-tab (Spaces ⊇ Processes) that deep-links to the engine (`/processes/{id}`). Space copy uses the plain-language lexicon ("Process space"). The "Ask" surface stays the "Console".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Verified   | `app/page.tsx`; `app/ask/page.tsx`; `components/console/ConsoleView.tsx`; `components/layout/AppShell.tsx`; `app/spaces/page.tsx`; `app/spaces/[spaceId]/page.tsx`; `lib/space-card-state.ts`; `components/spaces/ProcessSummaryPanel.tsx`     | 2026-06-30  | —                   | 2026-12-31 |
| F-ACTION-CONSOLE    | The Console (`/ask`) is the action console: the four Ask metadata selects (Audience/Channel/Space/Urgency) are retired and it is process-aware — picking a process makes the ANSWER process-aware (the selected process is resolved server-side into prompt context, never a citation) and lets an editor start a SAFE simulation run (simulation_only, no system-of-record write) reusing the spine's test-run path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Verified   | `app/ask/page.tsx`; `components/ask/AskForm.tsx`; `lib/ask/service.ts`; `lib/llm/prompt.ts`; `tests/unit/ask-form.test.tsx`                                                                                                                    | 2026-06-29  | —                   | 2026-12-31 |

| F-GOLDEN-HARNESS | A golden-data harness runs the reconciliation pipeline against ground-truth-labeled scenarios and measures the false-positive flag rate; a read-only live-capture tool writes gitignored, in-boundary golden drafts for team labeling; the gate enforces synthetic + human-verified captured sets. | Verified | `lib/lease-renewal/golden/harness.ts`; `scripts/capture-golden-data.ts`; `tests/unit/lease-renewal-golden-harness.test.ts` | 2026-06-29 | — | 2026-12-31 |

| F-RENEWAL-PROCESS-SEED | Lease Renewal is seedable as a real Draft process definition at the fixed id 'lease-renewal' (buildLeaseRenewalDefinitionRecord + idempotent `npm run seed:process-definitions`), so the existing /processes catalog surfaces it and the Renewal Desk links to it. Draft + non-executable (no action reference 'Approved for Execution'); activation runs the existing lifecycle later; not yet applied to prod Firestore (dry-run verified). | Verified | `lib/lease-renewal/process-definition-seed.ts`; `scripts/seed-process-definitions.ts`; `components/lease-renewal/RenewalDesk.tsx` | 2026-06-29 | — | 2026-12-31 |

| F-GOLDEN-LABELING | A labeling round-trip converts a live-captured golden draft into a team-VERIFIED set without inventing ground truth: `golden:worksheet` re-runs the pipeline to surface each candidate flag's reconciliation context; the team records accept/reject/severity decisions; `golden:apply-labels` writes a `labelsVerified:true` set the harness gate enforces. apply refuses an incomplete review or a worksheet stale vs. the draft's pipeline flags; both tools read/write only the gitignored in-boundary /golden-data/ tree with counts-only stdout. | Verified | `lib/lease-renewal/golden/labeling.ts`; `scripts/golden-labeling.ts`; `tests/unit/golden-labeling.test.ts` | 2026-06-29 | — | 2026-12-31 |

| F-PROD-CLOUD-MODEL | Production runs the cloud model only: `lib/config/server.ts` forces `modelProvider:"gemini"` and disables `localDemoAuth` when `NODE_ENV==="production"`. The local model + demo auth are dev/test-only stand-ins (a free prod-equivalent for testing), never a prod dependency. | Verified | `lib/config/server.ts`; `lib/llm/model-provider.ts` | 2026-06-29 | — | 2026-12-31 |

| F-OPEN-PLACEHOLDER | The source-state formerly labeled "Bailey Placeholder" is renamed to the plain-language "Open Placeholder" (a relevant SOP/source exists but its value is an open, unapproved placeholder). Resolves Q-BAILEY. | Verified | `lib/source-state.ts`; `lib/constants.ts`; `components/source-state-banner/SourceStateBanner.tsx` | 2026-06-29 | — | 2026-12-31 |

| F-INTENT-DETECT | The action console infers the process from the question via a hybrid: a free deterministic name/alias matcher (`detectProcess`, client-side, prod-safe) suggests a process as the user types; a model-backed fallback (`classifyProcessWithModel` via the ModelProvider seam — Gemini prod / local dev) runs only on explicit click; the manual picker stays as override. Edit-gated; the model path is the only cost and never fires automatically. | Verified | `lib/processes/intent.ts`; `lib/processes/classify.ts`; `app/api/processes/classify/route.ts`; `components/ask/AskForm.tsx` | 2026-06-29 | — | 2026-12-31 |

| F-RECON-PRECEDENCE | Reconciliation precedence (owner-confirmed 2026-06-29): RentVine is read-authoritative for lease facts (dates, rent, tenant/property); the renewal sheet is authoritative for the team worklog/decisions; a genuine cross-source conflict routes to a human and is NEVER auto-picked (suggestion-only, auto_apply_allowed=false). | Verified | `lib/lease-renewal/reconciliation.ts`; `lib/lease-renewal/field-reconciliation-rules.ts` | 2026-06-29 | — | 2026-12-31 |

| F-RENEWAL-DATE-SEMANTICS | The sheet "Renewal Date" column is the team renewal worklog/target — a DIFFERENT field from RentVine's authoritative lease-end (owner-confirmed 2026-06-29). The RentVine lease-mapper emits the lease-end as `lease_end_date` (no reconciliation spec), so it never flags the sheet's renewal_date; this removed the false-positive renewal_date conflicts (live re-capture 17->2 candidate flags, 0 renewal_date noise, only the 2 real current_rent conflicts remain). | Verified | `lib/integrations/rentvine/lease-mapper.ts`; `tests/unit/rentvine-lease-mapper.test.ts` | 2026-06-29 | — | 2026-12-31 |

| F-MAINT-INTAKE | Maintenance Work Order Intake has an intake foundation: a pure work-order DRAFT builder (`buildWorkOrderDraft`: capture -> summary/description/priority/unit/photos + blockers, with emergency-keyword priority inference) and a Draft process-definition template (`buildMaintenanceProcessTemplate`) referencing the gated RentVine work-order actions. `readyForExecution` is always false; the RentVine create stays production_allowed:false. Photo storage = Google Drive in-boundary (owner 2026-06-29). | Verified | `lib/maintenance/work-order-draft.ts`; `lib/maintenance/process-template.ts`; `lib/maintenance/constants.ts` | 2026-06-29 | — | 2026-12-31 |

| F-STT-SEAM | Maintenance voice capture uses a speech-to-text seam (`createSpeechToTextProvider`): a free dev/test stub stands in for Google Cloud Speech-to-Text (v1 speech:recognize REST), selected by config and fenced from prod (`SPEECH_PROVIDER` forced to google when NODE_ENV=production, mirroring the model provider). `/api/maintenance/transcribe` is edit-gated and caps audio size (~8MB base64) to bound STT cost. | Verified | `lib/speech/stt-provider.ts`; `app/api/maintenance/transcribe/route.ts`; `lib/config/server.ts` | 2026-06-29 | — | 2026-12-31 |

| F-MAINT-CAPTURE-UI | Maintenance capture has a dedicated edit-gated desk at `/maintenance` (reachable from the Spaces dropdown via spaceHref): typed issue + tap-to-record voice (MediaRecorder -> the transcribe API -> transcript) + unit + priority, producing a live work-order DRAFT preview via buildWorkOrderDraft. Simulation-only — the RentVine create stays gated. | Verified | `app/maintenance/page.tsx`; `components/maintenance/MaintenanceCapture.tsx`; `lib/spaces.ts` | 2026-06-29 | — | 2026-12-31 |

| F-MAINT-PHOTO | Maintenance photo storage retains the image-store seam and previously live-verified keyless Drive adapter, but execution now obeys the committed Action Registry entry `google_drive.maintenance_photo.store` (`Needs Permission`, `production_allowed:false`). While closed, `/maintenance` renders explicit unavailable guidance and no file input; a crafted `POST /api/maintenance/photo` returns typed `action_not_production_allowed` before reading the body, config, store, Drive client, or bytes. Local-demo auth forces the stub even when `.env.local` requests Drive. A future reviewed gate-open change must retain the built explicit preview of filename, MIME type, and safe in-boundary target label before confirmation. The Drive adapter's 2026-06-29 round-trip evidence remains capability evidence, not standing permission to execute. | Verified | `lib/maintenance/photo-action.ts`; `app/api/maintenance/photo/route.ts`; `components/maintenance/MaintenanceCapture.tsx`; `lib/config/server.ts`; `tests/unit/maintenance-photo-route.test.ts`; `tests/unit/maintenance-capture.test.tsx`; `tests/unit/server-config.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-DEMO-FIRESTORE-GUARD | Demo Firestore seed/reset/operator paths fail closed through one pre-Admin target guard. The effective `FIRESTORE_EMULATOR_HOST` may come from the process or `.env.local`, but it must parse as loopback, be reachable, and is then normalized into the process environment before any dynamic Firebase Admin import/initialization or child process. A project id is an emulator namespace only and never grants a live fallback; demo commands have no `--live` mode. Startup output is limited to verified host/port/project plus counts, and deterministic demo audit ids keep repeated/interrupted resets from duplicating rows. | Verified | `scripts/demo-firestore-target.mjs`; `scripts/demo-firestore.mjs`; `scripts/seed-demo-records.mjs`; `scripts/reset-demo-records.mjs`; `scripts/demo-operator.mjs`; `tests/unit/demo-firestore-target.test.mjs`; `tests/unit/demo-operator.test.mjs` | 2026-07-13 | — | 2026-12-31 |

| F-DRIVE-DWD | In-boundary Google Drive writes use a keyless domain-wide-delegation client (`lib/google-drive/drive-dwd.ts`, `GoogleDriveClient` + `mintDriveDwdToken`) acting AS a pmikcmetro.com user (SHEETS_DWD_SUBJECT) — never the personal account, no key file — mirroring the Sheets reader. `npm run maintenance:ensure-folder --live` find-or-creates the maintenance photo folder; the gated `google_drive.maintenance_photo.store` Action Registry entry (production_allowed:false, readiness Needs Permission) defines the governed access. LIVE 2026-06-29: the Drive DWD scope was authorized (admin), the Drive API enabled on pmi-kc-kb-prod, the in-boundary folder created (id in SPACE_DRIVE_FOLDER_IDS, gitignored), and a real upload round-trip verified (upload + viewable link + cleanup). | Verified | `lib/google-drive/drive-dwd.ts`; `scripts/ensure-maintenance-drive-folder.ts`; `lib/integrations/action-registry-seed.ts` | 2026-06-29 | — | 2026-12-31 |

| F-MAINT-SEED | Maintenance Work Order Intake is seedable as a real Draft process definition at fixed id maintenance-work-order-intake (buildMaintenanceDefinitionRecord), reusing the shared generic idempotent writer `seedProcessDefinition` (refuses any Approved for Execution reference). `npm run seed:process-definitions` seeds BOTH lease-renewal + maintenance (dry-run verified: 8/6 + 7/3 steps/refs, Draft); the live write is owner-gated. | Verified | `lib/maintenance/process-definition-seed.ts`; `lib/lease-renewal/process-definition-seed.ts`; `scripts/seed-process-definitions.ts` | 2026-06-29 | — | 2026-12-31 |

| F-DEVPROD-PARITY | Dev↔prod parity for live connections (S12): the deploy (`readRuntimeEnv`/`readRuntimeSecrets`) forwards the four NON-SECRET live-connection identifiers (`RENTVINE_API_BASE_URL`, `RENEWAL_SHEET_ID`, `SHEETS_IMPERSONATE_SA`, `SHEETS_DWD_SUBJECT`) as env vars and delivers the RentVine key/secret via Secret Manager (`--set-secrets`, wired when the base URL is set; secret id overridable via `*_SECRET_ID`), never inlined. The production cutover preflight REQUIRES the four non-secrets (RentVine pmikcmetro tenant over https mirroring the runtime guard, sheet id, a GCP-SA impersonator, a pmikcmetro.com DWD subject), so a green cutover guarantees prod connects instead of silently degrading to "not connected". Reads + the gated photo write only; `production_allowed:false` unchanged. REDEPLOYED + verified live 2026-07-01: the deployed `pmi-kc-kb-demo` service pulls the real 25 RentVine leases (Secret Manager creds) + the renewal Sheet (keyless DWD) and reconciles to the 2 real current-rent conflicts, the prod demo-auth fence holds (unauth `/`→307 sign-in; demo cookie `/api/ask`→401), and an authenticated live Ask grounds (200, Verified Source, 2 citations). Beyond env parity, prod needs TWO runtime-SA IAM grants — `secretmanager.secretAccessor` on the RentVine secrets AND `iam.serviceAccountTokenCreator` on the DWD reader SA (`SHEETS_IMPERSONATE_SA`, so the runtime SA can `signJwt` as the reader) — plus an explicit Admin custom claim per operator (`firebase:set-role`; the default role is Editor, which cannot reach the live review or the write-back approval decisions). All three were undocumented setup gaps found + fixed during the live verification; now in the cutover doc. | Verified | `scripts/deploy-demo-cloud-run.mjs`; `scripts/preflight-production-cutover.mjs`; `docs/client-production-cutover.md`; `tests/unit/cutover-readiness-golden.test.mjs` | 2026-07-01 | — | 2026-12-31 |

| F-CONSOLE-ACT-IN-PLACE | The Console acts in place for exactly ONE app-plane decision (console overhaul A4, scoped by S14). Everything F-CONSOLE-APP-STATE established still holds: a read-only, non-fatal app-state provider answers approvals / connection-setup gaps / Space process+connection coverage as advisory, deep-linked state; an always-visible, server-rendered action deck surfaces the three areas ("Needs your decision", "Connections to set up", "Space coverage") as clickable cards with each count + the top deep-linked rows, beside a read-only process strip; and Console voice input reuses the prod-fenced STT seam via /api/ask/transcribe (edit-gated). A `NeedsDecisionRow` may carry a minimal `itemId` plus the value-free `canApproveInline` boolean on `queue_item` rows only. The Console deck and unified inbox expose the existing PATCH `{action:"approve"}` only when that boolean is true: Low/Medium, Ready, actor-eligible, and non-self-assigned. The transition records an app-plane decision and never executes an external action; High/self-assigned queue items and every writeback/renewal_flag/connections/coverage row stay deep links with no approve affordance, and the execute bulk path stays blocked. Renewal/write-back boards and the Spaces card keep their read-only/deep-link posture. No send, SoR write, or registry-state change was added. | Verified | `lib/approval/needs-decision-inbox.ts`; `components/approval/NeedsDecisionInboxPanel.tsx`; `components/console/ConsoleActionDeck.tsx`; `components/console/ConsoleApproveButton.tsx`; `components/console/ConsoleView.tsx`; `tests/unit/needs-decision-inbox.test.ts`; `tests/unit/console-view.test.tsx`; `tests/unit/console-action-deck.test.tsx`; `tests/unit/approval-queue-api-routes.test.ts` | 2026-07-10 | — | 2026-12-31 |

| F-RENEWAL-REVIEW-SUBTAB | The Approval Queue has a renewal review sub-tab (OQ-UI-1, owner 2026-07-01): a value-free renewal review view (originally a "Renewals" peer tab; moved under the unified queue's "Other views" disclosure by F-APPROVAL-QUEUE-UNIFIED) that groups the deterministic renewal reconciliation flags by run (`buildRenewalReviewBoard` over the run views), ordered most-attention-first, and deep-links each flag to the built resolve surface (`/lease-renewal/runs/{id}`) where the real values + resolution live. Read/triage only — no approve/write affordance here; resolution stays on the run page via the resolve flow (Q-PREC-1). VALUE-FREE by construction (candidate/suggested values never leave the run view; a test asserts the board carries no value-bearing keys). Approving is an admin-tier function (OQ-APPR-1, owner 2026-07-01): V1 keeps the existing Editor/Approver/Admin capability model (`queueActionAvailability` gates approve to Approver/Admin); Dan + Josiah are Admin; Dan's nuanced per-scope approver delegation is a future admin surface, not built. Sourced from the deterministic simulation runs (no live Firestore queue items yet); the board degrades gracefully and reads/writes nothing. | Verified | `lib/approval/renewal-review.ts`; `lib/lease-renewal/renewal-review-board.ts`; `components/approval/RenewalReviewPanel.tsx`; `components/approval/ApprovalQueue.tsx`; `app/approval-queue/page.tsx`; `tests/unit/renewal-review.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-WORKING-ORDER | Session working order (owner directive 2026-07-01): (1) FRONT-LOAD the human-gated, unblocking work when the owner is present (secrets/Secret Manager, provisioning, answer capture, approvals, reauth) so downstream unattended model work stays unblocked — clear the human bottlenecks while the human is here. (2) SELF-ANSWER before asking the client: resolve from repo/docs/code first, then the developer, and route only the irreducible decisions (client-operational reality, vendor endpoints, statutory/legal rules) to the client as confirm-with-default. A drafted client note is the last resort, not the first move. | Verified | `AGENTS.md` Working Order | 2026-07-01 | — | 2026-12-31 |

| F-SESSION-AUTH | Session-start auth workflow (2026-07-01): `npm run auth:session` (`scripts/session-auth.ps1`, owner-run, INTERACTIVE) refreshes the gcloud CLI login + ADC ONLY when stale and confirms the RentVine env. Needed because the pmikcmetro.com org enforces interactive-only Google reauth (RAPT) — the agent's non-interactive shell can NEVER satisfy it, so a stale gcloud login (cost-bearing gcloud) or stale ADC (live Sheets/Firestore/Vertex reads) silently stalls a run mid-step. The agent CHECKS with the read-only `npm run preflight:adc` before any live Google read and asks the owner to run `auth:session` on failure; it never works around a stale token with a personal account (identity stays pmikcmetro.com). RentVine reads are unaffected (HTTP Basic from `.env.local`). | Verified | `scripts/session-auth.ps1`; `scripts/preflight-adc.mjs`; `package.json`; `AGENTS.md` Working Order | 2026-07-01 | — | 2026-12-31 |

| F-WRITEBACK-PROPOSAL | The append-only write-back PROPOSAL is generated (Q-WRITEBACK-METHOD method (a), owner 2026-07-01): `buildWritebackProposal` turns a flag-raising reconciliation into a value-bearing proposal — the value to append to a NEW append-only column ("KB Proposed — {field}") from the suggested-winner source, or a value-LESS "Needs input" proposal for a blocked/missing/no-winner field (a value is never invented). Surfaced read-only at the run evidence (`LeaseRenewalRunClient`) with binding guardrails (suggestion only; needs approval; append-only, never overwrites an existing cell; not executed) and as a value-free "Proposal ready" badge on the renewal review sub-tab (`F-RENEWAL-REVIEW-SUBTAB`). Pure + deterministic; NOT executed — writing to the operating Sheet still needs an approved per-action spec (SoR write); the cell-anchored compare-and-set model (method (b), `writeback.ts`) stays the graduate-later path. The run-evidence card points the reviewer to its APPROVAL PATH: approving a proposal = resolving the flag via the built resolve flow (pick the suggested source) — the proposal card links to it (no separate execute path). | Verified | `lib/lease-renewal/writeback-proposal.ts`; `lib/lease-renewal/run-view.ts`; `components/lease-renewal/LeaseRenewalRunClient.tsx`; `components/approval/RenewalReviewPanel.tsx`; `tests/unit/writeback-proposal.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-MAINT-UNIT-MATCHER | Maintenance has a read-only location→unit matcher (M-4, owner 2026-07-01: built FIRST, before any work-order create, so unit confidence is real not user-typed): `matchLocationToUnit` COMPOSES the verified lease-renewal join spine (`deriveAddressKey` + `joinScore` + `JOIN_MATCH_THRESHOLD`/`JOIN_AMBIGUOUS_THRESHOLD`) and emits the already-fixed `MaintenanceUnitMatch` contract, so `buildWorkOrderDraft` is UNCHANGED (its unmatched + "Needs Review" blockers fire off the match confidence). Two tiers: a structured unit id verbatim in the location → Verified (unique); else fuzzy address scoring capped at Likely for a unique top ≥0.85, Needs Review for ambiguous/tied/[0.4,0.85), no match below 0.4. `deriveUnitCandidatesFromExport` lifts read-only unit candidates from RentVine /leases/export rows; the shape is CONFIRMED (`smoke:rentvine-read --live` 2026-07-01, now dumps the append keys): each export row carries `unit` + `property` appends, and the `unit` append holds `unitID` AND the address (`streetNumber` + `streetName` + `address2`, plus city/postalCode). So the extractor lifts `unit:<id>` and composes the matchable street label from the SAME export read (no separate /properties call); a unit append with no address → a `Needs Verification:` label, never invented. GUARDRAILS: read-only, human-overridable (ordered candidates[]), never blocks, never invents, fuzzy never Verified, `autoMerge:false`, pure/deterministic. The live candidate SOURCE now works — matcher is LIVE-READY; the remaining step is the `/api/maintenance/match-unit` route + capture-UI wiring (feed the export candidates into `matchLocationToUnit`). | Verified | `lib/maintenance/unit-matcher.ts`; `lib/maintenance/work-order-draft.ts`; `lib/lease-renewal/join.ts`; `tests/unit/maintenance-unit-matcher.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-MAINT-NOTICE-VENDOR | Maintenance has non-executable owner-notice DRAFT + vendor-assignment SUGGESTION stages (M-5, owner 2026-07-01: BUILD both as draft/suggest surfaces). `buildOwnerNoticeDraft` composes a source-tagged owner notice from a `WorkOrderDraft` in the renewal owner-draft shape (reuses `DraftFact`; literal-false `production_allowed` + `send_allowed`; a missing owner name or an unmatched unit render `Needs Verification:` markers, never invented). `suggestVendorAssignment` deterministically infers the vendor TRADE from the issue text (`MAINTENANCE_TRADE_KEYWORDS`, most-hits-wins, ties by `MAINTENANCE_TRADES` order, "General" fallback) — the SPECIFIC vendor is NEVER named (no roster exists; `Needs Verification: client vendor roster`). Both non-executable (`production_allowed:false`, no autonomous send, no SoR write), pure + deterministic; the RentVine work-order create + any owner send stay gated. NOW SURFACED in the capture desk: building the work-order draft renders the owner-notice DRAFT (subject/body/`Needs before sending`) + the vendor-assignment SUGGESTION (trade + rationale + the Needs-Verification vendor) inline, each clearly labeled non-executable. | Verified | `lib/maintenance/owner-notice-draft.ts`; `lib/maintenance/vendor-assignment.ts`; `lib/maintenance/constants.ts`; `components/maintenance/MaintenanceCapture.tsx`; `tests/unit/maintenance-notice-vendor.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-WRITEBACK-APPROVAL | The lease-renewal write-back PROPOSAL approval CONTROL PLANE is built (Q-WRITEBACK-METHOD phase-2 — the "admin-enabled, per-write" surface the resolution layer defers to). A resolution QUEUES an append-only proposed write-back; an ADMIN then explicitly Approves (authorizes the future, gated write → "Approved / ready to write"), Returns, or Revokes it via `decideWritebackApproval` (Admin-only per OQ-APPR-1 — authorizing a write is strictly more sensitive than resolving a flag; mandatory plain-English reason; idempotent by source_trigger_key; append-only Activity log). Reachable states are a COMPILE-CHECKED non-executing subset of the audited write-back FSM (`writeback.ts` via `Extract<WriteBackState,…>`; Writing/Verifying/Written unreachable from here). NOT executed: every record carries `production_allowed:false` + `executed:false`, no Sheets/SoR call exists in the path, and the write itself stays gated behind an approved per-action spec (`F-WRITE-GATE` unchanged). A re-resolution that changes the queued value marks a prior approval STALE (needs re-approval), never silently authorizing a different value. Surfaced value-BEARING on the run evidence (Admin approve/return/revoke control) and value-FREE on the renewal review sub-tab (per-run awaiting/approved COUNTS only — the `F-RENEWAL-REVIEW-SUBTAB` value-free invariant is preserved by a test). Two new client-read-only Firestore collections (`lease_renewal_writeback_approvals` + `_activity`), server-written via the Admin SDK boundary. AUDIT TRAIL (2026-07-01 follow-on): the run page shows the append-only decision history under the approval control (each approve/return/revoke — who, when, why) via `listWritebackApprovalActivityForRun` (ONE run-scoped `where(run_id)` query grouped by flag key — no N+1), threaded through `buildRenewalRunView` onto the flag overlay's optional `activity[]` (oldest→newest); value-bearing + RUN-PAGE-ONLY (design §6.1) — the value-free board/queue never carry it, asserted by a test. | Verified | `lib/lease-renewal/writeback-approval.ts`; `lib/firestore/lease-renewal-writeback-approvals.ts`; `app/api/lease-renewal/writeback-approvals/route.ts`; `app/lease-renewal/runs/[runId]/page.tsx`; `lib/lease-renewal/run-view.ts`; `lib/approval/renewal-review.ts`; `components/lease-renewal/LeaseRenewalRunClient.tsx`; `firestore.rules`; `tests/unit/lease-renewal-writeback-approvals.test.ts`; `tests/unit/run-view-writeback-activity.test.ts`; `tests/unit/writeback-approval-plan.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-WRITEBACK-QUEUE | The Approval Queue has a cross-run "Write-back queue" tab (value-free read-only follow-on to `F-WRITEBACK-APPROVAL`, 2026-07-01): a cross-run view (originally a third peer tab; moved under the unified queue's "Other views" disclosure by F-APPROVAL-QUEUE-UNIFIED) that consolidates every QUEUED write-back proposal ACROSS ALL RUNS and groups it by APPROVAL STATE (Awaiting approval / Approved — ready to write, not executed / Returned), each row deep-linking to its run page to act. A NEW pure projection `buildWritebackApprovalQueue(views)` reuses the SAME `RenewalRunView[]` the review board assembles (`loadRenewalRunViews` — the page builds BOTH boards from ONE gather, so NO new Firestore reads) and copies ONLY value-free fields per row (fieldKey, fieldLabel, severity, runId, runLabel, state, href); the proposed value, decision reason, and decider stay behind the run-page deep link (a test pins the EXACT row key set + asserts value/reason/decider/activity never serialize). Read + deep-link ONLY — no approve/return affordance here (acting stays on the run page's Admin control, mirroring the review sub-tab's read/triage posture, OQ-UI-1); adds NO decision path, changes no `production_allowed`, executes nothing. | Verified | `lib/approval/writeback-approval-queue.ts`; `lib/lease-renewal/renewal-review-board.ts`; `components/approval/WritebackQueuePanel.tsx`; `components/approval/ApprovalQueue.tsx`; `app/approval-queue/page.tsx`; `tests/unit/writeback-approval-queue.test.ts`; `tests/unit/writeback-approval-queue-panel.test.tsx` | 2026-07-01 | — | 2026-12-31 |

| F-PRECUST-CYCLE | The 2026-07-02 pre-customer refinement cycle is owner-approved and decision-complete: all 12 packet decisions answered YES (owner 2026-07-02) with ONE amendment — renewal-notice timing rules (mid-month deadline, warning lead days, follow-up interval) are CONFIGURABLE DATA with per-lease/tenant + per-property overrides resolved most-specific-wins over global defaults (lease > property > global), never global constants; defaults flagged Needs Verification until Dan confirms values. The tracked spec is `docs/feature-suites/pre-customer-refinement.md` (S13: Tier-0 owner steps → Wave 1 copy/queue/deep-links/connections → Wave 2 move-in/move-out desks via S11 → Wave 3 notices/Dictate/learning loop); the disposable mapping packet is `docs/temp/pre-customer-refinement-plan.md`. Key locked calls: unified value-free "Needs your decision" default queue view EVOLVES the OQ-UI-1 tab layout (value-free triage + act-on-run-page invariants preserved; record the layout supersede when B1 ships); "bulk edit" = bulk DECISIONS with one shared mandatory reason stamped per record, never bulk field-value editing; future send policy = approved notice → UNSENT Gmail draft, human clicks Send (spec-only this cycle); learning loop V1 = deterministic rules + golden tuning, live decisions PRE-FILL worksheets only, Josiah merges rule-tuning PRs, no client values on GitHub; Dictate must work on iPhone; desk check-off persists app-plane; live seed targets prod with real Dan/Josiah uids. Hard gates unchanged (no autonomous send, no SoR write execution, no Cloud Scheduler, `production_allowed:false`, ~$10 cap). | Verified | `docs/feature-suites/pre-customer-refinement.md` | 2026-07-02 | — | 2026-12-31 |

| F-MAINT-MATCH-UNIT-LIVE | The maintenance capture desk is WIRED to the live unit matcher (M-4 complete): `MaintenanceCapture`'s "Find unit" button POSTs the location to `/api/maintenance/match-unit` (edit-gated), which reads live RentVine units (`loadLiveUnitCandidates` → one read-only `/leases/export` via a RentVine-only `buildLiveRentVineConfig`, degrading to a clear status category — not_configured/account_mismatch/auth_error/read_error — never a leaked message), derives candidates, and returns `matchLocationToUnit`'s result; the operator confirms/overrides via a candidate dropdown, and the `WorkOrderDraft` unit is the matcher's result (REAL confidence) — never the raw typed text (the old typed-unit synth is retired, so an unmatched location fires the "Match the location to a unit." blocker). Read-only, `autoMerge:false`, no write, `production_allowed:false`. Browser-verified LIVE 2026-07-01: 25 real units derived, a real address round-trips to the correct unit at Likely (no client address emitted). | Verified | `app/api/maintenance/match-unit/route.ts`; `lib/maintenance/live-unit-source.ts`; `lib/lease-renewal/live-config.ts`; `components/maintenance/MaintenanceCapture.tsx`; `tests/unit/maintenance-match-unit-route.test.ts` | 2026-07-01 | — | 2026-12-31 |

| F-MOVEIN-1 | Move-In is a real V1 desk (S13 Wave 2 / space-teeth E1+E2): a 10-step Draft process definition quoting `move-in-move-out-process.md` §3 VERBATIM (fixed id `move-in`), wired onto the Move-In Space (processDefinitionId + rentvine/google_sheets connectors). Per the owner Q2 OVERRIDE every step — including e-signature and certified funds — is a tracked checklist flag, NOT a hard gate; the desk shows a DRAFT welcome (email + Portal Chat) whose fees render "see RentVine" (never a hard-coded figure) and whose deposit posture cites the Missouri 2x-rent policy as text. Draft-only, production_allowed:false, no send, no system-of-record write; dry-run seed verified, the live seed is owner-run (E5). | Verified | `lib/move-in/process-template.ts`; `lib/move-in/process-definition-seed.ts`; `lib/move-in/welcome-draft.ts`; `docs/products/move-in-move-out-process.md` | 2026-07-02 | — | 2026-12-31 |

| F-MOVEOUT-1 | Move-Out + Deposit Disposition is a real V1 desk (S13 Wave 2 / space-teeth E1+E2): an 11-step Draft process definition quoting §4 VERBATIM (fixed id `move-out-deposit-disposition`), with a manual "Start move-out" trigger ONLY (no Renewals handoff, Q1). Per the Q3 OVERRIDE the desk assembles operator-entered evidence and computes a SUGGESTED deposit deduction (integer-cents, transparent, labeled SUGGESTION-ONLY, owner-approval-required) that NEVER posts to a ledger; the statutory deadline + legal wording stay literal "Needs Verification:" placeholders (Q2). "Disposition sent" and "everything finalized" are ordinary checklist steps (no second state machine, no new blocking mechanism). Draft-only, no send, no system-of-record write. | Verified | `lib/move-out/process-template.ts`; `lib/move-out/process-definition-seed.ts`; `lib/move-out/evidence-packet.ts`; `docs/products/v1-process-qa.md` | 2026-07-02 | — | 2026-12-31 |

| F-SPACE-DESK-1 | A reusable per-Space desk generalizes the Renewal Desk shape (S13 Wave 2 / space-teeth E2-E4): `SpaceDesk` (header + Stepper from the ProcessDefinitionRecord + connected-tools status via `classifyConnector` + one next action + an injected domain slot) renders for any Space carrying a processDefinitionId via `app/spaces/[spaceId]`. A new persisted per-step checklist layer (`workflow_run_step_checks` + append-only twin, natural-key upsert by `${run_id}:${step_id}`, gated at `edit` — app-plane, not the Admin write-back tier) backs it, with a "Start a run" control reusing the EXISTING test-runs path. The SAME shell powers all four new desks: Move-In, Move-Out, Tenant Renewal Notice (wraps `buildTenantOfferDraft` + the new `buildDotloopFollowUpDraft`, which references the two EXISTING Dotloop registry keys, Needs Permission), and Owner Renewal Outreach (wraps `buildOwnerRenewalDraft`) — proving the pattern generalizes. No SoR write, no send; all four definitions seed Draft + non-executable. | Verified | `components/desk/SpaceDesk.tsx`; `lib/firestore/workflow-run-step-checks.ts`; `tests/unit/workflow-run-step-checks-route.test.ts`; `app/spaces/[spaceId]/page.tsx`; `lib/lease-renewal/dotloop-followup-draft.ts` | 2026-07-02 | — | 2026-12-31 |

| F-NOTICE-ENGINE | The governed renewal-notice engine (S13 Wave 3 F): timing rules are configurable DATA, not code — a typed rule set (notice deadline day-of-month + month-offset, operator warning lead days, follow-up interval days, enabled) with three scopes resolved MOST-SPECIFIC-WINS (lease > property > global) by a pure resolver with per-field provenance (mirrors `cohort.ts`; reference-date-in, no Date.now, no I/O). Boundary-safe date math (day clamps to the target month; year rollover). V1 storage is a seedable app-plane config record (`lease_renewal_notice_rules`, one doc, client-read-only via `firestore.rules`; `npm run seed:notice-rules`, owner-run, dry-run verified). Every default renders `Needs Verification:` until Dan confirms it. Read-only surfacing: the effective RULE on the renewal Space desks (`NoticeRuleCard`) and the per-lease "Notice due by <date> (default)" + status on the live workspace (`sample-desk` notice view, F2). Live-data drafts reuse the built owner/tenant composers with the verbatim `DRAFT_BANNER` on the outreach/notice desks (F3). Operator-triggered reminders via `npm run notices:reminders` (pure `planNoticeReminders`, dry-run, deduped by stable key; NO Cloud Scheduler, NO email delivery, NO send, F4). Gmail `renewal_notice.draft_create` + the future send are DOCS-ONLY per-action specs (`production_allowed:false`, no runtime, F5). No SoR/Sheet/Gmail write, no send. | Verified | `lib/lease-renewal/notice-rules.ts`; `lib/firestore/lease-renewal-notice-rules.ts`; `lib/lease-renewal/notice-reminders.ts`; `components/desk/NoticeRuleCard.tsx`; `docs/integration-architecture.md` | 2026-07-02 | — | 2026-12-31 |

| F-DICTATE-VERIFIED | Dictate is hardened and verifiable (S13 Wave 3 G): `SpeechSetupError` now carries a classification `code` (`api_disabled`/`auth`/`encoding`/`http`) + Google's error detail, so an un-enabled Speech API surfaces its real cause instead of a bare HTTP status (G1). One shared `useAudioRecorder` hook dedupes the maintenance + Console recorders with a network-error catch, a ~55s auto-stop (v1 sync limit), an empty-transcript hint, and `MediaRecorder.isTypeSupported` mime negotiation (prefers opus) with an HONEST Safari/iPhone message when only `audio/mp4` is available (G2/G3). A new `npm run smoke:transcribe-live` POSTs a committed few-KB SYNTHETIC WAV fixture and passes on HTTP 200 (owner-run live). `speech.googleapis.com` is added to `REQUIRED_GCP_APIS` AND the cutover enable block in the SAME change, kept in sync by the doc-sync test. Enabling the API + running the live smoke are OWNER-RUN; the seam + script are built here. STT stays prod-fenced (Google forced in prod). | Verified | `lib/speech/stt-provider.ts`; `components/hooks/useAudioRecorder.ts`; `scripts/smoke-transcribe-live.mjs`; `scripts/preflight-gcp-setup.mjs`; `tests/unit/gcp-setup-preflight.test.mjs` | 2026-07-02 | — | 2026-12-31 |

| F-GMAIL-PER-USER | Owner Email + Gmail are represented as per-user domain-wide (console overhaul Slice F, representational only): the Connection Center gains an honest `gmail_inbox` connector ("Gmail (per-user inbox)", read + draft the signed-in user's own mailbox) that stays "Not connected" by construction (empty requiredConfig, no live probe, no runtime) until the client-approved access model + the domain-wide-delegation Gmail scopes are authorized; the send-only `gmail_sender` notifier card is unchanged. The Owner Email space is reframed from the legacy single-owner / Owner Router artifact framing to per-user (each pmikcmetro.com user works their own Gmail), kept read-only. No Gmail is read, no draft is created, no message is sent; nothing built here calls the Gmail API — the per-user runtime stays gated. | Verified | `lib/connections/connector-catalog.ts`; `lib/launch/content.ts`; `app/spaces/[spaceId]/page.tsx` | 2026-07-08 | — | 2026-12-31 |

| F-MAINT-TICKETS | Maintenance Work Order Intake persists tickets (console overhaul Slice E): the previously-ephemeral capture now creates a tracked ticket with a lifecycle (Open / Waiting on Response / Waiting on Vendor / Scheduled / Closed), labels, assignment, notes, and an append-only Activity twin, in two new server-written / editor-read Firestore collections (`maintenance_tickets`, `maintenance_ticket_activity`; client writes denied in `firestore.rules`). Written via the Admin SDK boundary at the `edit` capability; timestamps are ISO strings (no serverTimestamp) so the writer is deterministic + unit-testable against a fake and the real SDK; closing requires a reason. App-plane only: no system-of-record write, no send; the RentVine work-order create stays gated (production_allowed:false) — the queue is its trigger/record, not its execution. | Verified | `lib/firestore/maintenance-tickets.ts`; `app/api/maintenance/tickets/route.ts`; `app/api/maintenance/tickets/[ticketId]/route.ts`; `components/maintenance/MaintenanceQueue.tsx`; `components/maintenance/MaintenanceCapture.tsx`; `firestore.rules`; `tests/unit/maintenance-tickets.test.ts`; `tests/unit/maintenance-tickets-route.test.ts` | 2026-07-08 | — | 2026-12-31 |

| F-ADMIN-USERS | In-app user + role management ships (console overhaul Slice D): `/admin/users` (Admin-only via `manageAdmin`) lists the roster from `getAuth().listUsers()` and changes a role by merging a `role` custom claim via `setCustomUserClaims` (the same mechanism as `scripts/set-firebase-user-role.mjs`, which stays the break-glass path for the first Admin). `setAppUserRole` enforces a valid role, a mandatory plain-English reason, the pmikcmetro.com domain boundary, and a last-Admin guard (never drop to zero Admins), then writes an append-only `admin_role_changes` audit record (server-write-only, Admin-read in `firestore.rules`). App-plane auth op only: no external system, no send, no budget cost, not an Action Registry entry. The live `listUsers`/`setCustomUserClaims` calls are owner-gated behind `manageAdmin`; the guard logic is unit-tested via an injected auth. | Verified | `lib/admin/users.ts`; `app/api/admin/users/route.ts`; `app/api/admin/users/[uid]/route.ts`; `components/admin/UserManagementPanel.tsx`; `lib/firestore/admin-role-changes.ts`; `firestore.rules`; `tests/unit/admin-users.test.ts`; `tests/unit/admin-users-route.test.ts` | 2026-07-08 | — | 2026-12-31 |

| F-ADMIN-IA | The Admin tab is re-sectioned into three labeled areas (console overhaul Slice D): People and Access (who can sign in + how a role is changed), Activity and Logs (Ask volume, queue depth, notification health, setup health), and App Info and Readiness (config, migration readiness, connected-service consoles). Pure re-layout of already-loaded data; the three admin pages now guard on the `manageAdmin` capability (matching the rest of the app) instead of `requirePageRole("Admin")`. The in-app role-change surface is built (see F-ADMIN-USERS); `firebase:set-role` remains the break-glass path for the first Admin. | Verified | `app/admin/page.tsx`; `app/admin/migration/page.tsx`; `app/admin/gmail-inbox-zero/page.tsx`; `app/globals.css` | 2026-07-08 | — | 2026-12-31 |

| F-RENEWAL-ATTENTION | The Renewal Desk leads with a needs-attention fold (console overhaul Slice C): a pure `buildRenewalAttention` projection over the actionable leases produces one deep-linked next action per lease, ordered most-attention-first (open source conflicts, then earlier-stage work, then soonest end date), rendered above the metric grid so the operator sees the work first. Read-only; each item deep-links to that lease's workspace where the action happens; no new data, no SoR write. The run-page resolve/approve/bulk reason forms also received the missing CSS so the mandatory reason box is usable. | Verified | `lib/lease-renewal/attention.ts`; `components/lease-renewal/RenewalDesk.tsx`; `app/globals.css`; `tests/unit/lease-renewal-attention.test.ts` | 2026-07-08 | — | 2026-12-31 |

| F-SPACE-CARD-COLOR | Space cards color-code their state (console overhaul Slice B): a pure state→tone map (`SPACE_CARD_STATE_TONE`) renders process-ready green, connections-needed amber, needs-a-process red (stays red), and read-only reference purple, always with the state label text so severity is never conveyed by hue alone. `computeSpaceCardState` and its four states are unchanged; the color is a presentation layer over them. | Verified | `lib/space-card-state.ts`; `app/spaces/page.tsx`; `styles/tokens.css`; `tests/unit/space-card-state.test.ts` | 2026-07-08 | — | 2026-12-31 |

| F-LEARN-LOOP | The Dan-decisions-teach-the-model learning loop is deterministic V1 (S13 Wave 3 H): rules + golden-set tuning, NOT model retraining. A value-free decision-metrics card projects accept/correct/dismiss rates + the reason-code distribution over the existing resolution/approval collections (counts only; a sentinel test pins the exact key set and forbids value-bearing fields; H1). The additive `reason_code` taxonomy now has seven defaults, including S14's value-free `accepted_suggestion`. Codes persist on both decision records + Activity and remain optional categorization for manual paths; D1 requires a code on the Low/Medium exact suggested-source path, where its label supplies the audited reason, while High/Blocked and every manual override still require free text. The shared `ReasonCodeSelect` remains on the full resolve + single/bulk approval forms, while the one-tap decider supplies the fixed code directly (H2/S14). An OFFLINE `npm run golden:distill` CLI pre-fills golden worksheets from value-free `(fieldKey, resolution_kind)` signals and NEVER auto-verifies (`reviewed` stays false; the team still applies; counts-only stdout, H3). A rule-tuning-as-PR loop = the `.github/pull_request_template.md` checklist + a `verify:redaction` gate (in CI + `verify.sh`) that fails if any file under `golden-data/`/`docs/client_docs/` is tracked, so ONLY rules/thresholds/synthetic scenarios reach GitHub (Josiah merges, Dan reviews in-app; H4). No client value on GitHub. | Verified | `lib/lease-renewal/decision-metrics.ts`; `lib/lease-renewal/reason-codes.ts`; `lib/lease-renewal/golden/distillation.ts`; `lib/firestore/lease-renewal-resolutions.ts`; `scripts/check-redaction.mjs`; `.github/pull_request_template.md` | 2026-07-10 | — | 2026-12-31 |

| F-DEFCYCLE-APPPLANE-1 | Deferred-cycle app-plane slices shipped (branch `console-overhaul-g-deferred-cycle`; app-plane, `production_allowed:false`, no SoR write, no send): (2c) the Maintenance queue gains a per-ticket collapsible History panel backed by a new read-gated `GET /api/maintenance/tickets/:id/activity` route (reuses `listMaintenanceTicketActivity`), and its status pills use maintenance-accurate color tones so the leaked renewal/approval vocabulary ("Ready for Approval"/"Approved") is gone (the visible pill text is always the real ticket status); (3c) a pure `planCallTasks` cadence planner extends `planNoticeReminders` to emit operator "make a call" tasks, deduped and suppressed within a contact-cadence window, with "last contact" sourced from the internal renewal-letter-sent date (never a Gmail read), surfaced dry-run via `npm run notices:reminders` (no Cloud Scheduler, no send). `docs/loop-state.md` was also pruned under the 140-line cap (older Stop-Condition detail archived to `docs/status.md`). Verified locally: 1119 unit tests, typecheck, lint, copy-voice, freshness, and the Turbopack build all green. | Verified | `components/maintenance/MaintenanceQueue.tsx`; `lib/lease-renewal/notice-reminders.ts`; `scripts/run-notice-reminders.ts`; `tests/unit/maintenance-queue-component.test.tsx`; `tests/unit/maintenance-ticket-activity-route.test.ts`; `tests/unit/lease-renewal-notice-reminders.test.ts` | 2026-07-09 | — | 2026-12-31 |

| F-GMAIL-RUNTIME-GATED | The per-user Gmail draft runtime is built TO THE GATE (deferred cycle): `lib/gmail-runtime/` acts AS the signed-in `pmikcmetro.com` user via keyless domain-wide delegation (injected transport; `encodeRawDraft`) and exposes ONLY `createDraft` (gmail.compose scope) — there is no send method, no messages.send call, and no send scope, so the ceiling is an unsent draft a human presses Send on. `lib/integrations/action-gate.ts` (`isActionExecutable`/`assertActionExecutable`) reads the committed `ACTION_REGISTRY_SEED` (not Firestore) and refuses execution unless `production_allowed:true`, which the schema permits only with readiness Approved-for-Execution + evidence Documented. The renewal Prepare-owner-email button (`PrepareOwnerEmailButton` + edit-gated `POST /api/lease-renewal/owner-notice-draft`) builds the addressed UNSENT draft via `buildOwnerNoticeDraftRequest` (verbatim DRAFT_BANNER) and returns a typed needs-Gmail-access refusal that makes NO Gmail call while `gmail.renewal_notice.draft_create` was `production_allowed:false` AT BUILD TIME. Verified with a fake transport (no real Gmail call from the dev/test shell). UPDATE 2026-07-09: that one entry is now flipped to `production_allowed:true` on the committed DWD grant — see [[F-GMAIL-RENEWAL-DRAFT-LIVE]]; the two Gmail Inbox 0 entries stay `Planned`, and live prod use still needs an owner-run deploy of the flipped seed. | Verified | `lib/gmail-runtime/client.ts`; `lib/gmail-runtime/dwd-token.ts`; `lib/integrations/action-gate.ts`; `app/api/lease-renewal/owner-notice-draft/route.ts`; `components/lease-renewal/PrepareOwnerEmailButton.tsx`; `tests/unit/gmail-runtime-client.test.ts`; `tests/unit/action-gate.test.ts`; `tests/unit/owner-notice-draft-route.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-MAINT-INTAKE-PUBLIC | The public tokenized maintenance intake (A5) is the app's ONE unauthenticated write endpoint, and it cannot reach the edit gate or a system of record. A staff member mints an HMAC-signed token (edit-gated `POST /api/maintenance/intake/token` or `npm run intake:mint`) scoped to a property; `POST /api/maintenance/intake/public` accepts it in an `X-Intake-Token` header and, only if valid, writes to the `maintenance_unverified_intake` QUARANTINE collection through the no-actor `createUnverifiedIntakeFromPublic` writer — which takes NO actor and imports neither `createMaintenanceTicket`, `requireCapability`, `lib/auth/session`, RentVine, nor the unit-matcher (pinned by a negative-import test), so an external reporter can never create a real ticket or trigger a send/SoR write. Token verify uses a length-guarded `timingSafeEqual` (a forged token yields a generic 401, never a 500/timing oracle), a version-domain-separated HMAC over the literal payload bytes, and a verify-side 30-day max-TTL. Abuse controls are transactional (reads-before-writes): a single-use nonce (jti) blocks replay, a per-property daily cap is the 503 kill-ceiling (owner budget safety), and a revocation epoch invalidates outstanding tokens; an in-instance token-bucket pre-gate keyed on the salted rightmost-XFF IP sheds bursts before any HMAC. Bodies stream under a hard 16 KB cap; every field is NFC-normalized + stripped of controls/bidi/zero-width + CSV-injection-neutralized; the 202 returns a fresh random reference (never the doc id or jti). Fails CLOSED (503) until the owner provisions `MAINTENANCE_INTAKE_TOKEN_SECRET`; ships `production_allowed:false` (no registry action — it is an app-plane quarantine write, not an external action). A new enumerating `tests/unit/route-auth-boundary.test.ts` asserts every `app/api/**/route.ts` is authed except an explicit 3-entry allow-list (`auth/session`, `auth/demo`, this route). Verified locally: full unit suite + the emulator rules test (all client writes to intake/activity denied; nonce/counter/epoch server-only). | Verified | `lib/maintenance/intake-token.ts`; `lib/maintenance/intake-sanitize.ts`; `lib/maintenance/intake-client-ip.ts`; `lib/maintenance/intake-rate-limit.ts`; `lib/firestore/maintenance-unverified-intake.ts`; `app/api/maintenance/intake/public/route.ts`; `app/api/maintenance/intake/token/route.ts`; `scripts/mint-maintenance-intake-token.ts`; `firestore.rules`; `tests/unit/route-auth-boundary.test.ts`; `tests/firestore/maintenance-intake.rules.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-MAINT-INTAKE-REVIEW | The public intake (A5) has an edit-gated triage surface (2d): `lib/firestore/maintenance-intake-review.ts` (a SEPARATE module from the no-actor public writer, so the writer keeps its negative-import isolation) exposes `listUnverifiedIntake` (edit-gated read), `promoteUnverifiedIntake`, and `dismissUnverifiedIntake`. Promotion is ONE atomic transaction: it creates a real `maintenance_tickets` ticket + its Activity twin AND flips the intake to "promoted" together, so a double-click/concurrent promote cannot create two tickets (it 404s a missing intake, 409s an already-triaged one). By default the promoted ticket is `reporter.kind:"external"`, `unit:null`, labelled "Needs Verification", with priority inferred from the report text (provenance "auto-inferred") unless the operator overrides it ("operator-set"); slice 2a lets the editor OPTIONALLY confirm the unit at promotion via the shared type-ahead, in which case the ticket carries { unitId, label } and the "Needs Verification" label is dropped (no selection keeps the unchanged default). Dismissal records a required reason on the append-only intake Activity. Routes: edit-gated `GET /api/maintenance/intake`, `POST /api/maintenance/intake/:id/promote`, `POST /api/maintenance/intake/:id/dismiss` — all covered by the enumerating route-auth-boundary invariant (only `/intake/public` is allow-listed unauthenticated). The maintenance page renders an `UnverifiedIntakeReview` panel (client-safe `lib/maintenance/intake-model` type; non-fatal degrade). App-plane only; promotion creates a KB ticket, never a system-of-record work order, never a send. Verified locally: full unit suite incl. atomic-promote + idempotency + route-auth + component tests. | Verified | `lib/firestore/maintenance-intake-review.ts`; `lib/maintenance/intake-model.ts`; `app/api/maintenance/intake/route.ts`; `components/maintenance/UnverifiedIntakeReview.tsx`; `app/maintenance/page.tsx`; `tests/unit/maintenance-intake-review.test.ts`; `tests/unit/maintenance-intake-review-routes.test.ts`; `tests/unit/maintenance-intake-review-component.test.tsx`; `lib/maintenance/unit-index.ts` | 2026-07-09 | — | 2026-12-31 |
| F-GMAIL-RENEWAL-DRAFT-LIVE | Historical transport evidence from 2026-07-09 proved keyless DWD could create and delete an unsent renewal draft. The current Action Registry deliberately returns `gmail.renewal_notice.draft_create` to Planned/false because the executable routes used sample workspace data and browser-supplied recipients. Owner and tenant routes now return unaddressed, non-executable deterministic previews; even a registry toggle cannot make them construct a Gmail client. Promotion requires a real authorized workflow run, authoritative recipient sources, approved template versions, bodyless workflow linkage, and tests. | Verified | `docs/evidence/gmail-dwd-grant-2026-07.md`; `lib/integrations/action-registry-seed.ts`; `app/api/lease-renewal/owner-notice-draft/route.ts`; `app/api/lease-renewal/tenant-notice-draft/route.ts`; `tests/unit/owner-notice-draft-route.test.ts`; `tests/unit/tenant-notice-draft-route.test.ts` | 2026-07-14 | F-GMAIL-ACTIONS-ACTIVE | 2026-12-31 |
| F-MAINT-ASSIGNEE | Maintenance tickets gain delegable ownership (2b): a per-ticket assignee picker + an "Assigned to me" queue filter, all edit-gated (Editors assign — NOT the manageAdmin admin path). The roster comes from `lib/maintenance/assignees.ts` `listAssignableUsers()`, which is demo-aware: in `localDemoAuth` (dev/test only, NODE_ENV-fenced) it returns the synthetic demo users so the picker is testable with a plain `npm run dev`; in prod it reads the real Firebase Auth roster (`listAppUsers`) filtered to non-disabled accounts INSIDE the allowed hosted domain (mirrors the defense-in-depth domain guard on the admin role path — an external/deactivated account is never pickable or assignable). The roster is fetched server-side on the already edit-gated maintenance page and passed to the client queue (no admin-route reuse, no new endpoint); the client type is a client-safe `lib/maintenance/assignee-model` `AssignableUser {uid,email}` so no firebase-admin reaches the bundle. Assign is validated server-side: the ticket PATCH route calls `isAssignableUser(uid)` and 400s a uid not in the roster (unassign = null skips it), and the assign schema is `z.string().trim().min(1).nullable()` so "" / whitespace is rejected and the checked value equals the persisted value. Privacy invariant preserved: the picker shows the assignee's EMAIL, never the raw uid; a ticket assigned to someone outside the roster shows a value-free "Assigned (outside roster)" option. "Assigned to me" is an app-plane view filter (no per-user Firestore rule for maintenance tickets). App-plane only; no SoR write, no send, no role grant, no notification. Adversarially reviewed (find→verify): the domain-filter gap + a check/write trim mismatch were caught and fixed. Verified locally: full unit suite incl. demo/prod roster, domain+disabled exclusion, route assign-validation, and picker/filter/privacy component tests. | Verified | `lib/maintenance/assignees.ts`; `lib/maintenance/assignee-model.ts`; `components/maintenance/MaintenanceQueue.tsx`; `app/maintenance/page.tsx`; `lib/firestore/maintenance-tickets.ts`; `tests/unit/maintenance-assignees.test.ts`; `tests/unit/maintenance-ticket-assign-route.test.ts`; `tests/unit/maintenance-queue-assignee.test.tsx` | 2026-07-09 | — | 2026-12-31 |
| F-MAINT-UNIT-TYPEAHEAD | Maintenance has a shared unit type-ahead (2a). `lib/maintenance/unit-index.ts` caches the whole outcome of the already-approved read-only RentVine /leases/export read (`loadLiveUnitCandidates`) for ~10 min (`UNIT_INDEX_TTL_MS`, mirrors `VERIFICATION_TTL_MS`) so the picker never fans out a live read per keystroke; `searchUnits` is a pure deterministic substring/token filter (no fuzzy join, no LLM). Edit-gated `GET /api/maintenance/units/search` serves { units } from that cache (401 unauth, 400 over-long q, 503 with error_type when unavailable) and is covered by the route-auth-boundary invariant. The client `UnitTypeahead` (debounced, non-fatal degrade, Enter never submits the form) replaces MaintenanceCapture's manual Find-unit field (a picked authoritative unit is `Verified`) and adds an OPTIONAL unit confirmation to the intake-review promote flow. RentVine read needs RentVine API creds, NOT Google ADC; demo-aware: `config.localDemoAuth` (NODE_ENV-fenced) serves synthetic `DEMO_UNIT_CANDIDATES` so the picker is exercisable with a plain `npm run dev`. App-plane only; no SoR write, no send; match-unit's fuzzy route is retained + still tested. | Verified | `lib/maintenance/unit-index.ts`; `app/api/maintenance/units/search/route.ts`; `components/maintenance/UnitTypeahead.tsx`; `tests/unit/maintenance-unit-index.test.ts`; `tests/unit/maintenance-units-search-route.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-RENEWAL-LIVE-ACTIONABLE | The owner-gated live renewal review is ACTIONABLE (slice 1b): the resolve form + the Admin approve/return/revoke write-back controls were extracted verbatim from `LeaseRenewalRunClient` into a shared "use client" module (`components/lease-renewal/flag-actions.tsx`: FlagResolveForm, WritebackApprovalControl, WritebackProposalCard) and reused by `LiveRenewalReview` on the manageAdmin-gated live page. BLOCKER FIXED: `/api/lease-renewal/resolve` defaulted its run resolver to `getSimulationRun`, so a live-review flag (run_id `live-review`) 404'd; the route now injects `resolveRenewalRun`, which rebuilds the live run for `LIVE_REVIEW_RUN_ID` (`rebuildLiveRenewalRun` reuses the SAME tabs+runId as the page so source_trigger_key `lease_renewal:reconcile:live-review:{field}` matches — the key is derived from runId+field_key only, never the read timestamp) and falls back to `getSimulationRun` otherwise; `RunResolver` was widened to an async resolver and the data layer now awaits it. The live page loads the run's resolutions/approvals/activity (try/catch degrade) through a new `loadLiveRenewalReview` overlay; the approve/return/revoke route needed NO change (it reads the stored resolution). Queue-only: production_allowed:false, no system-of-record write, no send; the Action Registry EXECUTABLE_ALLOWLIST is unchanged. | Verified | `components/lease-renewal/flag-actions.tsx`; `components/lease-renewal/LiveRenewalReview.tsx`; `lib/lease-renewal/resolve-run.ts`; `lib/lease-renewal/live-review.ts`; `lib/firestore/lease-renewal-resolutions.ts`; `app/api/lease-renewal/resolve/route.ts`; `app/lease-renewal/live/page.tsx`; `tests/unit/live-renewal-review.test.tsx`; `tests/unit/lease-renewal-resolve-run.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-RENEWAL-PROPERTY-REPO | Per-property lease-renewal decision repository (slice 1c): a pure property-repository joins each simulation run's ADDRESS-joined flags to a canonical property key (deriveAddressKey of the record's join value, surfaced as ReconciledFieldOutcome.propertyKey) and buckets the EXISTING append-only resolution + write-back-approval Activity by property. Each surfaced entry is VALUE-FREE — exactly {actorUid, action, timestamp, reason}, never an address / field value / field_key / proposed value (sentinel-pinned). Because a flag's source_trigger_key is run+field, a key raised by two or more properties is attributed to NEITHER (no cross-property bleed; golden-tested), and NAME-joined fields (no address) are excluded. A new manageAdmin-only /lease-renewal/property/[propertyKey] server page renders one property's decision history, degrading non-fatally when Firestore is unavailable. Reuses the existing lease_renewal_resolution_activity + lease_renewal_writeback_approval_activity collections via single-field run_id equality reads — no new collection or index. App-plane only: no system-of-record write, no send, production_allowed:false throughout. | Verified | `lib/lease-renewal/property-repository.ts`; `lib/lease-renewal/pipeline.ts`; `lib/firestore/lease-renewal-resolutions.ts`; `tests/unit/lease-renewal-property-repository.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-GMAIL-DRAFT-COMPOSER | The anticipatory AI draft-TEXT composer for Gmail Inbox 0 (deferred-cycle bullet 3, slice 3a) lives at `lib/gmail-inbox-zero/anticipatory-draft.ts` and runs through the ModelProvider seam (`lib/llm/model-provider.ts`), so a free local model stands in for Gemini in dev/test and Gemini runs in prod (NODE_ENV-fenced; provider + model are injected by the caller, never built here — mirrors `lib/processes/classify.ts`). The deterministic `buildReplyDraft` spine (`lib/gmail-inbox-zero/drafts.ts`) runs FIRST: an unapproved reply template or a hard-excluded category (Owner money / Legal/notices / Tenant disputes) is refused with `refusedBeforeModel:true` BEFORE the model is invoked, so the model never sees excluded mail. The verbatim `DRAFT_BANNER` and the `Needs Verification: <fact>` placeholders are re-applied deterministically by re-running `buildReplyDraft` over the model's tailored body, a leading banner the model wrongly emits is stripped so it is never duplicated, and any model/setup throw, non-JSON, wrong-shape, or empty output degrades non-fatally to the deterministic template draft (`usedModel:false`). Single-thread explicit invoke only (one call composes one thread; no batch/inbox sweep), keeping on-demand Flash usage under the $10 cap. The file is registered in `CLIENT_DRAFT_FILES` (`scripts/check-copy-voice.mjs`) so em dashes in it hard-fail copy-voice. Pure app-plane text: NO Gmail API call, no send capability, no Action Registry entry touched (the Gmail Inbox 0 entries stay Planned / `production_allowed:false`; the per-user Gmail runtime stays gated). Verified locally with a fake ModelProvider: spine-refuses-before-model (call count 0), banner-exactly-once, placeholder re-apply, model-banner-strip, and every degrade path; full unit + copy-voice + freshness sweep green. | Verified | `lib/gmail-inbox-zero/anticipatory-draft.ts`; `lib/gmail-inbox-zero/drafts.ts`; `lib/llm/model-provider.ts`; `scripts/check-copy-voice.mjs`; `tests/unit/gmail-inbox-zero-anticipatory-draft.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-NOTIF-FRAMEWORK | An in-app notification framework unifies the app's notifications (console overhaul Slice 3b): a client-safe family catalog (`NOTIFICATION_FAMILIES`) with two AVAILABLE in-app families (Approvals, Maintenance tickets) and two STUBBED families (RentVine replies, Owner replies) rendered available:false with "Waiting on Gmail access". Maintenance-lifecycle notifications are written PII-FREE (event + ticket status only; no summary/unit/reporter) INSIDE the existing ticket transactions to a new `maintenance_ticket_notifications` collection, for the ticket's assignee only and never the actor who made the change. A pure `buildNotificationFeed` merges the per-user approval-queue + maintenance notifications newest-first, drops muted families, and returns the family views; read-gated `/api/notifications` serves it, `/api/notifications/mark-read` dispatches the per-source read flip. Per-user in-app preferences live in `user_notification_preferences` (self-scoped, doc id = uid) via `notification-preferences.ts`; the framework is IN-APP ONLY and email stays hard-off (no email path here; KB_APPROVAL_NOTIFICATIONS_ENABLED stays false). Both new collections are server-written via the Admin SDK boundary and client-read-only (own records only) in `firestore.rules`. The `NotificationMenu` reads the unified feed and shows the stubbed families. App-plane only: no SoR write, no send, no Action Registry flip. | Verified | `lib/notifications/families.ts`; `lib/notifications/feed.ts`; `lib/firestore/maintenance-ticket-notifications.ts`; `lib/firestore/notification-preferences.ts`; `lib/firestore/maintenance-tickets.ts`; `components/layout/NotificationMenu.tsx`; `firestore.rules`; `tests/firestore/notifications.rules.test.ts`; `tests/unit/notification-feed.test.ts`; `tests/unit/maintenance-ticket-notifications.test.ts` | 2026-07-09 | — | 2026-12-31 |
| F-APPROVAL-QUEUE-UNIFIED | The Approval Queue collapses its four peer tabs into ONE presentation (Slice 4a, evolved by S14): the value-free, attention-ordered "Needs your decision" list is the always-visible landing surface, and All items + Renewal reviews + Write-back proposals live behind an "Other views" disclosure. Plain-language labels and one primary deep link per row remain. S14 extends the pinned `NeedsDecisionRow` shape with exactly one value-free capability boolean, `canApproveInline`: only a Low/Medium, Ready, actor-eligible, non-self-assigned `queue_item` may render the existing app-plane Approve action. High-risk or self-assigned queue items and every renewal-flag/write-back row remain deep-link-only; values, reasons, deciders, and assignee UIDs never enter the row. All-items transitions and notification deep links are unchanged. No new external action, system-of-record write, send, or registry-state change was added; the value-free and self-approval sentinels were extended, never weakened. | Verified | `components/approval/ApprovalQueue.tsx`; `components/approval/NeedsDecisionInboxPanel.tsx`; `components/console/ConsoleView.tsx`; `lib/approval/needs-decision-inbox.ts`; `tests/unit/approval-queue-component.test.tsx`; `tests/unit/needs-decision-inbox.test.ts` | 2026-07-10 | — | 2026-12-31 |
| F-RUNNER-AGNOSTIC | Governance is runner-neutral: `AGENTS.md` is the single durable source. `CLAUDE.md` remains a compatibility pointer for Claude Code, while Codex has no repo-tracked harness adapter and reads `AGENTS.md` plus `docs/` directly; `scripts/check-router-boundary.mjs` requires the neutral framing and no longer requires a Codex config file. The six-identity checklist row (a) is generalized off "Claude" in `AGENTS.md` + `scripts/preflight-identity.mjs`. The same plan->build->verify+falsify->stop loop is invoked identically under either runner. Docs/gate only; no runtime change. | Verified | `AGENTS.md`; `scripts/check-router-boundary.mjs`; `scripts/preflight-identity.mjs` | 2026-07-11 | — | 2026-12-31 |
| F-ADVERSARIAL-CI-GATE | The adversarial + freshness gates now bind every PR regardless of authoring runner: `.github/workflows/ci.yml` runs `verify:falsification`, `verify:context-freshness`, and the new `verify:spec-traceability` (previously local-only via `scripts/verify.sh`). CI is runner-agnostic, so the adversarial pass applies equally to Claude- and Codex-authored work. | Verified | `.github/workflows/ci.yml`; `scripts/verify.sh`; `package.json` | 2026-07-10 | — | 2026-12-31 |
| F-SPEC-TRACEABILITY | New/overhaul feature-suite specs use `docs/feature-suites/TEMPLATE.md` (a `<!-- spec-shape: overhaul-v1 -->` sentinel plus two extra sections — Adversarial acceptance checks with stable `AC-S<n>-<k>` ids, and Forbidden actions / hard gates). `tests/unit/feature-suite-spec-shape.test.mjs` enforces the shape + README registration of every sentinel'd spec; `scripts/check-spec-traceability.mjs` (`verify:spec-traceability`) enforces AC-id uniqueness, suite-number match on BOLD declarations only (so cross-spec references are allowed), and that any `AC-` id cited in `docs/facts.md` resolves — a durable spec->implementation check the loop's falsification phase records. The 13 pre-existing S1–S13 specs keep their original shape. | Verified | `docs/feature-suites/TEMPLATE.md`; `tests/unit/feature-suite-spec-shape.test.mjs`; `scripts/check-spec-traceability.mjs`; `docs/autonomous-agent-runner.md`; `docs/meta-prompts/scaffold.md` | 2026-07-10 | — | 2026-12-31 |
| F-RENEWAL-DECIDER-MOBILE | S14's buildable app-plane tier is implemented on the existing simulation-run and live-review pages: a phone-first mode renders one severity-ordered renewal flag at a time with N-of-M controls and collapsed diagnostics; Low/Medium exact suggested-source acceptance posts the fixed `accepted_suggestion` code without typed text and stamps its label into the resolution + Activity; High/Blocked and all manual overrides still require free text; a queued safe acceptance exposes a distinct Admin-only one-tap write-back authorization; per-user Skip progress persists transactionally with an Activity twin while suppression is limited to the same browser session (a new session resurfaces unresolved work); and safe Low/Medium Approval Queue items may approve inline while High, self-assigned, renewal-flag, and write-back rows remain deep-link-only. The desktop card stack and bulk bar remain available, every external execution gate remains unchanged, and no write-back is executed. Covers AC-S14-1, AC-S14-2, AC-S14-3, AC-S14-4, AC-S14-5, AC-S14-6, AC-S14-7, AC-S14-8, and AC-S14-9. | Verified | `components/lease-renewal/RenewalDecider.tsx`; `components/lease-renewal/RenewalDeciderCard.tsx`; `lib/firestore/lease-renewal-resolutions.ts`; `lib/firestore/lease-renewal-writeback-approvals.ts`; `lib/firestore/renewal-decider-progress.ts`; `lib/approval/needs-decision-inbox.ts`; `tests/unit/renewal-decider.test.tsx`; `tests/unit/lease-renewal-resolutions.test.ts`; `tests/unit/renewal-decider-progress.test.ts`; `tests/unit/needs-decision-inbox.test.ts` | 2026-07-10 | — | 2026-12-31 |

| F-RBAC-SUBUSERS | S16 role-scoped sub-users ship app-plane: a SECOND orthogonal Firebase custom claim `scopes` (`SPACE_SCOPES` = renewals + maintenance in `lib/constants.ts`, with `SPACE_SCOPE_HOME` landing hrefs) narrows WHICH spaces a principal may enter, layered on the unchanged `role` capability tier — a maintenance sub-user is `{role:"Editor", scopes:["maintenance"]}`. `validateAuthClaims` parses it: absent/null/"" ⇒ `undefined` = ALL-spaces wildcard (so today's Dan/Josiah need no re-claim); an empty array OR an unknown scope THROWS `AuthError` 403; the value is canonicalized to `SPACE_SCOPES` order and frozen. Two axes compose — `requireCapabilityInSpace`/`requireSpaceAccess` (API 403) and `requirePageSpaceAccess` (page redirect to `primarySpaceHref`, never /sign-in for an authenticated scope miss) admit a surface only if `role` satisfies the capability AND `scopes` include the space-scope; scopes only SUBTRACT reach (a maintenance Editor still cannot approve inside maintenance). Every scoped route carries the guard: all 5 lease-renewal + 8 approval-queue routes require "renewals", all 11 maintenance routes require "maintenance", and the unauthenticated HMAC `maintenance/intake/public` stays unguarded (F-MAINT-INTAKE-PUBLIC); the enumerating `route-auth-boundary` invariant is EXTENDED with a `SCOPED_ROUTE_SCOPE` map (wrong-scope caught by the scope literal in the regex) without weakening the base every-route-authed check or its 3-entry allow-list. Nav (AppShell), the Spaces directory + `/spaces/[spaceId]` (server-side redirect, not merely hidden), and the Console deck/process-strip drop out-of-scope items; S21 adds a required `space_id` for route-created processes and checks record-level Space scope on process read/edit/test/publish paths. The `hd===pmikcmetro.com` boundary is untouched. A manageAdmin-only `PATCH /api/admin/users/[uid]/scopes` (`setAppUserScopes`, sibling of `setAppUserRole`) MERGES the claim so the target's `role` is preserved, enforces the domain boundary + a ≥3-char reason + empty/unknown rejection BEFORE `setCustomUserClaims`, and appends exactly one `admin_scope_changes` audit; "All spaces" clears the claim (never writes []). App-plane auth op only: no Action Registry entry, SoR write, send, or Google scope. Covers AC-S16-1, AC-S16-2, AC-S16-3, AC-S16-4, AC-S16-5, AC-S16-6, AC-S16-7, AC-S16-8, and AC-S16-9. RESIDUAL: the scope audit + claim merge remain best-effort/non-atomic; future scoped prefixes must be added to the curated route map; workflow-run and Ask cross-cutting access retain their prior capability/scope behavior. | Verified | `lib/constants.ts`; `lib/auth/session.ts`; `lib/auth/page-guards.ts`; `lib/admin/users.ts`; `lib/firestore/admin-scope-changes.ts`; `lib/space-scope-resources.ts`; `app/api/process-definitions`; `components/admin/UserManagementPanel.tsx`; `components/layout/AppShell.tsx`; `app/spaces/[spaceId]/page.tsx`; `components/console/ConsoleView.tsx`; `tests/unit/route-auth-boundary.test.ts`; `tests/unit/space-scope.test.ts`; `tests/unit/auth-session.test.ts`; `tests/unit/page-guards.test.ts`; `tests/unit/admin-scopes.test.ts`; `tests/unit/admin-scopes-route.test.ts`; `docs/auth-identity-and-access-strategy.md` | 2026-07-14 | — | 2026-12-31 |

| F-UNIFIED-ATTENTION | S17 unified console + attention hub ships app-plane (D2, owner 2026-07-10): the Console stays the at-a-glance home and a new read-only `/notifications` hub is a SUPERSET time-ordered LOG the same signals ALSO flow into. B3 — ONE value-free attention contract (`lib/attention/lanes.ts`): a closed 5-lane enum (decision, connection, coverage, renewal, review) plus a `toAttentionSignal` WHITELIST constructor copying exactly six value-free keys (lane, severity, label, detail, href, signal_key), so no address, proposed value, free-text reason, field_key, reason_code, or decider can serialize onto the deck, hub, or renewal desk; the deck, the bell/hub, and the renewal fold (`buildRenewalAttention` now stamps the `renewal` lane) all derive lane + severity from it. B1 — the read-gated, auth-first `/notifications` hub page + a bell "See all notifications" link. B2 — two AVAILABLE standing families (`connections_setup`, `space_coverage`) projected value-free from `resolveConnectionsState`/`resolveCoverageState` (env + connector presence + the app's own Firestore only; ZERO external calls, no RentVine/Sheet/Gmail client), rendered in a "Set-up" section so the hub is a true superset of the deck's three cards. B4 — an additive low-alarm layer on `user_notification_preferences` (`lane_thresholds`/`snoozed_lanes`/`digest_lanes`, MERGE-PRESERVED so a mute-only bell PATCH never wipes them) plus a pure resolver (per-lane min-severity threshold, snooze-until, digest collapse of N rows to one). B5 — an Admin-only, value-free, muteable `team_review` DIGEST: ONE signal per period ("N high-risk overrides, M self-corrections", never one ping per edit; structurally one-or-null) projected over the existing value-free decision metrics. B6 — `POST /api/notifications/mark-all-read` (read-gated, self + space-scoped) that flips every unread EVENT notification and leaves the standing families untouched, plus richer per-lane all-clear copy. B7 — the S16-scope-filtered Console deck is lane-stamped (every card + row carries a `data-lane` from the closed enum); this suite adds NO scope filter of its own (S16 owns it, AC-S16-4) and NO new approve/send/write affordance beyond the existing A4 in-place approve. The family catalog is exactly SEVEN (five available incl. team_review Admin-gated at serve time; the two Gmail-dependent families stay `available:false` "Waiting on Gmail access"). Adversarially verified 2026-07-10 (three independent fresh-context falsifiers, all nine ACs HOLD, no production breakage) and browser-verified (the hub renders the Notifications heading, the all-clear log, and both standing lanes as a superset of the deck); the surfaced test-coverage gaps were then closed (a `hub.ts` negative-import scan, a page auth-order guard, the full-mode Admin-review-signal gate, and the decision-count single-source invariant). 1440 unit tests, typecheck, lint, copy-voice, falsification, redaction, and the production build all green. PRECISION on AC-S17-3: the decision integer is single-sourced from `gatherNeedsDecisionInbox`/`buildNeedsDecisionInbox` (`counts.total`) for the deck, the Console approvals answer, the Space card, and the Approval Queue (the S13 B5 interlock is EXTENDED, not weakened); the notification bell BADGE is the unread-EVENT-log count, a distinct metric by the D2 superset-log design, NOT the decision count. NEEDS-VERIFICATION (Q-DAN-REVIEW-CADENCE): `self_corrections` currently counts returned write-back authorizations (the observable proxy) and the digest window is a display default, both pending Dan's cadence confirmation; the fuller "later stale/re-resolved" definition is a documented future refinement. App-plane only: no Action Registry entry, no SoR write, no autonomous send, no new Google scope, no Cloud Scheduler; email stays hard-off (`email_enabled` literal-false); every surface `production_allowed:false`; the two Gmail-dependent families (G1), any out-of-app delivery (G2), a scheduled digest (G3), and deploy (G4) stay owner/vendor-gated. Covers AC-S17-1, AC-S17-2, AC-S17-3, AC-S17-4, AC-S17-5, AC-S17-6, AC-S17-7, AC-S17-8, AC-S17-9. | Verified | `lib/attention/lanes.ts`; `lib/attention/standing-signals.ts`; `lib/attention/review-lane.ts`; `lib/notifications/feed.ts`; `lib/notifications/families.ts`; `lib/notifications/low-alarm.ts`; `lib/notifications/hub.ts`; `app/notifications/page.tsx`; `app/api/notifications/route.ts`; `app/api/notifications/mark-all-read/route.ts`; `components/layout/NotificationMenu.tsx`; `components/console/ConsoleActionDeck.tsx`; `components/console/ConsoleView.tsx`; `lib/lease-renewal/attention.ts`; `lib/lease-renewal/decision-metrics.ts`; `lib/firestore/notification-preferences.ts`; `lib/firestore/schemas.ts`; `tests/unit/attention-lanes.test.ts`; `tests/unit/notification-feed.test.ts`; `tests/unit/notification-hub.test.ts`; `tests/unit/lease-renewal-decision-metrics.test.ts`; `tests/unit/notifications-route.test.ts`; `tests/unit/console-action-deck.test.tsx` | 2026-07-10 | — | 2026-12-31 |

| F-GMAIL-HUB | The Gmail workflow HUB ships app-plane TO-THE-GATE (S15, owner decision D3 2026-07-10): the two verified-but-invisible engines get their first user-facing surface, and NO Gmail READ scope is requested this cycle. (1) Edit-gated `POST /api/gmail-hub/anticipatory-draft` builds the injected `createModelProvider` (prod-fenced to Gemini via `readServerConfig`) and calls `composeAnticipatoryReplyDraft` over a PASTED `ReplyTemplate` + `TriageMessageFacts`; the deterministic `buildReplyDraft` spine runs FIRST, so an unapproved template or a hard-excluded category (Owner money / Legal-notices / Tenant disputes) refuses with `refusedBeforeModel:true` and the model is never called; the route imports no `@/lib/gmail-runtime/*` (negative-import pinned) and makes NO Gmail call. (2) A client-side `TemplateWorkspace` (replacing the static `app/admin/gmail-inbox-zero/page.tsx` placeholder — see supersede COPY-GMAIL-READONLY-V1) runs `evaluateInboxTriage` + `buildReplyDraft` over pasted facts against synthetic `SAMPLE_LABEL_RULES`/`SAMPLE_REPLY_TEMPLATES` (no PII), surfacing the Approved-only / hard-exclusion / rollout-phase invariants with no Send control. (3) A pure `lib/gmail-inbox-zero/thread-summary.ts` composer (mirrors the anticipatory-draft DI + non-fatal degrade contract) turns pasted thread text into `{summary, waiting_on, suggested_next_action}` via edit-gated `POST /api/gmail-hub/thread-summary` (empty/whitespace paste → typed 400; any throw/non-JSON/empty output degrades `usedModel:false`; no gmail-runtime import). (4) The `/gmail-hub` read-capability page + `GmailHubHome` present drafts+templates+summaries with the in-app `NOTIFICATION_FAMILIES`; every live-mailbox affordance (the "read my inbox" control + the `rentvine_replies`/`owner_process_replies` families) renders disabled carrying the single now-exported `WAITING_ON_GMAIL` = "Waiting on Gmail access" literal; an unscoped "Gmail Hub" AppShell nav entry is added. (5) A `PrepareTenantEmailButton` + edit-gated renewals-scoped `POST /api/lease-renewal/tenant-notice-draft` TWIN the owner button/route: `buildTenantNoticeDraftRequest` returns a `channel:"tenant"`, `production_allowed:false` / `send_allowed:false`, verbatim-DRAFT_BANNER request, and it REUSES the one pre-approved compose-only gate `gmail.renewal_notice.draft_create` ([[F-GMAIL-RENEWAL-DRAFT-LIVE]]) — adding NO new registry entry and flipping NO gate (`isActionExecutable` stays true for EXACTLY that key; `gmail.label.apply`/`gmail.draft.create` stay Planned/false). (6) The reframed per-user Owner Email space now opens the hub (`spaceHref("owner-email") → /gmail-hub`); its read-only detail page stays honest and links into the hub. Builds the missing SURFACE for [[F-GMAIL-DRAFT-COMPOSER]] + [[F-NOTIF-FRAMEWORK]] and updates (does not weaken) the [[F-ADMIN-IA]] `app/admin/gmail-inbox-zero/page.tsx` reference. App-plane only: no Gmail read scope (`verify:router-boundary` green — no `gmail.readonly`/`gmail.modify` literal), no autonomous send (createDraft-only ceiling, no send method), no SoR write; live prod renewal-draft use and any inbox READ stay owner/vendor-gated (client Gmail access model + a NEW DWD read grant + owner-run deploy). Verified locally 2026-07-10: 1473 unit tests, typecheck, lint, router-boundary, copy-voice, spec-traceability, freshness, and the production build all green. Covers AC-S15-1, AC-S15-2, AC-S15-3, AC-S15-4, AC-S15-5, AC-S15-6, AC-S15-7. | Verified | `app/gmail-hub/page.tsx`; `components/gmail-hub/GmailHubHome.tsx`; `components/gmail-hub/AnticipatoryDraftComposer.tsx`; `components/gmail-hub/TemplateWorkspace.tsx`; `components/gmail-hub/ThreadSummaryPanel.tsx`; `app/api/gmail-hub/anticipatory-draft/route.ts`; `app/api/gmail-hub/thread-summary/route.ts`; `lib/gmail-inbox-zero/thread-summary.ts`; `lib/gmail-inbox-zero/sample-hub.ts`; `app/api/lease-renewal/tenant-notice-draft/route.ts`; `components/lease-renewal/PrepareTenantEmailButton.tsx`; `app/admin/gmail-inbox-zero/page.tsx`; `lib/spaces.ts`; `components/layout/AppShell.tsx`; `lib/notifications/families.ts`; `tests/unit/gmail-hub-anticipatory-draft-route.test.ts`; `tests/unit/gmail-hub-template-workspace.test.tsx`; `tests/unit/gmail-hub-thread-summary.test.ts`; `tests/unit/tenant-notice-draft-route.test.ts`; `tests/unit/gmail-hub-action-gate.test.ts` | 2026-07-10 | — | 2026-12-31 |

| F-ANTICIPATION-LANE | The Console gains a read-only "Anticipated work" lane (S18 process-auto-initiation, owner-default: "initiation" = app-plane draft/queue creation only — the app ANTICIPATES + PROPOSES work a human starts; it never auto-runs, sends, or writes, and real scheduled/cron auto-initiation stays hard-gated). A pure `buildAnticipatedWork({referenceDateIso, deskView, ruleSet})` (`lib/anticipation/projection.ts`) folds the renewal desk's `classifyRenewalCohort` output + `planNoticeReminders` + `planCallTasks` over the in-boundary SAMPLE batch into a VALUE-FREE `AnticipatedWorkList`: one group per owner-named family (lease-renewals, owner-renewal-outreach, tenant-renewal-notice, maintenance-work-order-intake) plus a named compliance/new-user placeholder, each carrying EXACTLY `{processDefinitionId, spaceId, spaceName, category, count, urgency, summary, startHref}` — no address, rent, tenant name, or lease-end date crosses onto the list (sentinel-pinned, same posture as the needs-decision inbox + write-back queue). No `Date.now`/I-O (the reference date is an input); two calls are deep-equal. `components/console/ConsoleAnticipatedWork.tsx` (a read-only sibling of `ConsoleProcessStrip`, wired into `ConsoleView.tsx`'s non-fatal gather, renewals-scoped like the approvals gather) renders each family's value-free count + urgency (`overdue`/`due-soon`/`upcoming`/`all-clear`/`no-source-yet`) with a permanent "Computed on request · this never runs on a schedule and never sends." caption and, when all-clear, the exact "All clear — nothing is coming up right now."; an un-fed family shows the `no-source-yet` placeholder and NO startable item. The one-click start reuses the EXISTING `POST /api/process-definitions/{id}/test-runs` (`components/console/StartTestRunButton.tsx`, editor-only via `canStartSimulation`; a viewer or a definition-less family gets a read-only `spaceHref` deep link, never a start control) and lands on `/workflow-runs/{id}` — simulation-only, no new endpoint, no send, no SoR write. The renewal-family counts reconcile with `npm run notices:reminders -- --date=2026-07-14 --json` (notice_due_soon=2 → tenant-notice count 2; callTasks=0 → owner-outreach count 0; cohort actionable=3 → lease-renewals count 3), so the lane cannot fork from the planners. A repo scan confirms NO cron/`setInterval`/Scheduler in `lib/anticipation` or `components/console`. Extends [[F-CONSOLE-APP-STATE]] + [[F-NOTICE-ENGINE]] (the anticipation lane S13 left as a manual CLI dry-run) and honors [[F-PRECUST-CYCLE]]'s no-Cloud-Scheduler stop + [[F-WRITE-GATE]]; additive, supersedes nothing. App-plane only: no Action Registry entry, no send, no SoR write, no new Google scope, no scheduler, ~$10 cap, deploy owner-run; the LIVE lease/meeting feed and real cron auto-initiation stay owner/vendor-gated. Verified locally 2026-07-10: 1484 unit tests, typecheck, lint, format:check, copy-voice, router-boundary, spec-traceability, freshness, falsification, and the production build all green; adversarial per-AC falsification. Covers AC-S18-1, AC-S18-2, AC-S18-3, AC-S18-4, AC-S18-5, AC-S18-6, AC-S18-7, AC-S18-8, AC-S18-9. | Verified | `lib/anticipation/projection.ts`; `components/console/ConsoleAnticipatedWork.tsx`; `components/console/StartTestRunButton.tsx`; `components/console/ConsoleView.tsx`; `app/globals.css`; `tests/unit/anticipation-projection.test.ts`; `tests/unit/console-anticipated-work.test.tsx` | 2026-07-10 | — | 2026-12-31 |

| F-GMAIL-DRAFT-SAFETY | Gmail Hub draft safety now uses canonical category ids (`vendor`, `scheduling`, `general_question`, `owner_money`, `legal_notices`, `tenant_disputes`) rather than display-label equality. One deterministic normalizer handles case, NFKC width, punctuation, slash/hyphen/underscore, whitespace, singular/plural, and approved aliases. The edit-gated route treats client text as untrusted and scans category, subject, and pasted fact names before reading model config or constructing the provider; the pure composer repeats the check before invocation. Unknown/blank, hard-excluded, or obvious excluded intent returns `refusedBeforeModel:true`, with both provider construction and model call count zero at the server boundary. UI category controls emit stable ids. Mailbox-read and no-send gates are unchanged. | Verified | `lib/gmail-inbox-zero/constants.ts`; `lib/gmail-inbox-zero/draft-safety.ts`; `lib/gmail-inbox-zero/drafts.ts`; `lib/gmail-inbox-zero/anticipatory-draft.ts`; `app/api/gmail-hub/anticipatory-draft/route.ts`; `components/gmail-hub/AnticipatoryDraftComposer.tsx`; `tests/unit/gmail-draft-safety.test.ts`; `tests/unit/gmail-hub-anticipatory-draft-route.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-ATTENTION-DECISION-SUPERSET | Console and full Notifications requests now call one canonical `gatherDecisionAttention` boundary, which wraps `gatherNeedsDecisionInbox` once and projects its `counts.total` plus rows through the six-key `toAttentionSignal` whitelist. The full hub renders that actionable backlog as a distinct standing decision lane even when the unread event log is empty. Event-empty copy says only that no recent event notifications exist; it cannot claim global all-clear while decisions or setup conditions remain. The bell badge remains unread-event-only. Scope filtering and value-free serialization are unchanged. | Verified | `lib/attention/decision-backlog.ts`; `components/console/ConsoleView.tsx`; `lib/notifications/hub.ts`; `lib/notifications/feed.ts`; `app/notifications/page.tsx`; `tests/unit/decision-attention-backlog.test.ts`; `tests/unit/notification-hub.test.ts`; `tests/unit/notification-feed.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-MAINT-TICKET-READINESS | A maintenance ticket may be persisted only from a blocker-free draft with a non-empty trimmed description and an explicitly selected `Verified` unit. The client retains blocker-bearing previews but disables Create with linked guidance, invalidates the draft immediately when its inputs or selected unit change, and deduplicates pending creation. The route and writer both parse the strict schema before any transaction, so crafted missing/blank/unverified input creates no ticket or Activity twin. The queue remains KB app-plane only; no RentVine work-order execution was added. | Verified | `components/maintenance/MaintenanceCapture.tsx`; `components/maintenance/UnitTypeahead.tsx`; `lib/firestore/maintenance-tickets.ts`; `app/api/maintenance/tickets/route.ts`; `tests/unit/maintenance-capture.test.tsx`; `tests/unit/maintenance-tickets-route.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-ANTICIPATED-STARTABILITY | The Console anticipation lane derives startability from the process definitions actually loaded for the principal: edit permission, in-scope presence, and a non-Retired state are all required before rendering Start. Missing/out-of-scope/retired definitions render the space link. A definition that disappears between render and POST changes to actionable inline recovery, and an in-flight ref prevents duplicate test-run starts. The existing test-run service remains `simulation_only:true`; no scheduler/send/system-of-record path was added. | Verified | `components/console/ConsoleView.tsx`; `components/console/ConsoleAnticipatedWork.tsx`; `components/console/StartTestRunButton.tsx`; `tests/unit/console-anticipated-work.test.tsx`; `lib/firestore/workflows.ts` | 2026-07-13 | — | 2026-12-31 |
| F-RENEWAL-CONFIRM-HISTORY | High/Blocked flag resolution uses an in-app `role=dialog` confirmation with required free-text validation before open, focus trap, Escape/cancel, focus return, preserved inputs, explicit confirm, and duplicate-request prevention; resolution authorization remains distinct from write-back approval. Admin property-history navigation is now reachable from sample renewal records and from run/live flags only when `buildRunPropertyKeyIndex` proves the source trigger maps to one canonical property. Ambiguous/property-less triggers receive no destination. The history route retains renewals-scope plus Admin guards, value-free oldest-to-newest entries, graceful Firestore-unavailable copy, and a validated internal return path. | Verified | `components/lease-renewal/flag-actions.tsx`; `lib/lease-renewal/run-view.ts`; `lib/lease-renewal/property-history-link.ts`; `app/lease-renewal/lease/[leaseId]/page.tsx`; `app/lease-renewal/property/[propertyKey]/page.tsx`; `tests/unit/live-renewal-review.test.tsx`; `tests/unit/lease-renewal-property-repository.test.ts`; `tests/unit/property-history-link.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-DICTATE-LIFECYCLE | Console Dictate exposes requesting-permission, recording, stopping, processing, transcript-appended, no-speech, and error states visibly and through one polite atomic live region. Transcript insertion is explicitly announced, existing typed text is preserved, focus returns to Dictate after completion/error, rapid duplicate permission requests are ignored, and unmount stops the recorder/stream. The STT route/provider and production fence are unchanged. | Verified | `components/hooks/useAudioRecorder.ts`; `components/ask/AskForm.tsx`; `tests/unit/use-audio-recorder-lifecycle.test.tsx`; `tests/unit/ask-form.test.tsx` | 2026-07-13 | — | 2026-12-31 |
| F-PHONE-OVERFLOW-CONTAINMENT | Gmail Hub and expanded Approval Queue phone layouts constrain their actual nested grids, panels, forms, controls, previews, disclosures, rows, links, and chips with shrinkable tracks and wrapping. Gmail removed the nested `.content` wrapper and constrains long selects/facts/previews. Approval Queue keeps its tablist as the only intentional local horizontal scroller. No `body`/document overflow hiding or actionable-content clipping was added; desktop layouts and safety copy remain. | Verified | `components/gmail-hub/GmailHubHome.tsx`; `components/gmail-hub/TemplateWorkspace.tsx`; `app/globals.css`; `components/approval/ApprovalQueue.tsx`; `components/approval/NeedsDecisionInboxPanel.tsx`; `tests/unit/responsive-containment-css.test.ts` | 2026-07-13 | — | 2026-12-31 |
| F-PROD-DEMO-2026-07-13 | Verified `main` commit `b24c67d` is deployed to the PMI KC production Cloud Run service as revision `pmi-kc-kb-demo-00013-gm4` with 100% traffic. The production preflight passed; pmikcmetro.com Admin sign-in, the 390px renewal decider, shared Console/Notifications decision backlog, Gmail Hub draft + pasted-thread summary, anticipated work, Maintenance readiness guard, and a live cited Ask all passed direct production checks. The verification created no decision, ticket, assignment, Gmail send, system-of-record write, or fixture cleanup. The owner subsequently replaced the external Gmail self-delivery check with an additive, clearly labeled browser-only simulator; that newer slice has its own verification fact. | Verified | `docs/status.md`; `docs/environment-handoff.md`; `docs/wednesday-demo-runbook-2026-07-15.html` | 2026-07-13 | — | 2026-08-13 |
| F-GMAIL-DEMO-CHAIN | Owner direction D4 adds a clearly labeled synthetic email chain to Gmail Hub for the Wednesday walkthrough: one browser-seeded message, an "Add simulated reply" control, a shared subject/thread counter, and reset. The component is client state only; it has no `fetch`, Gmail-runtime, Firestore, browser-storage, external-delivery, or Send path. This is a demo interaction, not evidence of Gmail delivery or mailbox integration, and it changes no Action Registry entry or Google scope. Full verification passed (220 files / 1,551 tests + production build). The verified working tree based on `b24c67d` deployed as `pmi-kc-kb-demo-00014-cwq` with 100% traffic; direct production checks proved 1→2 append, reset to 1, refresh to 1, 390px containment, disabled live-mailbox access, zero Send controls, and no browser warnings/errors. Covers AC-S15-8. | Verified | `components/gmail-hub/SimulatedEmailChain.tsx`; `components/gmail-hub/GmailHubHome.tsx`; `tests/unit/gmail-hub-simulated-email-chain.test.tsx`; `docs/feature-suites/gmail-hub.md`; `docs/status.md`; `docs/environment-handoff.md` | 2026-07-13 | — | 2026-12-31 |
| F-GMAIL-LIVE-DIRECTION | The 2026-07-13 owner decision authorized keyless per-user Gmail transport with exact human confirmation and no autonomous send. The 2026-07-14 product decision narrows that transport to workflow-linked renewal and maintenance communication; it does not revoke the identity or transport proof. | Verified | `docs/evidence/gmail-production-activation-2026-07-13.md`; `docs/feature-suites/gmail-live-per-user.md` | 2026-07-14 | F-GMAIL-HUB | 2026-12-31 |
| F-GMAIL-LIVE-PER-USER | The Gmail subject and From remain bound to the server-verified signed-in `pmikcmetro.com` user, with no cross-mailbox Admin access or rollout allowlist. The product surface no longer exposes the user's recent inbox or generic compose. | Verified | `lib/gmail-runtime/subject.ts`; `lib/gmail-hub/service.ts`; `docs/feature-suites/gmail-live-per-user.md` | 2026-07-14 | F-GMAIL-PER-USER | 2026-12-31 |
| F-GMAIL-RUNTIME-S19 | The injected runtime retains bounded profile/thread/history/watch/draft/send/label methods, defensive MIME projection, authenticated Pub/Sub, cursor/dedupe handling, context-bound confirmation, one-attempt idempotency, and bodyless audit. Product routes may call those methods only through the narrower workflow/action gates. | Verified | `lib/gmail-runtime`; `lib/gmail-hub`; `firestore.rules`; `tests/unit/gmail-runtime-client.test.ts`; `tests/unit/gmail-hub-service.test.ts`; `tests/unit/gmail-hub-pubsub.test.ts` | 2026-07-14 | F-GMAIL-RUNTIME-GATED | 2026-12-31 |
| F-GMAIL-ACTIONS-ACTIVE | The 2026-07-13 proof established the Gmail transport. The current product exposes three production-enabled workflow actions: linked-thread read, exact-confirmed linked reply, and approved-label application. Generic new-message send is Disabled, `gmail.draft.create` is Test-ready/production-closed, and sample-backed notice drafts are Planned. | Verified | `docs/evidence/gmail-production-activation-2026-07-13.md`; `lib/integrations/action-registry-seed.ts`; `components/gmail-hub/WorkflowCommunicationPanel.tsx` | 2026-07-15 | — | 2026-12-31 |
| F-GMAIL-LIVE-ACTIVE | Per-user Gmail transport and production resources were live-proven on 2026-07-13. The workflow-boundary implementation is local-only as of 2026-07-14, so the production revision is transport evidence rather than proof of the new UI/API boundary. | Verified | `docs/evidence/gmail-production-activation-2026-07-13.md`; `docs/environment-handoff.md`; `docs/loop-state.md` | 2026-07-14 | F-GMAIL-LIVE-PER-USER | 2026-12-31 |
| F-GMAIL-RUNTIME-ACTIVE | The runtime allowlist contains exactly `gmail.readonly`, `gmail.compose`, `gmail.labels`, and `gmail.modify`; each method asks for its least-privilege subset. Application authorization narrows the send-capable technical grant. | Verified | `lib/gmail-runtime/scopes.ts`; `lib/gmail-runtime/dwd-token.ts`; `tests/unit/gmail-hub-capabilities.test.ts` | 2026-07-14 | F-GMAIL-RUNTIME-S19 | 2026-12-31 |
| F-GMAIL-PROD-ACTIVE | Revision `pmi-kc-kb-demo-00020-24d` proved DWD, Gmail API, exact-confirmed synthetic self-send/reply, label mutation, authenticated push, watch, cursor, and audit behavior on 2026-07-13. No live action or deploy occurred in the 2026-07-14 workflow-boundary slice. | Verified | `docs/evidence/gmail-production-activation-2026-07-13.md`; `docs/environment-handoff.md` | 2026-07-14 | F-GMAIL-ACTIONS-ACTIVE | 2026-12-31 |
| F-GMAIL-WORKFLOW-BOUNDARY | Workflow Communications is a Gmail-backed adapter and evidence source, not a general inbox. The primary `/gmail-hub` compatibility surface lists only bodyless workflow links/attention; recent inbox, arbitrary query, free-form compose, and arbitrary labels are absent. Live thread read, link, label, exact-confirmation, reply, reconciliation, and on-demand analysis require a strict server-authorized Live renewal or maintenance context before Gmail client construction. Test Maintenance and Lease contexts fail before DWD/Gmail construction, and their UIs expose no Live Gmail controls. Maintenance tickets must exist and match scope. Covers AC-S19-1, AC-S19-2, AC-S19-3, AC-S19-4, and AC-S19-12. | Verified | `lib/gmail-hub/workflow-context.ts`; `lib/gmail-hub/workflow-authorization.ts`; `lib/gmail-hub/service.ts`; `components/gmail-hub/WorkflowCommunicationPanel.tsx`; `components/maintenance/MaintenanceQueue.tsx`; `tests/unit/gmail-hub-live-routes.test.ts`; `tests/unit/maintenance-test-workflow-component.test.tsx` | 2026-07-15 | F-GMAIL-LIVE-ACTIVE | 2026-12-31 |
| F-GMAIL-IDENTITY-TRANSPORT | Firebase and Gmail DWD remain separate identity systems. A Gmail subject always comes from the server-verified `pmikcmetro.com` session; Admin grants no cross-mailbox access. The runtime scope allowlist remains exactly `gmail.readonly`, `gmail.compose`, `gmail.labels`, and `gmail.modify`; defensive MIME parsing remains bounded and attachments remain metadata-only. Scope capability is constrained by route, role, space, workflow context, and Action Registry authorization. | Verified | `lib/auth/session.ts`; `lib/auth/roles.ts`; `lib/gmail-runtime/subject.ts`; `lib/gmail-runtime/scopes.ts`; `lib/gmail-runtime/dwd-token.ts`; `lib/gmail-runtime/mime.ts`; `tests/unit/gmail-runtime-mime.test.ts`; `tests/unit/gmail-hub-capabilities.test.ts` | 2026-07-14 | F-GMAIL-RUNTIME-ACTIVE | 2026-12-31 |
| F-EXTERNAL-ACTION-GATE | The Action Registry has 38 entries. Three internal Gmail workflow actions are executable: targeted linked-thread read, exact-confirmed linked-thread reply, and approved-label application. Generic new-message send is Disabled and the weaker unused draft route is production-closed before client construction. Every S22/S25/S26 account, OAuth, Vendor-mail, renewal-send, portal, SMS, assignment, and maintenance-owner-send addition defaults to `production_allowed:false`; each may be activated independently when its real provider contract and exact Live target are configured. Production Test workflows use separate internal adapters and receipts, not Registry promotion. | Verified | `lib/integrations/action-registry-seed.ts`; `lib/integrations/action-gate.ts`; `tests/unit/action-registry-schema.test.ts`; `tests/unit/action-gate.test.ts`; `tests/unit/gmail-hub-service.test.ts` | 2026-07-15 | F-GMAIL-ACTIONS-ACTIVE | 2026-12-31 |
| F-GMAIL-SEND-CONTROL | R01/S20 give internal Editors `sendEmail` for enabled Medium work, but that capability is usable only through workflow-context authorization and exact confirmation; it is not generic compose/send authority. S24 now binds confirmation to actor, mailbox, recipient, thread, exact body, immutable artifact, reply policy, and source context; any drift, reuse, concurrency, expiry, or ambiguous transport outcome produces at most one Gmail attempt with no autonomous retry. Generic `gmail.message.send` is Registry-closed and permanently Blocked. The legacy event-driven approval sender remains hard-disabled. | Verified | `lib/auth/roles.ts`; `lib/execution/risk-policy.ts`; `lib/gmail-hub/governed-artifacts.ts`; `lib/gmail-hub/service.ts`; `lib/gmail-hub/workflow-context.ts`; `lib/notifications/approval.ts`; `tests/unit/gmail-hub-service.test.ts`; `tests/unit/execution-risk-policy.test.ts` | 2026-07-14 | F-GMAIL-RUNTIME-GATED | 2026-12-31 |
| F-GMAIL-TARGETED-RECEIVE | Authenticated Pub/Sub remains a mailbox-change signal. History additions are matched only against active bodyless links for that mailbox; unrelated IDs, overflow, and 404 resync fetch no thread body and create no task/model call/notification. A linked reply creates one deduplicated, value-free attention record scoped to the actor and workflow, while the user's own linked outgoing message is ignored. Watch renewal remains manual and observable; no scheduler is authorized. | Verified | `lib/gmail-hub/service.ts`; `lib/gmail-hub/state-store.ts`; `lib/gmail-hub/notifications.ts`; `lib/gmail-hub/pubsub.ts`; `tests/unit/gmail-hub-pubsub.test.ts`; `tests/unit/gmail-workflow-notifications.test.ts` | 2026-07-14 | F-GMAIL-PROD-ACTIVE | 2026-12-31 |
| F-GMAIL-REVIEWED-UNDERSTANDING | Live Gmail AI assistance is user-invoked for one authorized linked thread. Unknown or hard-excluded categories refuse before Gmail/model construction; content exclusions repeat before the model call. Output is an unpersisted, unapplied `Needs Review` proposal with Gmail provenance. It cannot directly create a task, change workflow state, select a vendor, approve cost, satisfy renewal channel completion, or write an external system. | Verified | `app/api/gmail-hub/workflow-analysis/route.ts`; `lib/gmail-inbox-zero/draft-safety.ts`; `lib/gmail-inbox-zero/thread-summary.ts`; `tests/unit/gmail-workflow-analysis-route.test.ts` | 2026-07-14 | F-GMAIL-DRAFT-SAFETY | 2026-12-31 |
| F-GMAIL-BODYLESS-STATE | Workflow links, action audit, confirmation/send records, mailbox state, sync audit, cleanup audit, and legal-hold audit persist identifiers, workflow references, hashes, state, actors, policy metadata, and timestamps only—never body, MIME, attachments, prompts, tokens, recipient content, hold reason, or case text. Firestore clients are denied every Gmail operational collection. S24 encodes the periods in code rather than a mutable link-TTL environment variable. Firestore TTL and Scheduler are optional operations automation, not application V1 gates. | Verified | `lib/gmail-hub/state-store.ts`; `lib/gmail-hub/retention-policy.ts`; `lib/gmail-hub/retention-store.ts`; `firestore.rules`; `tests/firestore/gmail-hub.rules.test.ts`; `tests/unit/communications-retention.test.ts`; `tests/unit/communications-legal-hold.test.ts` | 2026-07-15 | F-COMMUNICATIONS-POLICY-BUILT | 2026-12-31 |
| F-GMAIL-PROD-TRANSPORT-EVIDENCE | The 2026-07-13 production proof established that keyless DWD, the four exact scopes, Gmail API transport, authenticated Pub/Sub, cursor/dedupe state, exact-confirmed self-send/reply, and label mutation worked on revision `pmi-kc-kb-demo-00020-24d`. That historical proof remains tied to `00020-24d`: the newer workflow-boundary code was later deployed as the Pre-V1 candidate in F-PREV1-CANDIDATE-DEPLOYED without live Gmail use, and neither deployment nor the older transport proof authorizes a general inbox or proves the new workflow-scoped behavior. | Verified | `docs/evidence/gmail-production-activation-2026-07-13.md`; `docs/status.md`; `docs/loop-state.md`; `lib/gmail-hub/pubsub.ts` | 2026-07-15 | F-GMAIL-PROD-ACTIVE | 2026-12-31 |
| F-V1-READINESS-AUDIT | The 2026-07-14 evidence audit found seven primary user surfaces and a substantially built internal operations application, not a broadly process-blocked scaffold. Production proves auth, cited Ask, live read connections, Gmail transport, and prior browser smoke; the newer workflow-only Communications boundary is verified locally but not deployed. Actual V1 finalization gates are owner agreement on the internal-V1/tab contract, approved launch-Space source scope and sensitivity, any Communication artifacts/retention needed for promotion, named acceptance/go-live/monitoring/rollback ownership, browser acceptance, and an explicit release action. Vendor endpoints, non-Gmail writes, autonomous initiation, extra users, and event-driven approval email are later separately approved work under the recommended internal-V1 boundary, not launch blockers. | Verified | `docs/v1-readiness-audit-2026-07-14.html`; `components/layout/AppShell.tsx`; `docs/plan.md`; `docs/client-checklist.md`; `docs/status.md` | 2026-07-14 | — | 2026-08-14 |
| F-V1-AUDIT-ROUND1 | The owner approved the internal-V1/no-non-Gmail-write contract, workflow-linked Communications boundary, all seven tab visions, conditional promotion after test/validation, Dan as business-acceptance owner, Josiah as technical go-live/monitoring/rollback/Gmail-watch owner, the current three Gmail AI exclusions with no additions for V1, and the safe defaults. Editors/Admins should be able to contribute processes/files/folders with minimal friction; all roles may see/run read-only connection checks. Round 1 did not resolve the difference between Editor “send” and external transmission, Admin self-approval, V1 end-to-end Gmail versus non-Gmail execution, source publication/activation, external vendor identity/mailbox/auto-reply, exact retention periods, which current scaffolds become approved templates, or Console live-data detail level; those are isolated in the Round 2 packet. | Verified | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `docs/status.md`; `docs/plan.md` | 2026-07-14 | F-V1-READINESS-AUDIT | 2026-08-14 |
| F-V1-AUDIT-ROUND2 | Round 2 replaces the earlier internal/no-non-Gmail-write release contract with an expanded external-user V1: internal Editors may directly execute every enabled action; Admins may self-approve and execute their own proposal at every risk; named Lease Renewal and Maintenance workflows must operate end to end including their approved external outputs; Editor source/process additions publish immediately with an audit log; external maintenance Vendors authenticate with externally addressed app credentials, see only their assigned ticket queue, and use their own external mailbox; every AI-assisted maintenance send remains explicitly human-confirmed; the current three communication scaffolds are the V1 template candidates subject to content review/versioning; Console shows full authorized live property/tenant/rent/message detail with test-data support; and the vendor portal is required for V1. Exact action membership, “enabled” per-item semantics, vendor mailbox provider/auth lifecycle, publication containment, retention values, template edits/reply policy, Console body/test separation, and staged-release shape are isolated in the final Round 3 packet. This direction is product scope, not standing approval for a live write, account creation, mailbox connection, send, or deploy. | Verified | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `docs/v1-readiness-audit-round-3-2026-07-14.html`; owner response in `docs/status.md` | 2026-07-14 | F-V1-AUDIT-ROUND1 | 2026-08-14 |
| F-V1-AUDIT-ROUND3 | The final owner response closes R01–R09 and replaces the Round 2 open-semantics phase. Internal Editors directly execute enabled Low/Medium instances; consequential High and remediable Blocked decisions require Admin, and Admin may self-approve at every risk without bypassing technical safety gates. Every R02 Lease and R03 Maintenance action is final-V1 app execution scope. Trusted Editor publication, external Vendor invite/TOTP/per-vendor Gmail OAuth, exact operational retention, three v1.0 artifacts plus source-visible human-confirmed AI reply policy, Console body/test separation, and staged pre-V1/final acceptance are locked. S20–S27 and the V1 gap implementation program are the executable contracts. These product decisions are not standing live-action, account, OAuth, send, write, configuration, deploy, or smoke authority; current runtime/registry gates remain until each suite is built and separately approved for live use. | Verified | `docs/v1-readiness-audit-round-3-2026-07-14.html`; `docs/v1-gap-implementation-program-2026-07-14.md`; `docs/feature-suites/execution-authority.md`; `docs/feature-suites/v1-release-acceptance.md`; owner response in `docs/status.md` | 2026-07-14 | F-V1-AUDIT-ROUND2 | 2026-12-31 |
| F-V1-EXECUTION-AUTHORITY-BUILT | S20 is Local green. One immutable server policy classifies every current Registry key plus approved S21/S25/S26 keys: enabled reads/health/local drafts/labels are Low, exact-confirmed workflow communication and validated trusted publication/photo exceptions are Medium, and SoR/document/account/OAuth/assignment/accounting writes are High; missing evidence, endpoint, permission, connection, source/value/scope/validation, or `production_allowed:false` is technical Blocked. Internal Editors now directly exact-confirm enabled workflow communication; generic new-message send remains permanently forbidden. A bodyless deterministic-id execution ledger binds actor/action/risk/preview hash/approval/one attempt/outcome; High work creates an execution-linked queue item, and Admin exact-hash approval/return/revoke updates queue and ledger atomically. Admin self-approval requires a reason; stale preview, second claim, returned/revoked state, role/scope failure, and ambiguous result cannot call an executor twice. Firestore clients cannot access either execution collection. No Action Registry value, external provider, cloud state, live data, send, write, or deploy changed. | Verified | `lib/execution/risk-policy.ts`; `lib/execution/authority.ts`; `lib/execution/service.ts`; `lib/firestore/action-executions.ts`; `lib/firestore/approval-queue.ts`; `lib/auth/roles.ts`; `firestore.rules`; `tests/unit/execution-risk-policy.test.ts`; `tests/unit/execution-authority.test.ts`; `tests/unit/execution-ledger.test.ts`; `tests/unit/execution-approval-queue.test.ts`; `tests/unit/action-execution-service.test.ts`; `tests/firestore/action-executions.rules.test.ts` | 2026-07-14 | Q-V1-EDITOR-EXECUTION | 2026-12-31 |
| F-TRUSTED-PUBLICATION-BUILT | S21 is Local green. Admin-only, reasoned connector/root policies bind allowed launch Spaces, detected MIME/type caps, sensitivity ceiling, and a required scanner; existing policies can only tighten. Editor file/folder selection and process publication validate scope/root/path, declared type/length, streamed bytes, server-detected MIME, source-state/citation/process graph, actual Action Registry references, malware, and sensitivity. Upload intake is bounded before retention, and validated content is stored as hash-checked Firestore-safe 384 KiB chunks; rollback reuses and preflight-verifies the immutable content reference instead of duplicating a whole body. Scanner outage and every failed check fail closed with a bodyless rejection audit and no Active pointer. The resource pointer owns the monotonic version counter in the publication transaction, policy identity is reread before commit, and concurrent publishes cannot choose the same version. A rollback creates another immutable version only after transactionally rereading the current policy and revalidating the target's connector/root, Space/type/MIME/size/sensitivity, safe path, source metadata, and content identity; a disabled or tightened policy cannot be bypassed. Structured authority fields are rejected, authority-looking prose remains inert, and publication cannot change claims, policy, Registry readiness, executor state, or external-action authority. The active process UI publishes directly without a routine Approval Queue item. The only fake scanner is fenced to local non-production execution with a loopback emulator; production root/scanner activation, import/indexing, Drive mutation, deployment, and live acceptance remain separately Gated. Covers AC-S21-1 through AC-S21-7 locally; it is not Live-proven or Accepted. | Verified | `lib/publication/policy.ts`; `lib/publication/content.ts`; `lib/publication/scanners.ts`; `lib/publication/validation.ts`; `lib/publication/service.ts`; `lib/publication/authority-firewall.ts`; `lib/publication/process-definition.ts`; `app/api/spaces`; `components/spaces/TrustedPublicationPanel.tsx`; `components/admin/PublicationPolicyAdminPanel.tsx`; `firestore.rules`; `tests/unit/publication-content.test.ts`; `tests/unit/publication-provider.test.ts`; `tests/unit/trusted-publication.test.ts`; `tests/unit/publication-validation.test.ts`; `tests/unit/publication-authority-firewall.test.ts`; `tests/unit/publication-versioning.test.ts`; `tests/firestore/trusted-publication.rules.test.ts` | 2026-07-14 | Q-SOURCE-PUBLISH-BOUNDARY | 2026-12-31 |
| F-CONSOLE-LIVE-BOUNDARY-BUILT | S23 is Local green. The Console now receives a server-selected, scope-filtered operational projection whose property, tenant, rent, lease, workflow, and bounded Gmail metadata fields carry source, observed-at, freshness, and unavailable state. Ordinary production selects only the live-provider boundary and renders named Rentvine/workflow/Gmail failures without constructing fixtures; local, emulator, test, and explicitly named non-production test deployments use guarded synthetic providers and show a persistent `Test data` badge. The production cutover preflight rejects, and deployment tooling clears, `CONSOLE_TEST_DEPLOYMENT_NAME`. Inline communication is sender/recipients/timestamp/subject plus inert 240-Unicode-character/three-line snippet only; message body, attachment content, and thread identifiers do not cross the Console projection. One targeted full-thread call occurs only after the user opens an already authorized workflow communication panel. Wrong-Space rows are omitted; URLs, notifications, audits, and serialized Console state remain value-free. Live provider wiring, approved-record browser acceptance, deployment, and any customer/Gmail read remain separately gated. Covers AC-S23-1, AC-S23-2, AC-S23-3, AC-S23-4, AC-S23-5, AC-S23-6, and AC-S23-7. | Verified | `lib/console/environment.ts`; `lib/console/live-data.ts`; `lib/console/snippet.ts`; `lib/console/test-data-provider.ts`; `components/console/ConsoleLiveDataPanel.tsx`; `components/console/ConsoleView.tsx`; `components/gmail-hub/WorkflowCommunicationPanel.tsx`; `scripts/preflight-production-cutover.mjs`; `tests/unit/console-live-data.test.tsx`; `tests/unit/console-message-boundary.test.tsx`; `tests/unit/console-environment-boundary.test.ts`; `tests/unit/console-snippet.test.ts` | 2026-07-14 | Q-CONSOLE-REAL-DATA | 2026-12-31 |
| F-COMMUNICATIONS-POLICY-BUILT | S24 is Local green. `communications-retention:v1.0` separates 10-minute confirmation usability from 30-day record deletion and encodes 7-day dedupe, 90-day sync, 365-day refreshed links, and 7-year bodyless audit. Every newly written or successfully migrated canonical row carries Firestore Date/Timestamp `expires_at` plus matching numeric `expires_at_ms`; legacy rows remain gated on the migration, and legal hold nulls both while release restores both from policy. Cleanup uses bounded indexed numeric-expiry queries, rechecks each candidate transactionally, and atomically records bodyless processed hashes/counts with deletion in a resumable run ledger before finalizing a counts-only audit. Each run freezes its original ordered candidate hashes and limit; an audit-outage retry can process only that set, a hash crowded out of the bounded resume query is explicitly failed/accounted for a later new run, completed reuse makes no mutation, and strict processed/failed/deleted bounds prevent over-deletion or stale immutable audit counts. The cleanup-run ledger itself is an eight-target, seven-year bodyless-audit retention/TTL target. A bodyless legacy-TTL migration projection is built. The runnable worker requires an explicit emulator acknowledgement, non-production mode, and a loopback Firestore emulator. Exactly three frozen v1.0 artifact hashes render through approved generators and block non-authoritative recipient/mailbox/source, missing values, `Needs Verification`, and unreceipted channel-success claims. `workflow-reply:v1.0` is explicit-request, excluded-category-safe, transient, source-visible, diffed, and rejects unsupported claims. No AI output/extracted fact is persisted; exact confirmation binds artifact, policy, and sources. Production composite-index readiness, legacy-record migration, TTL policy, scheduler/worker identity, live reads/sends, Registry change, cloud configuration, deployment, and acceptance remain Gated. Covers AC-S24-1 through AC-S24-8 locally; it is not Live-proven or Accepted. | Verified | `lib/gmail-hub/retention-policy.ts`; `lib/gmail-hub/retention-store.ts`; `lib/gmail-hub/retention-worker.ts`; `lib/gmail-hub/governed-artifacts.ts`; `lib/gmail-hub/ai-reply-policy.ts`; `lib/gmail-hub/service.ts`; `scripts/run-communications-cleanup.ts`; `firestore.indexes.json`; `app/api/admin/communications-retention/holds/route.ts`; `tests/unit/communications-retention.test.ts`; `tests/unit/communications-retention-worker.test.ts`; `tests/unit/communications-legal-hold.test.ts`; `tests/firestore/communications-retention-worker.test.ts`; `tests/unit/governed-artifacts.test.ts`; `tests/unit/workflow-ai-reply.test.ts` | 2026-07-14 | Q-GMAIL-RETENTION | 2026-12-31 |
| F-VENDOR-PORTAL-BUILT | S22 is Local green. A separate external Vendor session accepts only the Firebase `vendor`/`vendor_id` lane after verified email, TOTP second factor, and recent auth; it inherits no staff role or Space. Admin invite logic exact-confirms `vendor-invite:v1.0`, creates no reusable password, returns/logs/audits no setup link, and compensates partial creation. Activation, list/detail/thread access, OAuth start/callback, pre-vault persistence, and final connection save all join the verified token's normalized email to the active immutable invited Vendor record; changed/reverified-email, guessed, deassigned, or disabled access uses indistinguishable 404s. Server-side Google OAuth uses single-use expiring state, PKCE, exact redirect/provider/same-address mailbox/exact four-scope checks, and writes refresh material only through a token-vault interface; a mid-flight identity drift destroys any just-created secret and saves no connection. Firestore stores server-only metadata/reference. Vendor or pre-authorized Admin assigned-ticket replies bind actor, mailbox, Vendor, ticket, thread, body, RFC message id, expiry, and one attempt; ambiguous outcome never retries. Disable immediately closes the active-vendor join, disables Auth, revokes sessions, and queues token destruction while retaining bodyless audit. External sign-in/TOTP enrollment, assigned-ticket pages, Admin invite preview, server-only collections/rules, and isolated-port emulator verification are built. Every new Registry action remains false; Identity Platform/TOTP, invite delivery, OAuth client/consent/redirect, Secret Manager vault, first Vendor/ticket, mailbox access/send, deploy, and live acceptance remain gated. Covers AC-S22-1 through AC-S22-9. | Verified | `lib/vendor`; `lib/firestore/vendors.ts`; `app/vendor`; `app/api/vendor`; `components/vendor`; `components/admin/VendorAdminPanel.tsx`; `firestore.rules`; `tests/unit/vendor-auth.test.ts`; `tests/unit/vendor-invite.test.ts`; `tests/unit/vendor-assignment-boundary.test.ts`; `tests/unit/vendor-gmail-oauth.test.ts`; `tests/unit/vendor-gmail-boundary.test.ts`; `tests/unit/vendor-gmail-send.test.ts`; `tests/unit/vendor-lifecycle.test.ts`; `tests/firestore/vendor-portal.rules.test.ts` | 2026-07-14 | Q-VENDOR-PORTAL-IDENTITY | 2026-12-31 |
| F-DAN-REVIEW-CADENCE | Round 1 safe-default approval confirms the V1 team-review digest as weekly and renewals-only. The existing `self_corrections` proxy remains returned write-back authorizations; a richer stale/re-resolved definition is later refinement, not a launch question. | Verified | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `lib/attention/review-lane.ts`; `docs/feature-suites/unified-console-and-attention.md` | 2026-07-14 | — | 2026-12-31 |
| F-LEASE-V1-EXECUTION-BOUNDARY | S25's safe local boundary is Local green and the suite remains Gated. The exact 11-action/seven-group Lease graph uses Registry-derived exact preview schemas, immutable risk, canonical actor-independent external-action identity, persisted same-workflow dependency receipts, documented contract/connection/mapping, current S20 role/scope/approval, one provider claim, strict bodyless receipt, reconciliation, and correction. The server bridge freezes values/refs before awaits, resolves and exact-matches the canonical action definition, requires pure prepare validation and exact Medium confirmation, and binds High Approval Queue review to both the value hash and displayed target/account/source context; browser-supplied authority, mutable-input drift, forged dependencies/contracts, schema/risk lowering, production Registry override, and unmarked synthetic references fail before provider work. Typed in-memory providers execute governed Gmail with literal draft banner, exact RFC Message-ID, and provider-fetched canonical payload; provider-atomic one-cell Sheet CAS/readback; expected-state Rentvine renewal mutation with exact readback/idempotency reconciliation; Dotloop loop/document; trusted-consent portal/SMS; and conditional Boom. The canonical non-routable alias run completes all 11 adapters through the local-only orchestrator with one allowlisted bodyless receipt/attempt each and zero live calls; the S20 bridge is separately adversarially verified. Every new action stays `production_allowed:false`; real account contracts, conditional/versioned mutation support, authoritative mappings, credentials, permitted-environment proofs, Registry promotion, deployment, browser evidence, and owner acceptance remain Gated. AC-S25-1 through AC-S25-10 have local evidence, not Live-proven or Accepted evidence. | Verified | `lib/integrations/final-v1-action-contracts.ts`; `lib/external-execution`; `lib/external-execution/s20-bridge.ts`; `lib/lease-renewal/execution`; `lib/release/synthetic-execution.ts`; `lib/firestore/external-action-executions.ts`; `components/execution/ExternalExecutionReadiness.tsx`; `tests/unit/external-execution-boundary.test.ts`; `tests/unit/external-execution-s20-bridge.test.ts`; `tests/unit/v1-synthetic-execution.test.ts`; `tests/unit/lease-execution-matrix.test.ts`; `tests/unit/lease-execution-authority.test.ts`; `tests/unit/lease-gmail-execution.test.ts`; `tests/unit/renewal-sheet-executor.test.ts`; `tests/unit/rentvine-renewal-executor.test.ts`; `tests/unit/dotloop-renewal-executor.test.ts`; `tests/unit/renewal-channel-execution.test.ts`; `tests/unit/boom-renewal-executor.test.ts` | 2026-07-14 | Q-V1-ENDTOEND-BOUNDARY | 2026-12-31 |
| F-MAINTENANCE-V1-EXECUTION-BOUNDARY | S26's safe local boundary is Local green and the suite remains Gated. The exact 19-action/nine-group Maintenance graph composes S22 Vendor lifecycle/assignment/mail with validated append-only Drive photo storage and exact folder/filename/hash readback, provider-atomic expected-state Rentvine create/assign/status/close behavior with exact readback/idempotency reconciliation, authoritative S24 owner mail with exact RFC/payload readback, no-authority AI behavior, expected-stage LeadSimple mutation/reconciliation, and QuickBooks exact draft-Bill-only execution/reconciliation. Registry-derived exact schemas, immutable canonical definitions, same-workflow dependency receipts, current S20/Vendor authority, one provider claim, bodyless receipt, reconciliation, and correction are enforced before and after the provider seam; client Firestore access is denied. Admin may perform exact assigned-ticket support but can never consent-connect a Vendor mailbox. The canonical non-routable alias run completes all 19 typed adapters through the local-only orchestrator with one bodyless receipt/attempt each and zero live calls; S20/S22 authority seams are separately adversarially verified and the action-level UI distinguishes test aliases from production gates. Every new action stays `production_allowed:false`. Live identity/folder/provider contracts, conditional/versioned mutation support, authoritative mappings, credentials, permitted proofs, Registry promotions, deployment, browser evidence, and owner acceptance remain Gated. AC-S26-1 through AC-S26-10 have local evidence, not Live-proven or Accepted evidence. | Verified | `lib/integrations/final-v1-action-contracts.ts`; `lib/maintenance/execution`; `lib/vendor`; `lib/release/synthetic-execution.ts`; `lib/firestore/external-action-executions.ts`; `components/execution/ExternalExecutionReadiness.tsx`; `tests/unit/external-execution-boundary.test.ts`; `tests/unit/external-execution-s20-bridge.test.ts`; `tests/unit/v1-synthetic-execution.test.ts`; `tests/unit/maintenance-execution-matrix.test.ts`; `tests/unit/maintenance-execution-authority.test.ts`; `tests/unit/maintenance-photo-executor.test.ts`; `tests/unit/rentvine-work-order-executor.test.ts`; `tests/unit/maintenance-owner-email.test.ts`; `tests/unit/maintenance-ai-boundary.test.ts`; `tests/unit/leadsimple-maintenance-executor.test.ts`; `tests/unit/quickbooks-draft-bill-executor.test.ts`; `tests/firestore/external-action-executions.rules.test.ts`; `tests/firestore/external-action-execution-store.test.ts` | 2026-07-14 | Q-V1-ENDTOEND-BOUNDARY | 2026-12-31 |
| F-V1-RELEASE-READINESS-BOUNDARY | Historical pre-working-app contract, superseded by `F-WORKING-APP-V1`: S27's safe local readiness boundary was Local green and final acceptance remained Gated. A V1 manifest requires a production environment, exact unique S20–S26 AC/action sets, authoritative mandatory commit/deployed-revision/rules/indexes/Registry pins, and globally unique canonical production evidence across every suite, action, release, smoke/migration, browser, rollback, and named-acceptance gate. Repository evidence files/anchors must resolve, smoke/migration proof is structured, and exact named ISO-dated Dan/Josiah acceptance carries the same non-circular release-identity hash over all candidate pins/proofs; forged strings, omitted pins, local input, or synthetic all-green input cannot promote V1. The UI labels the candidate Pre-V1. The production-disabled local Admin harness traverses the real S22 invite → verified-email TOTP → PKCE/exact-scope/same-mailbox vault seam → assigned-ticket mail/reply negatives → disable/revoke journey, then all 11 S25 and 19 S26 typed adapters using canonical `example.invalid` aliases and invented IDs, with zero live provider calls. Cutover parsing rejects unknown/duplicate/empty flags, requires an exact safe `us` search location and a fully project-bound Gmail DWD/Pub/Sub quartet whose audience equals the production push route, fixes the Cloud Run target to `pmi-kc-kb-demo` in `us-central1`, and emits no setup/corpus/deploy/rollback command when any reviewed GCP/Firebase/runtime/Sheets/Gmail identity, topic, location, or CLI project is malformed or conflicting. Source-corpus command generation also refuses unsafe paths/resources, placeholders, and any manifest that is not production-ready. Notification delivery remains off; a captured service-matching prior revision/traffic restoration is required before rollback is ready, with no service or datastore deletion. A bodyless manifest report, action ledger, monitoring/rollback plan, exact client-unblock checklist, and eight-surface desktop/phone plan are durable. Three Moderate dev-only audit findings in the `firebase-tools` chain require disposition without force/downgrade. The code-only Pre-V1 deployment and unauthenticated boundary smoke in F-PREV1-CANDIDATE-DEPLOYED do not satisfy final acceptance: real provider/action proofs, manifest-bound signed-in smoke/browser evidence, rollback rehearsal, monitoring evidence, dependency disposition, and Dan/Josiah signatures remain Gated; no final V1 claim is permitted. Local evidence covers AC-S27-1, AC-S27-2, AC-S27-3, and AC-S27-5, plus only the local render/guard portions of AC-S27-4 and AC-S27-7. AC-S27-4, AC-S27-6, AC-S27-7, AC-S27-8, and AC-S27-9 remain Gated for their deployed/live/acceptance requirements and are not Live-proven or Accepted. | Verified | `lib/release/manifest.ts`; `lib/release/fake-acceptance.ts`; `lib/release/synthetic-execution.ts`; `lib/release/synthetic-vendor-acceptance.ts`; `app/api/admin/v1/fake-acceptance/route.ts`; `scripts/v1-manifest-report.ts`; `scripts/build-cutover-report.mjs`; `scripts/source-corpus-manifest.mjs`; `scripts/source-corpus-readiness.mjs`; `tests/unit/v1-release-manifest.test.ts`; `tests/unit/v1-synthetic-execution.test.ts`; `tests/unit/vendor-release-label.test.tsx`; `tests/unit/cutover-report.test.mjs`; `tests/unit/cutover-readiness-golden.test.mjs`; `tests/unit/live-cost-scripts.test.mjs`; `docs/v1-client-unblock-checklist-2026-07-14.md`; `docs/v1-monitoring-and-rollback-plan-2026-07-14.md`; `docs/v1-tab-browser-acceptance-plan-2026-07-14.md`; `docs/v1-dependency-disposition-2026-07-14.md`; `docs/status.md` | 2026-07-15 | F-V1-AUDIT-ROUND3 | 2026-12-31 |
| F-V1-LOCAL-BROWSER-REGRESSION | The S27 local-only browser regression is green against an isolated loopback Firestore emulator and demo project: Console, Spaces, Approval Queue, Workflow Communications, Connections, Admin, Notifications, and the external Vendor sign-in surface rendered with the Pre-V1 label at 1440×900 and 390×844; Lease and Maintenance also rendered their action-level provider-readiness lists. The internal surfaces showed synthetic data honestly, all checked phone documents had no horizontal overflow, and fresh page logs were error-free. The pass exposed and fixed one isolation defect: an internal demo/staff cookie could make `/vendor` surface a Firebase decode error; invalid, revoked, or non-Vendor cookies now fail closed to `/vendor/sign-in`, with focused tests. This is Local evidence only: keyboard/failure-path role acceptance against approved records and the pinned deployed production revision remain Gated. | Verified | `lib/vendor/auth.ts`; `tests/unit/vendor-auth.test.ts`; `docs/v1-tab-browser-acceptance-plan-2026-07-14.md`; `docs/status.md` | 2026-07-14 | — | 2026-12-31 |
| F-PREV1-CANDIDATE-DEPLOYED | Historical deployment record: owner-authorized code-only deployment placed commit `0dc0c7aa7be600e1097e80de448227917dc9101a` on Cloud Run as Ready revision `pmi-kc-kb-demo-00021-bj8` with 100% traffic; Cloud Build `9ae6958b-ec8c-48ef-bf0b-46dbe8194880` succeeded and prior revision `pmi-kc-kb-demo-00020-24d` was captured. Both public hosts, staff/Vendor sign-in, protected-route redirects, and unauthenticated Ask/Vendor API denials passed. The service intentionally uses ingress `all` with the invoker IAM check disabled, so the failed attempt to add an `allUsers` binding did not prevent reachability. No Registry promotion, external provider action, import, rules/index/TTL/scheduler configuration, or rollback occurred. This is Pre-V1 code-delivery/auth-boundary evidence only; 29 action proofs remain 0 accepted/0 production-allowed and the report remains at 169 open gates. | Verified | `docs/status.md`; `docs/environment-handoff.md`; `docs/v1-pre-release-report-2026-07-14.md`; `docs/pmi-kc-v1-current-state-2026-07-15.html` | 2026-07-15 | — | 2026-12-31 |
| F-WORKING-APP-V1 | V1 means the stable production application works as designed, not that every optional provider is already activated. Application readiness is proven by authenticated production behavior across explicitly labeled Live and isolated Test records. Test records may perform real app/Firestore writes, traverse complete workflows, and reach Done; they can never call an external provider or count as Live-provider evidence. Live external execution is activated and reported per action, with an exact target/effect preview, permitted human confirmation or approval, idempotency, bodyless receipt, reconciliation, monitoring, and rollback. Missing provider credentials or contracts leave that provider action unavailable without relabeling the working app Pre-V1. Named stakeholder signoff is useful release metadata, not application-readiness authority. | Verified | `lib/data-mode.ts`; `lib/release/manifest.ts`; `lib/release/fake-acceptance.ts`; `components/admin/V1ProductionTestWorkspacePanel.tsx`; `tests/unit/v1-release-manifest.test.ts`; `tests/unit/v1-synthetic-execution.test.ts` | 2026-07-15 | F-V1-RELEASE-READINESS-BOUNDARY | 2026-12-31 |
| F-PRODUCTION-DUAL-DATA-LANES | Production exposes separate server-selected Live and Test projections at the same time. Legacy records without a lane resolve to Live. Test aliases are reserved, visibly badged, and rejected from Live records; Test providers require the explicit Test workspace. The Console uses a bounded cached Rentvine lease read for Live rows and a distinct invented-data provider for Test rows, never a fallback. | Verified | `lib/data-mode.ts`; `lib/console/environment.ts`; `lib/console/live-data.ts`; `lib/console/rentvine-live-provider.ts`; `components/console/ConsoleLiveDataPanel.tsx`; `components/console/ConsoleView.tsx`; `tests/unit/console-environment-boundary.test.ts`; `tests/unit/console-rentvine-live-provider.test.ts` | 2026-07-15 | F-CONSOLE-LIVE-BOUNDARY-BUILT | 2026-12-31 |
| F-LEASE-WORKING-V1 | Lease Renewal is a working V1 workflow. The normal Renewals area creates a canonical invented Test run in dedicated Firestore collections, moves it `Created → Reviewed → Approved → Executing → Done`, and exposes all 11 Lease action keys with exact Test target/risk/effect, matrix dependency checks, fresh confirmation, deterministic one-attempt/bodyless receipts, and refresh-safe progress. Both UI and server refuse Done until every action has one valid receipt. Test modules construct no Gmail/RentVine/Sheets/Dotloop/SMS/Boom provider; receipts state `provider_contacted:false` and `live_proof_eligible:false`; all direct client writes to the Test collections are denied. | Verified | `lib/lease-renewal/test-workflow.ts`; `lib/firestore/lease-renewal-test-runs.ts`; `app/api/lease-renewal/test-runs`; `components/lease-renewal/LeaseTestRunsWorkspace.tsx`; `components/lease-renewal/LeaseTestJourney.tsx`; `firestore.rules`; `tests/unit/lease-renewal-test-workflow.test.ts`; `tests/firestore/lease-renewal-test-workflow.rules.test.ts` | 2026-07-15 | F-LEASE-V1-EXECUTION-BOUNDARY | 2026-12-31 |
| F-MAINTENANCE-WORKING-V1 | Maintenance is a working V1 workflow. Staff can create and manage Live in-app tickets. The canonical Test route creates an invented plumbing ticket for `unit:test-maple-204`; it supports staff assignment, `vendor:test-summit-plumbing` assignment, status/activity/notes, six explicit simulated provider actions with target labels and exact confirmation, receipts that state no provider was contacted, close, and reopen. Live/Test unit, Vendor, assignment, action, and receipt lanes cannot cross. No composite index, TTL, Scheduler, or provider credential is required for the Test workflow. | Verified | `lib/maintenance/test-workflow.ts`; `lib/firestore/maintenance-tickets.ts`; `app/api/maintenance/tickets/test-seed/route.ts`; `app/api/maintenance/tickets`; `components/maintenance/MaintenanceExecutionReadiness.tsx`; `components/maintenance/MaintenanceQueue.tsx`; `tests/unit/maintenance-test-workflow.test.ts`; `tests/unit/maintenance-test-workflow-routes.test.ts` | 2026-07-15 | F-MAINTENANCE-V1-EXECUTION-BOUNDARY | 2026-12-31 |
| F-VENDOR-WORKING-V1 | Vendor authentication and assigned-ticket work are part of working V1. An Admin exact-previews and provisions only the canonical non-routable Summit Plumbing Test Vendor, receives a one-response Firebase password-setup link, and can exact-preview disable/revoke. The Test Vendor uses the real Firebase password and TOTP flow, must sign in again after enrollment, is bound to Test assignments/tickets, and operates a Firestore-backed assigned-ticket mailbox with saved drafts, governed labels, exact-confirmed simulated replies, idempotent receipts, and no OAuth/Gmail construction. Live Vendors retain verified routable email, TOTP, assigned-ticket authorization, and same-address OAuth/vault requirements when their Live mailbox action is activated. | Verified | `lib/vendor/test-identity.ts`; `lib/vendor/test-mailbox.ts`; `lib/vendor/auth.ts`; `lib/vendor/oauth.ts`; `lib/vendor/gmail.ts`; `app/api/admin/vendors`; `app/api/vendor`; `components/admin/VendorAdminPanel.tsx`; `components/vendor/VendorTestMailboxPanel.tsx`; `tests/unit/vendor-test-identity.test.ts`; `tests/unit/vendor-test-mailbox.test.ts` | 2026-07-15 | F-VENDOR-PORTAL-BUILT | 2026-12-31 |
| F-RETENTION-SAFE-DEFAULT | The versioned communications retention rules and legal-hold behavior are application policy. Firestore TTL, extra composite indexes, and Cloud Scheduler are optional operations optimizations, not V1 release requirements. The safe initial production default is bodyless bounded records, explicit legal hold, on-demand bounded cleanup, and visible cleanup health; automatic expiry/scheduling may be enabled later when operational volume justifies it. | Verified | `lib/gmail-hub/retention-policy.ts`; `lib/gmail-hub/retention-store.ts`; `lib/gmail-hub/retention-worker.ts`; `components/admin/CommunicationsRetentionAdminPanel.tsx`; `tests/unit/communications-retention.test.ts`; `tests/unit/communications-legal-hold.test.ts` | 2026-07-15 | F-COMMUNICATIONS-POLICY-BUILT | 2026-12-31 |
| F-EXTERNAL-EXECUTION-DATA-LANE | External action identity, context hashes, idempotency keys, execution records, receipts, and audit rows bind the data lane. Production Live orchestration rejects Test records. The Admin cross-provider diagnostic remains memory-only and accepts only explicitly branded adapters with no live client. Normal Lease/Maintenance Test journeys persist separately owned app-plane run/ticket/attempt/receipt state and never import or construct the Live orchestrator/provider. Every Test receipt is ineligible for Live evidence. Synthetic references are valid only in Test mode. | Verified | `lib/external-execution/identity.ts`; `lib/external-execution/orchestrator.ts`; `lib/lease-renewal/test-workflow.ts`; `lib/maintenance/test-workflow.ts`; `tests/unit/external-execution-boundary.test.ts`; `tests/unit/lease-renewal-test-workflow.test.ts`; `tests/unit/v1-synthetic-execution.test.ts` | 2026-07-15 | F-EXTERNAL-EXECUTION-AUTHORITY-BOUNDARY | 2026-12-31 |
| F-VENDOR-AUTH-PROD-CONFIG | Production Identity Platform now has Email/Password enabled with password required, global MFA `ENABLED`, and the TOTP provider `ENABLED` with adjacent interval `1`; the canonical Cloud Run hostname remains authorized and the existing Google provider remains enabled. The corrected `updateMask=mfa` update explicitly included both global state and provider state, was run as `josiah@pmikcmetro.com`, and was read back safely. This clears the project-level Test Vendor password/TOTP prerequisite; password/TOTP enrollment/challenge is the remaining human-secret browser acceptance step. | Verified | `docs/evidence/vendor-auth-production-config-2026-07-15.md`; Cloud Audit Logs `ProjectConfigService.UpdateConfig` at `2026-07-15T11:03:45Z` | 2026-07-15 | F-VENDOR-WORKING-V1 | 2026-12-31 |
| F-V1-LANE-ADVERSARIAL-AUDIT | Final adversarial review closed five cross-lane/concurrency risks before release: production S20 rejects Test and stamps lane-correct receipts; provider dependencies require the same lane and evidence eligibility; each Maintenance Test ticket/action has one deterministic idempotent receipt; Admin Vendor Gmail requires explicit Live mode plus persisted Vendor/assignment/ticket/thread Live equality before client construction; and Test Vendor reply confirmation plus mailbox append commits atomically so concurrent replies cannot be lost. Focused regressions and the full suite are green. | Verified | `lib/external-execution/s20-bridge.ts`; `lib/external-execution/orchestrator.ts`; `lib/firestore/maintenance-tickets.ts`; `lib/vendor/gmail.ts`; `lib/vendor/test-mailbox.ts`; `lib/firestore/vendors.ts`; `tests/unit/external-execution-s20-bridge.test.ts`; `tests/unit/external-execution-boundary.test.ts`; `tests/unit/maintenance-tickets.test.ts`; `tests/unit/vendor-gmail-boundary.test.ts`; `tests/unit/vendor-test-mailbox.test.ts` | 2026-07-15 | F-EXTERNAL-EXECUTION-DATA-LANE | 2026-12-31 |
| F-EXTERNAL-EXECUTION-AUTHORITY-BOUNDARY | S25/S26 orchestration reuses S20's immutable role/risk decision and exact queue semantics rather than accepting caller authority. One canonical workflow/action/action-key identity spans S20 and provider idempotency independent of preparer; values and refs are frozen before awaits; the bridge resolves a frozen committed definition and exact-matches every immutable field, so a forged same-key dependency/contract/group/risk cannot weaken execution. Medium requires explicit exact-current-preview confirmation; High approval binds the value hash and displayed connection/mapping/contract/source target context; dependencies are reloaded as canonical same-workflow succeeded S20 records. Production rejects fake executors, synthetic refs, Registry overrides, risk/schema lowering, internal use of the S22 direct ledger, and non-Firestore S22 persistence. S22 is the narrow durable Vendor exception: verified-email TOTP, same mailbox, active assigned ticket where required, and current self-consent permit only Vendor mailbox lifecycle/assigned-thread/photo actions; Vendor cannot execute internal/SoR/accounting actions. Admin may support an assigned ticket through S20 without Vendor TOTP but cannot consent-connect a Vendor mailbox. Registry, authority, scope, approval, dependencies, preview, target context, technical read authority, and strict allowlisted receipt are revalidated around the one atomic provider claim/reconciliation; malformed results stay ambiguous and revocation/drift consumes no extra attempt. A Registry execution kill switch cannot strand separately governed read-only reconciliation, while a new write remains closed. Firestore terminal updates compare state/attempt, make an identical terminal receipt idempotent, and reject conflicting concurrent overwrites. A concurrent/cross-actor duplicate can make only one provider call. The S20 bridge refuses browser-origin authority or an unmarked test alias. Covers the authority/concurrency portions of AC-S25-2, AC-S25-9, AC-S26-2, AC-S26-6, and AC-S26-9 locally. | Verified | `lib/integrations/final-v1-action-contracts.ts`; `lib/external-execution/authority.ts`; `lib/external-execution/identity.ts`; `lib/external-execution/receipt.ts`; `lib/external-execution/orchestrator.ts`; `lib/external-execution/s20-bridge.ts`; `lib/execution/authority.ts`; `lib/vendor/auth.ts`; `lib/vendor/assignment.ts`; `tests/unit/external-execution-boundary.test.ts`; `tests/unit/external-execution-s20-bridge.test.ts`; `tests/unit/external-execution-authority.test.ts`; `tests/unit/lease-execution-authority.test.ts`; `tests/unit/maintenance-execution-authority.test.ts`; `tests/firestore/external-action-execution-store.test.ts` | 2026-07-14 | F-V1-EXECUTION-AUTHORITY-BUILT | 2026-12-31 |
| F-PROD-RULES-DEPLOYED | Firestore rules matching the working-app candidate were compiled with zero issues and released to `cloud.firestore` as ruleset `63b31613-59ba-495c-9ef3-455a5c593f51` at `2026-07-15T11:40:37Z`. The rules permit server-owned Lease/Maintenance/Vendor Test persistence while denying direct client writes to the protected Test collections. TTL, additional indexes, and Scheduler are not required for this release. | Verified | `firestore.rules`; `docs/evidence/working-app-v1-production-2026-07-15.md`; `tests/firestore/lease-renewal-test-workflow.rules.test.ts` | 2026-07-15 | — | 2026-12-31 |
| F-V1-BROWSER-ROLLBACK | Final production browser evidence for commit `38ebcf530e3fe193547806bace91246ccea20c0b` on revision `pmi-kc-kb-demo-rmrm9mp6v-04c897acee28` passed delayed direct signed-in desktop and 375px-phone loads of Ask, Spaces, Approval Queue, Gmail Hub, Connections, Admin, Lease Renewal, and Maintenance. Every route showed the expected H1, no horizontal overflow, and zero console errors. The pass isolated an Approval Queue hydration mismatch to implicit host/browser time-zone formatting; the deployed formatter explicitly uses `America/Chicago`, and a regression test pins stable output. The final rollback rehearsal moved 100% traffic to captured predecessor `pmi-kc-kb-demo-rmrm8t6y7-d250f83ddfee`; staff and Vendor sign-in returned 200, unauthenticated `/ask` redirected to `/sign-in`, and the existing signed-in Console worked. Traffic returned 100% to the final revision with the same healthy boundaries, and the final-revision ERROR log query returned no entries. The earlier `f02112d / 00025-mhw` rollback remains historical evidence. | Verified | `components/approval/ApprovalQueueModel.ts`; `tests/unit/approval-queue-component.test.tsx`; `docs/evidence/working-app-v1-production-2026-07-15.md`; `docs/v1-tab-browser-acceptance-plan-2026-07-14.md`; `docs/v1-monitoring-and-rollback-plan-2026-07-14.md` | 2026-07-15 | F-V1-LOCAL-BROWSER-REGRESSION | 2026-12-31 |
| F-CURRENT-SERVING-CHECKPOINT-2026-07-22 | Cloud Run serves exact product commit `7663cec` (main — the 2026-07-22 unattended overnight build cycle: comp basis + Zillow deep link, KB answer transparency + source freshness, maintenance owner-notice draft go-live, Admin add-a-Space request intake, cost-gated re-index control, RentVine write approved-in-principle kept gated OFF, and Dotloop OAuth scaffolding gated OFF) as revision `pmi-kc-kb-demo-rmrwmk2kn-ae2beeaf9de7` at 100% traffic with the managed production runtime identity. Owner-authorized unattended cost-bearing deploy (`--budget-confirmed --allow-multiple-spaces`; the ~$10 kill-switch is the backstop) run non-interactively with a fresh ADC session on 2026-07-22; full gate green (typecheck, 2,697 unit tests / 373 files, lint 0 errors / 13 known warnings, prettier, verify:copy-voice/falsification/context-freshness/spec-traceability, production build). Live-verified over HTTP: unauth `/`→307, `/sign-in`→200, `/admin`→307, `/approval-queue`→307, `/api/ask`→405; deployed env confirmed `vertex spaces:11`, `drive folders:11`, `LEASE_RENEWAL_SHEET_WRITEBACK_ENABLED:true`, `ASK_DEMO_MODE:false`. The captured rollback target is the immediately prior serving revision `pmi-kc-kb-demo-rmrwc70pc-d5cb9815094b` (the prior 2026-07-22 Sheet write-back deploy). | Verified | `docs/loop-state.md`; `docs/status.md`; `docs/overnight-build-run-2026-07-22.md`; `scripts/deploy-demo-cloud-run.mjs` | 2026-07-22 | — | 2026-08-22 |
| F-WORKING-APP-V1-VERIFIER | The 2026-07-18 clean integrated `bash scripts/verify.sh` run from `f6d5ddbce8b250b64df3bc58c81398f09e33b869` passes format; lint with 0 errors/8 known warnings; typecheck; 322 test files/2,290 tests; router, falsification, context, traceability, and redaction gates; and the 77-route production build. Firestore passes 59/59 and core E2E passes 32 with 18 intentional prerequisite skips. The repaired production dependency graph reports zero runtime findings under `npm audit --omit=dev`. | Verified | `scripts/verify.sh`; `package-lock.json`; `docs/status.md`; `docs/loop-state.md` | 2026-07-18 | — | 2026-08-18 |
| F-VENDOR-SETUP-LINK-RECOVERY | A closed or expired Test Vendor setup response no longer requires identity deletion or an undocumented workaround. For the canonical `pending_setup` Test Vendor only, Admin exact-previews and confirms a replacement password-setup link. Domain/runtime/API/UI guards reconcile the Test Vendor record with the Firebase UID, normalized email, verified/enabled state, exact reason-bound preview, and HTTPS response before generation. Provision and recovery links use `Cache-Control: no-store`, appear only in the confirmed Admin response, and are never persisted or delivered. The winning claim atomically appends a bodyless `test_vendor_setup_link_regeneration_claimed` audit; successful completion appends `test_vendor_setup_link_regenerated`. Invalid alias/Firestore status/mode/preview inputs that cannot claim write no audit. Firebase UID/email/verification/enabled reconciliation and link mint occur after claim so they are fenced against races; Auth drift, mint failure, or any other post-claim/pre-completion failure truthfully retains only the claim event. Both actions store actor UID/Vendor id/reason hash/time and never a target Firebase UID, link, secret, or plaintext reason. | Verified | `lib/vendor/test-identity.ts`; `lib/vendor/admin-runtime.ts`; `app/api/admin/vendors/test/route.ts`; `components/admin/VendorAdminPanel.tsx`; `tests/unit/vendor-test-identity.test.ts`; `tests/unit/vendor-test-admin-route.test.ts`; `tests/unit/vendor-test-admin-runtime.test.ts`; `tests/unit/vendor-test-workspace-components.test.tsx` | 2026-07-15 | F-VENDOR-WORKING-V1 | 2026-12-31 |
| F-VENDOR-TEST-AUTH-RESET | The canonical `.invalid` Test Vendor has a repeatable Admin-only authentication reset/re-enable lifecycle from `pending_setup`, `active`, or `disabled`. A reasoned exact preview binds the current Firebase UID, lifecycle status, and `inviteVersion`. A transactional, per-request claimed/prepared/completed marker with a two-minute lease ensures only one active request can mutate Firebase or mint a setup link; overlap and completed replay fail before auth mutation, and an expired claim is recoverable only after the deployed 60-second request lifetime. The winning initial claim atomically appends bodyless `test_vendor_authentication_reset_claimed`; a failed post-claim/pre-commit attempt truthfully retains only that claim event. The owning request hardens and deletes the old principal, creates a disabled replacement with the exact Test claims, transactionally rotates the Vendor UID, increments the invite version, returns the record to `pending_setup`, clears TOTP/activation state, and appends one canonical bodyless `test_vendor_authentication_reset` audit for that invite increment before enabling the replacement only for password setup and returning one HTTPS `no-store` link. Both audits contain actor UID/Vendor id/reason hash/time and never a target/replacement Firebase UID, link, secret, or plaintext reason. Compensation re-hardens only while the request still owns the lease, so a loser cannot disable a winner. The stable Vendor id, Test tickets, assignments, mailbox history, and completed receipts remain; the old password, TOTP factors, sessions, action links, and UID-bound confirmations become invalid. Live, arbitrary, drifted, privileged-extra-claim, wrong-email/claim/mode, or provider-connected identities refuse before mutation. Reset constructs no OAuth, Gmail, token vault, provider client, external delivery, Registry promotion, or Live evidence. | Verified | `lib/vendor/test-identity.ts`; `lib/vendor/admin-runtime.ts`; `lib/firestore/vendors.ts`; `app/api/admin/vendors/test/route.ts`; `components/admin/VendorAdminPanel.tsx`; `tests/unit/vendor-test-authentication-reset-store.test.ts`; `tests/unit/vendor-test-identity.test.ts`; `tests/unit/vendor-test-admin-runtime.test.ts`; `tests/unit/vendor-test-admin-route.test.ts`; `tests/unit/vendor-test-workspace-components.test.tsx` | 2026-07-15 | — | 2026-12-31 |
| F-VENDOR-RESET-EXPIRED-TAKEOVER | Recovery of an expired Test Vendor authentication-reset claim never adopts or reuses an abandoned authentication generation, even when that disabled Firebase user carries the exact canonical claims. A normal Admin reload binds the marker's original source UID/status/`inviteVersion` tuple without returning the UID. While the lease remains live, only the original reason reproduces the same confirmation hash/effect and every different-reason/takeover attempt refuses. After expiry, a fresh Admin reason may rebind that validated source tuple, avoiding manual Firestore repair. Every successful expired takeover atomically records one distinct bodyless `test_vendor_authentication_reset_recovery_claimed` audit with recovery actor UID, fresh reason hash, and timestamp; it stores no plaintext reason, target/replacement Firebase UID, link, or secret. Takeover hardens, revokes, and deletes the email-resolved abandoned principal, then requires a fresh Firebase UID distinct from the original source UID, current Firestore record UID, and resolved Auth UID; allocation of any forbidden UID is deleted and rejected. A claimed recovery later creates the one canonical reset audit when it commits the invite increment. A prepared repair changes only the UID, preserving its already committed single invite increment and canonical `test_vendor_authentication_reset` audit; the separate recovery-claim audit is not a duplicate reset audit. Lease renewal after external enable and ownership-scoped compensation ensure a delayed old owner cannot mint a second setup link, complete the winner's marker, disable/delete the winner, or otherwise alter the winning generation. | Verified | `lib/vendor/test-identity.ts`; `lib/vendor/admin-runtime.ts`; `lib/firestore/vendors.ts`; `tests/unit/vendor-test-identity.test.ts`; `tests/unit/vendor-test-admin-runtime.test.ts`; `tests/unit/vendor-test-authentication-reset-store.test.ts` | 2026-07-15 | — | 2026-12-31 |
| F-VENDOR-DISABLE-RESET-SERIALIZATION | Canonical Test Vendor disable and authentication reset serialize on the same Firestore lifecycle record. Any `claimed` or `prepared` reset marker makes disable return the generic 409 before Firebase mutation or audit, even after the claim lease expires; the reset must be recovered first. If disable wins before reset claims the record, the old status-bound reset confirmation becomes stale and a fresh exact preview from `disabled` can reset normally. A `completed` reset marker does not block a later disable. This is repeatability and race containment, not a new approval gate. | Verified | `lib/vendor/lifecycle.ts`; `lib/firestore/vendors.ts`; `tests/unit/vendor-lifecycle.test.ts`; `tests/unit/vendor-test-authentication-reset-store.test.ts` | 2026-07-15 | — | 2026-12-31 |
| F-VENDOR-TEST-MAILBOX-TRANSACTIONAL-REVOCATION | Every canonical Test Vendor mailbox read, mailbox create/update, confirmation creation, and reply commit revalidates inside its Firestore transaction the current active Vendor UID, active Test assignment, Test ticket, active thread link, mailbox identity, and absence of a `claimed`/`prepared` authentication reset. Disable, deassignment, thread unlink, UID rotation, or reset claim therefore revokes stale access before mailbox content/state, confirmation, reply, or receipt changes. | Verified | `lib/firestore/vendors.ts`; `lib/vendor/test-mailbox.ts`; `tests/unit/vendor-test-mailbox-transaction-boundary.test.ts`; `tests/unit/vendor-test-mailbox.test.ts` | 2026-07-15 | — | 2026-12-31 |
| F-INTERNAL-ROSTER-VENDOR-SEPARATION | Identity class takes precedence over email domain. The Admin People and Access roster lists only internal Firebase users in the configured hosted domain. Any principal carrying a `vendor`, `vendor_id`, or `data_mode` custom-claim key is fail-closed as non-staff—even when the value is `false`, empty, or malformed—so partial Vendor-claim drift cannot enter the roster, last-Admin count, role/scope mutation, internal session-cookie validation, or internal ID-token validation. Canonical Vendor authentication is a separate positive path that still requires the exact valid `vendor:true` + canonical `vendor_id` + matching `data_mode` triple. External Vendors remain available only through the separate Vendor administration and portal surfaces and never inherit Editor authority or the absent-scope/all-Spaces default. | Verified | `lib/admin/users.ts`; `lib/auth/session.ts`; `tests/unit/admin-users.test.ts`; `tests/unit/auth-session.test.ts`; `docs/auth-identity-and-access-strategy.md` | 2026-07-15 | — | 2026-12-31 |
| F-DEPLOY-EXACT-REVISION-TRAFFIC | The Cloud Run deployment wrapper now generates one timestamp-plus-48-bit-entropy revision suffix, passes it to `gcloud run deploy`, and after successful creation routes 100% traffic to that exact full revision with `--to-revisions=<revision>=100`. It never promotes floating `LATEST`, so a concurrent deployment cannot make this invocation serve code it did not create. This also clears a prior named-revision rollback pin without leaving the new healthy revision unserved. The wrapper uses the supported `--no-invoker-iam-check` public-shell flag; traffic promotion does not add an `allUsers` binding or otherwise widen application authentication/authorization, and a failed deploy never reaches promotion. | Verified | `scripts/deploy-demo-cloud-run.mjs`; `tests/unit/live-cost-scripts.test.mjs`; `docs/client-production-cutover.md` | 2026-07-15 | — | 2026-12-31 |
| F-PROCESS-AUDIT-PASS1 | The first resumable deployed application-process audit is complete against the canonical Cloud Run revision serving commit `38ebcf530e3fe193547806bace91246ccea20c0b`. Its 281 stable cases cover every guide section and all 32 reviewer-checklist items: 222 cases completed and 59 stopped at precise security, identity, fixture, product-defect, or external-boundary blockers. Current behavior totals are 191 pass, six expected denials, 20 failures, five recorded audit-harness failures, and 59 blocked; process alignment totals are 144 advances, 36 partial, 42 gaps, and 59 blocked. The final run records 137 normalized findings, 1,798 ordered events, 279 case-specific DOM records, 110 bodyless structured records, and two Test-only screenshots. Forty-eight Test mutation intents, effects, and registry entries reconcile exactly; no Live effect occurred. The corrected reusable runner validates evidence ownership, exact enum/deployment metadata, contract drift, resumability, and fail-closed case classification. The current dependency scan adds one Critical infrastructure finding; no product or dependency defect was changed during pass one. | Verified | `docs/status.md`; `docs/pmi-kc-working-process-guide-what-each-action-does-2026-07-16.html`; `scripts/process-audit-cases.mjs`; `scripts/process-audit-runner.mjs`; `tests/unit/process-audit-runner.test.mjs` | 2026-07-18 | — | 2026-08-18 |
| F-PROCESS-AUDIT-REMEDIATION-PREP | The authorized pass-two repair program has an exact disposition for every pass-one finding and an executable baseline for every stable capability. The resumable ledger classifies 106 findings as applicable and 31 as evidence-based exclusions: 14 separately activated Live providers, 11 unsupported or owner-unconfirmed Live/customer requirements that must not be invented, five recommendations broader than already-passing case contracts, and the explicitly superseded `contextualized.md` expectation. All 281 capability rows carry their pass-one result/evidence. Repairs update the Critical WebSocket dependency, persistent Test Lease resolution/business closeout, Maintenance transitions/deep links/terminal truth, safe Test intake, approval/attention fixtures, Test Vendor audit recovery, trusted publication/rollback/pinned continuation, exact Gmail-watch confirmation/idempotency, workflow checklist terminal guards, honest Connections states, and bodyless decision/handoff projections across Approval, Notifications, Communications, Spaces, and property history. Reusable runner amendments are revision-checked and idempotent. The prepared tree was fully validated and integrated as `3033eac81629cd2a67a111256bbdc226b94edbce`; deployed regression repairs are integrated through `f6d5ddbce8b250b64df3bc58c81398f09e33b869`. A clean fixture/role/session restoration checkpoint now exists, while remaining pass-two cases, per-surface sidecar reconciliation, final integration/deployment, and final restoration remain in progress under `F-PROCESS-AUDIT-PASS2-CYCLE`. | Verified | `package-lock.json`; `lib/lease-renewal/test-workflow.ts`; `lib/publication/test-fixture.ts`; `lib/firestore/approval-test-fixtures.ts`; `lib/operations/test-handoffs.ts`; `scripts/process-audit-runner.mjs`; `tests/unit/process-audit-runner.test.mjs`; `docs/status.md` | 2026-07-18 | — | 2026-08-18 |
| F-PROCESS-AUDIT-PASS2-CYCLE | Product commit `f6d5ddbce8b250b64df3bc58c81398f09e33b869` is serving at 100% on Cloud Run revision `pmi-kc-kb-demo-rmrqntfvs-4ebadb1e34a5` with the managed runtime identity; its clean integrated verifier passed 322 test files / 2,290 tests, Firestore 59/59, core E2E 32 passed / 18 intentional skips, all governance/build gates, and zero runtime dependency findings. Deployed `VENDOR-PORTAL-011` passes with a private/no-store/bodyless Waiting→Complete Vendor-handoff projection and no provider or Live effect. The resumable pass-two run has 35 terminal cases—29 pass, five expected denials, one honest `not_reachable` empty state—246 pending, and zero in progress. It proves the canonical Test Vendor lifecycle, Admin/Editor/Renewals scope boundaries, and every Approval capability: eleven reads pass, the empty write-back branch remains honestly `not_reachable`, five isolated Test actions pass, and two execution paths stop before provider construction as expected denials. `APPROVAL-012` reproduced a same-page `?item_id=` selection mismatch; protected PR #79 integrated the repair, and deployed browser proof now shows URL, selected link, detail, Activity, views, filters, and refresh state remain synchronized with zero console errors. Approval ledger and capability rows are reconciled at revision 18: nine applicable findings are resolved, the prior documentation-only row remains evidence-excluded, and all 19 Approval capabilities have terminal pass-two outcomes. All seven Approval fixtures are restored to Ready for Approval; both managed staff identities are restored to Admin with All spaces access; the isolated restricted session is signed out; auth-preflight revision 3 records the honest retained-session posture. No secret, mailbox body, provider payload, external delivery, or Live mutation entered evidence. Remaining cases, their ledger/matrix terminalization, final baseline restoration, exact final integration/deployment, and final parity/report gates remain active. | Verified | `components/approval/ApprovalQueue.tsx`; `tests/unit/approval-queue-component.test.tsx`; `components/maintenance/MaintenanceQueue.tsx`; `lib/firestore/vendors.ts`; `lib/vendor/test-mailbox.ts`; `scripts/process-audit-runner.mjs`; `docs/loop-state.md`; `docs/status.md` | 2026-07-18 | — | 2026-08-18 |
| Q-V1-EDITOR-EXECUTION | RESOLVED by R01 and implemented locally by S20: internal Editors directly execute enabled Low/Medium instances; consequential High instances route to an execution-linked Admin queue; Admin may self-approve with an exact preview and reason. Exact-confirmed workflow communication is Medium; SoR/document/account/OAuth/assignment/accounting changes are High. Technical Blocked conditions cannot be approved away. Only already-enabled exact workflow Gmail actions can use Editor send capability; S25/S26 actions remain Registry-closed despite their local fake boundaries. | Verified | `docs/v1-readiness-audit-round-3-2026-07-14.html`; `docs/feature-suites/execution-authority.md`; `lib/execution/risk-policy.ts`; `lib/execution/authority.ts`; `lib/firestore/action-executions.ts`; `lib/auth/roles.ts` | 2026-07-14 | F-V1-EXECUTION-AUTHORITY-BUILT | 2026-12-31 |
| Q-V1-ENDTOEND-BOUNDARY | RESOLVED by the working-app model: Lease and Maintenance expose complete persistent production Test journeys using invented aliases, explicit actions, bodyless receipts, and Done. Every listed Live provider action remains represented and is activated independently only when its provider contract, authoritative target mapping, credentials, and reconciliation are configured. An undocumented provider contract blocks that Live action only; it does not relabel the stable working application Pre-V1. S25/S26 define the exact Live action keys and gates. | Verified | `docs/feature-suites/lease-renewal-execution.md`; `docs/feature-suites/maintenance-execution.md`; `lib/lease-renewal/test-workflow.ts`; `lib/maintenance/test-workflow.ts` | 2026-07-15 | F-WORKING-APP-V1 | 2026-12-31 |
| Q-SOURCE-PUBLISH-BOUNDARY | RESOLVED by R05 and implemented locally by S21: an Editor addition publishes Active immediately only after server validation inside an Admin-configured connector/root and Editor Space scope, with allowed type/size, malware and sensitivity checks, immutable version/rollback, and append-only audit. A required validator outage fails closed. Imported content can never enable an external action, change Action Registry/config/system prompt, or widen a role/scope. Production policy/root/scanner values and live import/index remain gated. | Verified | `docs/v1-readiness-audit-round-3-2026-07-14.html`; `docs/feature-suites/trusted-publication.md`; `lib/publication`; `app/api/process-definitions`; `app/api/spaces` | 2026-07-14 | F-TRUSTED-PUBLICATION-BUILT | 2026-12-31 |
| Q-VENDOR-PORTAL-IDENTITY | RESOLVED by R04 and implemented locally by S22: Admin creates the external Vendor and the Vendor receives a one-time password-setup link; no self-registration or Admin-visible reusable password. Verified-email TOTP MFA gates ticket detail and mailbox connection. V1 supports only the Vendor's own same-address Gmail/Google Workspace mailbox through per-vendor server-side OAuth, never DWD; Microsoft/other providers are post-V1. Authorization is assigned-ticket-only and every send is exactly confirmed by Vendor or Admin. Live identity/OAuth/token resources and acceptance remain gated. | Verified | `docs/feature-suites/vendor-portal-and-mailbox.md`; `lib/vendor`; `app/vendor`; `tests/unit/vendor-auth.test.ts`; `tests/unit/vendor-gmail-oauth.test.ts` | 2026-07-14 | F-VENDOR-PORTAL-BUILT | 2026-12-31 |
| Q-CONSOLE-REAL-DATA | RESOLVED by S23 and the working-app release: Console inline communication detail is sender, recipients, timestamp, subject, and an inert 240-character/three-line snippet; full body is fetched only inside the authorized Live workflow panel. The server—not a browser flag—selects data mode. Production displays a fresh Live operational projection and clearly labeled isolated production Test records; unavailable providers are named without fixture fallback. | Verified | `docs/feature-suites/console-live-data.md`; `lib/console/live-data.ts`; `lib/console/environment.ts`; `components/console/ConsoleView.tsx`; `tests/unit/console-environment-boundary.test.ts` | 2026-07-15 | F-CONSOLE-LIVE-BOUNDARY-BUILT | 2026-12-31 |
| Q-GMAIL-RETENTION | RESOLVED by R06 and S24: confirmation usability 10 minutes with bodyless record deletion at 30 days; Pub/Sub dedupe 7 days; sync audit 90 days; workflow link 365 days from last authorized update; bodyless send/write/workflow audit 7 years; no persisted V1 AI output or extracted Gmail facts; Admin legal hold/later written policy override deletion. Bounded on-demand cleanup is the safe V1 default; TTL/Scheduler automation is optional. | Verified | `docs/feature-suites/communications-policy.md`; `lib/gmail-hub/retention-policy.ts`; `lib/gmail-hub/retention-store.ts`; `tests/unit/communications-retention.test.ts`; `tests/unit/communications-legal-hold.test.ts` | 2026-07-15 | F-COMMUNICATIONS-POLICY-BUILT | 2026-12-31 |
| Q-GMAIL-RENEWAL-ARTIFACTS | RESOLVED by R07: the exact current owner-renewal and tenant-renewal generator behavior is approved as immutable base artifacts `owner-renewal:v1.0` and `tenant-renewal:v1.0`. Authorized verified value replacement and source-visible AI rewrite/additional edits are allowed; missing recipients/values and false channel-success claims block send. AI may free-draft only from the authorized workflow context, invent no fact/commitment, persist no output by default, and every send is human-reviewed/exact-confirmed. S24 is the executable policy. | Verified | `docs/v1-readiness-audit-round-3-2026-07-14.html`; `lib/lease-renewal/owner-draft.ts`; `lib/lease-renewal/tenant-draft.ts`; `docs/feature-suites/communications-policy.md` | 2026-07-14 | — | 2026-12-31 |
| Q-GMAIL-MAINT-ARTIFACTS | RESOLVED by R07: the exact current maintenance owner-notice generator behavior is approved as immutable base artifact `maintenance-owner:v1.0`, with authorized verified value replacement and source-visible AI rewrite/additional edits. Authoritative assigned workflow recipient/mailbox and required values must resolve before send; AI invents no fact/commitment, persists no output by default, and every owner/Vendor send is exactly confirmed by a permitted human. S22/S24/S26 govern identity, policy, and execution. | Verified | `docs/v1-readiness-audit-round-3-2026-07-14.html`; `lib/maintenance/owner-notice-draft.ts`; `docs/feature-suites/communications-policy.md`; `docs/feature-suites/maintenance-execution.md` | 2026-07-14 | — | 2026-12-31 |
| Q-GMAIL-AI-EXCLUSIONS | V1 keeps exactly the current hard exclusions—owner money, legal/notices, and tenant disputes—with no additions. Future additions remain Admin-governed; this decision does not authorize autonomous model processing. AMENDED 2026-07-23 (`D-RENT-SUGGEST`, `F-ROADMAP-BUILD-AUTHORIZED`): the owner-money exclusion OPENS for the renewal rent number specifically. The app may compute a comp-derived suggested figure that a human Admin must explicitly approve before it enters a draft (S29); legal/notices and tenant disputes stay excluded, and no autonomous model send is authorized. | Verified | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `lib/gmail-inbox-zero/draft-safety.ts`; `docs/status.md` | 2026-07-14 | — | 2026-12-31 |
| Q-GMAIL-WATCH-OWNER | Josiah owns manual Gmail watch renewal and degraded-watch response; no scheduler is assumed or authorized. AMENDED 2026-07-23 (`D-AUTOMATION-LINE`, `F-ROADMAP-BUILD-AUTHORIZED`): that no-scheduler default is lifted for the READ-ONLY watch renewal specifically. A Cloud Scheduler that auto-renews the read-only Gmail watch IS now authorized (owner dependency: Pub/Sub topic + Scheduler, roadmap §5, S31); it performs no send and raises attention only. No scheduler drives any send; manual renewal remains the fallback until armed. | Verified | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `docs/status.md` | 2026-07-14 | — | 2026-12-31 |
| F-V1-REMEDIATION-DECISIONS | Owner ruled all 22 decision-findings from the 2026-07-20 v1 readiness-audit remediation plan (see the decision log). Governance direction: onboarding self-registers but a new account gets no space access until an Admin assigns scope (a new unprovisioned state; absent scopes stays all-spaces for provisioned users); Editors retain single-operator exact-confirm workflow send (consistent with F-SEND-AUTHORIZED); publish is the one canonical process-activation path and activate is retired or redirected; the template store becomes the single editable source of message copy via an in-app editor plus approval lifecycle that composers read from, which evolves and must be reconciled with the immutable v1.0 artifact model (Q-GMAIL-RENEWAL-ARTIFACTS / Q-GMAIL-MAINT-ARTIFACTS) during the build. Accepted with no code: Editor send, bulk high-risk approval, the last-Admin race as a known limitation, and the renewal Sheet staying read-only for v1. Owner or Dan verifies before real drafts reach residents: RentVine field names and the multi-tenant recipient (default addresses all tenants). No ruling weakens the draft-only, no-source-of-truth-write, action-gate, vendor-isolation, or demo-off-in-production invariants. | Verified | `docs/v1-remediation-decisions-2026-07-20.md` | 2026-07-20 | — | 2026-12-31 |

| F-RENTVINE-WRITE-APPROVED | Owner approved RentVine renewal write IN PRINCIPLE (2026-07-22) behind the app confirm/approval gates. The full renewal-write executor + S25 contract are BUILT and unit-proven (`RentvineRenewalExecutor`: read-drift-check, compare-and-set with idempotency, readback, reconcile) over the least-privilege `RenewalMutationProvider` seam and registered in the lease execution matrix (`rentvine.lease.renewal_writeback`, High risk, correction/rollback note). It stays `production_allowed:false` pending a DOCUMENTED write endpoint + provider semantics: a live RentVine read on 2026-07-22 (Slice 1) confirmed RentVine is GET-only (lease export/list/view only, no write endpoint), so the D16 write half is unresolved. No live write client was built (that would guess an undocumented endpoint) and no autonomous write occurs; a human confirms every write. Flipping the gate is a one-line reviewed change once the endpoint is confirmed. | Verified | `lib/lease-renewal/execution/providers.ts`; `lib/lease-renewal/execution/matrix.ts`; `lib/integrations/action-registry-seed.ts`; `tests/unit/rentvine-renewal-executor.test.ts`; `docs/products/rentvine-live-field-map-2026-07-22.md` | 2026-07-22 | — | 2026-12-31 |

| F-MAINT-OWNER-DRAFT-LIVE | Maintenance owner-notice DRAFT went live (Slice 6, 2026-07-22): `gmail.maintenance_owner_notice.draft_create` is `production_allowed:true` (readiness "Approved for Execution", evidence "Documented"), draft-into-Gmail only. The authoritative property-owner email resolves from the live RentVine export at `portfolio.owners[].email` (confirmed present + email-shaped on 25/25 leases; `resolveRenewalRecipient`'s owner channel was extended to search `portfolio.owners[]` / `property.owners[]`). The paired `gmail.maintenance_owner_notice.send` stays `production_allowed:false` — no autonomous send. The migration-readiness governance allowlist was updated so this owner-ruled flip is not flagged as a surprise while any OTHER surprise flip still is. | Verified | `lib/integrations/action-registry-seed.ts`; `lib/lease-renewal/recipient-resolution.ts`; `lib/admin/migration-readiness.ts`; `tests/unit/recipient-resolution.test.ts`; `docs/products/rentvine-live-field-map-2026-07-22.md` | 2026-07-22 | — | 2026-12-31 |

| F-NEGOTIATION-EXCLUDED | AI-driven negotiation is a named product exclusion (D19; ties to Q-GMAIL-AI-EXCLUSIONS). The app DRAFTS the owner renewal email and captures the operator's comp basis (Zillow low/high + PMI rental-analysis number + comps URL), but it NEVER invents, suggests, or negotiates a rent figure: every number is the operator's own input and a human negotiates + decides. An absent comp renders a visible `Needs Verification` marker, never a fabricated value (`buildOwnerRenewalDraft`); a comp-basis write-back proposal with no numbers is a value-less Blocked proposal (`buildCompBasisProposal`). OWNER AUTHORIZED OPENING THIS (2026-07-23, `D-RENT-SUGGEST`, see `F-ROADMAP-BUILD-AUTHORIZED`): S29 lets the app compute a comp-derived SUGGESTED rent number that enters a draft ONLY after an explicit per-number Admin approval (absent comp data still renders `Needs Verification`, never fabricated; a human still decides and sends). This row describes current behavior; S29 supersedes it when built. | Verified | `lib/lease-renewal/owner-draft.ts`; `lib/lease-renewal/writeback-proposal.ts`; `lib/lease-renewal/renewal-progress.ts`; `tests/unit/comp-basis-and-market.test.ts` | 2026-07-22 | — | 2026-12-31 |

| F-OVERNIGHT-RUN-2026-07-22 | The 2026-07-22 unattended overnight build cycle ran to completion per the decision-complete runbook (owner-authorized via the Round-1 decision packet). It shipped: comp basis capture + Zillow deep link (Slice 3), KB answer transparency (Slice 4) + source freshness (Slice 5), maintenance owner-notice draft go-live (Slice 6, F-MAINT-OWNER-DRAFT-LIVE), Admin add-a-Space request intake (Slice 7), a cost-gated re-index control (Slice 8), the RentVine write approval-in-principle with the executor kept gated OFF (Slice 9, F-RENTVINE-WRITE-APPROVED), and Dotloop OAuth scaffolding gated OFF (Slice 10). Live work: RentVine field map confirmed (Slice 1); the Sheet write-back harness was built but its live proof DEFERRED pending the Sheets WRITE scope on the lease-renewal-reader SA's DWD grant (Slice 2). Every safety invariant held: no autonomous send or live system-of-record write, RentVine write stays `production_allowed:false` pending a documented endpoint, the Sheet write-back smoke targets only a NEW test sheet, and the 11-space KB config was preserved. | Verified | `docs/overnight-build-run-2026-07-22.md`; `docs/audit-2026-07-22.html`; `docs/loop-state.md`; `docs/status.md` | 2026-07-22 | — | 2026-12-31 |

| F-OVERNIGHT-QA-2026-07-22 | The 2026-07-22 build was adversarially browser-QA'd (Slice 11) against a local emulator instance (Firestore :8090 + Auth :9099, demo Admin), read-only vs the live-connected RentVine/Sheet, with zero console errors. Every shipped flow verified PASS: auth boundary (unauth->sign-in; Editor->403 on admin APIs), KB answer transparency ("Answered by <model> · N sources" renders), KB freshness (omits when a source has no review date; never fabricated), maintenance owner-notice draft (no send affordance anywhere on /maintenance), add-a-Space (persists + emits provisioning commands preserving all 11 Spaces; Admin-only), cost-gated re-index (button disabled until confirmed; API refuses without confirm; never ingests), RentVine write-back (gated off; "no write, no system-of-record update"; no execute control), and Dotloop connect ("authorize in the morning"; connect returns credentials_not_configured with no live call). The comp-basis live browser walk was safely BLOCKED (the demo session could not authenticate the live RentVine read, so no customer PII was exposed); its logic is comprehensively unit-verified. One finding was fixed at source: F1, the `seed-action-registry` executable allow-list had not been updated for the Slice 6 maintenance-draft flip, so `npm run seed:action-registry` refused; added the key + a regression test. The app is light-only by design (no dark-mode CSS; unchanged by the run). | Verified | `docs/customer-demo-walkthrough-2026-07-21.html`; `scripts/seed-action-registry.ts`; `tests/unit/seed-action-registry-allowlist.test.ts`; `docs/status.md` | 2026-07-22 | — | 2026-12-31 |

| F-ROADMAP-BUILD-AUTHORIZED | Owner directed (2026-07-23) that PMI KC is a full suite of applications, not a demo with a permanent "next phase" list: every roadmap gap is built to its external seam or justified as a permanent NEVER, with no third "deferred indefinitely" state. The scope, the ordered build waves (S28–S39), and the exact owner-dependency list live in `docs/roadmap-unblock-2026-07-23.md`. Build-to-seam rule: for each suite the loop builds the app-plane plus the live provider (replacing any fake) plus the full S25/S26 preview/confirm/receipt/rollback contract plus the staged gate-flip, and stops ONLY at the one named owner dependency (a documented endpoint, a credential/scope grant, a vendor confirmation, or a billing approval), never at a fake provider or a whole deferred feature. Four owner Q&A decisions: D-RENT-SUGGEST (the app MAY compute a comp-derived SUGGESTED renewal rent number that enters a draft only after an explicit per-number Admin approval; authorizes S29 to supersede F-NEGOTIATION-EXCLUDED; a human still sends); D-RENTVINE-ENDPOINT (owner provides the documented RentVine write endpoint, the loop builds all else plus the flip); D-BUILDER-FULL (a full no-code page/layout builder, S37); D-AUTOMATION-LINE (automated notifications to internal staff and read-only Gmail-watch auto-renew are allowed, while every client-facing send stays human-confirmed). Strengthens F-SEND-AUTHORIZED; the safety NEVERs in roadmap §7 are the only permanent exclusions. | Verified | `docs/roadmap-unblock-2026-07-23.md`; `AGENTS.md`; `docs/autonomous-agent-runner.md` | 2026-07-23 | — | 2026-12-31 |

## Supersede Log

When new direction lands, delete the superseded rule from the active doc (do not append next to it),
add a row here with a unique `marker` phrase, and point the replacing fact's `supersedes` at the old
id. The freshness gate greps each `marker` across the active governance set and fails if a superseded
rule still reads as active.

| superseded-id                    | replaced-by-id           | date       | where-old-text-lived                                                                                                                                           | marker                                                                  | why                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | ------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| LEGACY-OWNER-EMAIL-FIRST         | F-GMAIL-LANE             | 2026-06-25 | `docs/products/README.md`                                                                                                                                      | Owner-email-first Gmail workflow                                        | This was first replaced by a Dan-mailbox lane, then superseded again on 2026-07-14 by workflow-linked renewal and maintenance communications.                                                                                                                                                                                                                                        |
| COPY-GMAIL-READONLY-V1           | F-GMAIL-HUB              | 2026-07-10 | `app/admin/gmail-inbox-zero/page.tsx`                                                                                                                          | Read-only v1 Gmail Inbox 0 management placeholder                       | The static read-only v1 admin placeholder is replaced by the live Template & triage workspace (S15 Slice 2); the honest not-connected Gmail status is preserved, the placeholder copy is not appended beside it.                                                                                                                                                                     |
| F-LOCALMODEL-GAP                 | F-LOCALMODEL-SEAM        | 2026-06-25 | `lib/llm/answer.ts`                                                                                                                                            | model calls are Gemini-only                                             | The provider seam (`lib/llm/model-provider.ts`) lets a free local model stand in for Gemini via the same path; the Gemini-only gap is closed (S9).                                                                                                                                                                                                                                   |
| COPY-RV-SOT                      | F-VOICE                  | 2026-06-25 | `lib/connections/connector-catalog.ts`                                                                                                                         | read-authoritative source of truth                                      | RentVine "powers" dropped internal jargon for plain operator copy (S2).                                                                                                                                                                                                                                                                                                              |
| COPY-NEXT-RELEASE                | F-VOICE                  | 2026-06-25 | `components/connections/ConnectorCard.tsx`                                                                                                                     | Available in the next release.                                          | Deleted the dead disabled control that advertised an unbuilt feature (S2).                                                                                                                                                                                                                                                                                                           |
| COPY-VERIFY-CONNECT              | F-VOICE                  | 2026-06-25 | `lib/connections/connection-status.ts`                                                                                                                         | All details provided — PMI will verify and connect.                     | Ready-to-verify detail rewritten to "Ready to connect."; verification is not live yet (S2).                                                                                                                                                                                                                                                                                          |
| COPY-SUBTITLE-VERIFY             | F-VOICE                  | 2026-06-25 | `components/connections/ConnectionCenter.tsx`                                                                                                                  | PMI handles the credentials and verification for you.                   | Connections subtitle dropped the not-live verification promise (S2).                                                                                                                                                                                                                                                                                                                 |
| COPY-SHEET-CONTROLPLANE          | F-VOICE-2                | 2026-07-02 | `lib/connections/connector-catalog.ts`                                                                                                                         | the exception and control plane                                         | Google Sheets "powers" line rewritten to say what the app reads and flags; the value-prop jargon is gone (S13 A1).                                                                                                                                                                                                                                                                   |
| COPY-CONN-PMI-HANDLES            | F-VOICE-2                | 2026-07-02 | `components/connections/ConnectionCenter.tsx`                                                                                                                  | PMI handles the setup for you                                           | Connections subtitle rewritten to "the app stores the credentials and checks each connection" (S13 A1).                                                                                                                                                                                                                                                                              |
| COPY-SIM-LEXICON                 | F-VOICE-2                | 2026-07-02 | `components/console/ConsoleView.tsx`                                                                                                                           | run a safe simulation                                                   | User-facing "simulation" replaced with "test run" across the Console, Ask, and workflow surfaces; internal identifiers (simulation_only) unchanged (S13 A4).                                                                                                                                                                                                                         |
| Q-IA-RENEWALS                    | F-IA-CONSOLE-HOME        | 2026-06-29 | `docs/facts.md`                                                                                                                                                | fold under a Processes dropdown                                         | Shipped as the operations-console home with Renewals under the Spaces dropdown (R1); the earlier Processes-dropdown framing is retired. Re-pointed to F-IA-CONSOLE-HOME when F-OPS-CONSOLE-IA was superseded (S6a).                                                                                                                                                                  |
| Q-ASK-RESCOPE                    | F-ACTION-CONSOLE         | 2026-06-29 | `docs/facts.md`                                                                                                                                                | the R4 action-console slice (not yet built)                             | The four Ask metadata selects are retired and the Console launches a simulation workflow; the rescope is built (R4a).                                                                                                                                                                                                                                                                |
| F-OPS-CONSOLE-IA                 | F-IA-CONSOLE-HOME        | 2026-06-30 | `docs/facts.md`                                                                                                                                                | the home (`/`) is a launcher                                            | The card-launcher home was replaced by the Console (Console-as-home, at `/` and `/ask`); the standalone Processes nav tab retired, its routes + engine kept (A-IA-V2, S6a).                                                                                                                                                                                                          |
| Q-BAILEY                         | F-OPEN-PLACEHOLDER       | 2026-06-29 | `docs/facts.md`                                                                                                                                                | source-state label's meaning is unconfirmed                             | Owner confirmed the meaning; the "Bailey Placeholder" state is renamed to "Open Placeholder" (plain language).                                                                                                                                                                                                                                                                       |
| SPACETEETH-HARD-GATES            | F-PRECUST-CYCLE          | 2026-07-02 | `docs/feature-suites/space-teeth.md`                                                                                                                           | e-sign + certified-funds hard gates                                     | Move-in V1 has NO hard blocking gates per the owner's Q2 override (`docs/products/v1-process-qa.md`, 2026-07-01); the desk tracks e-sign/certified-funds as checklist flags. The stale spec line predated the override.                                                                                                                                                              |
| F-CONSOLE-APP-STATE              | F-CONSOLE-ACT-IN-PLACE   | 2026-07-09 | `docs/facts.md`                                                                                                                                                | NEVER executes: approvals/sends/writes stay in their own gated surfaces | The Console now acts in place for ONE app-plane decision — approving a queue item via the existing PATCH — so the blanket never-executes framing is retired. No external action is reachable (approve records a decision only; execute stays blocked); every other surface stays read-only/deep-link (A4).                                                                           |
| OQ-UI-1-TAB-LAYOUT               | F-APPROVAL-QUEUE-UNIFIED | 2026-07-09 | `docs/products/v1-process-qa.md`                                                                                                                               | renewal review and write-back queue as peer tabs beside All items       | Slice 4a collapses the Approval Queue's four peer tabs into one urgent-first value-free "Needs your decision" list plus an "Other views" disclosure (owner-directed 2026-07-02, F-PRECUST-CYCLE). Only the tab arrangement is retired; the value-free-triage, resolve/approve-on-the-run-page, and ROW_KEYS invariants of F-RENEWAL-REVIEW-SUBTAB / F-WRITEBACK-QUEUE are preserved. |
| APPROVAL-LIST-MOBILE-PLAN        | F-RENEWAL-DECIDER-MOBILE | 2026-07-10 | `docs/plan.md`                                                                                                                                                 | Mobile Approval Queue v1 uses the same queue list and detail view       | S14 replaces the list-only mobile renewal-decision direction with a one-card-at-a-time decider while retaining the established desktop list and bulk bar as an alternate mode.                                                                                                                                                                                                       |
| APPROVAL-LIST-MOBILE-GAP         | F-RENEWAL-DECIDER-MOBILE | 2026-07-10 | `docs/product-definition-gap-plan.md`; `docs/products/lease-renewal-agent.md`                                                                                  | Mobile Approval Queue v1 should use the same queue list and detail view | S14 replaces the list-only mobile renewal-decision direction with a one-card-at-a-time decider while retaining the established desktop list and bulk bar as an alternate mode.                                                                                                                                                                                                       |
| CODEX-CONFIG-POINTER             | F-RUNNER-AGNOSTIC        | 2026-07-11 | `AGENTS.md`; `scripts/check-router-boundary.mjs`; `docs/away-mode.md`; `docs/facts.md`                                                                         | `.codex/config.toml` for Codex                                          | The tracked Codex harness config was removed because repo-local runner settings were interfering; Codex now reads the shared router directly, and the boundary gate no longer requires a Codex adapter file.                                                                                                                                                                         |
| ACTION-REGISTRY-ALL-FALSE        | F-SEND-AUTHORIZED        | 2026-07-11 | `docs/facts.md`; `docs/integration-architecture.md`; `docs/loop-state.md`                                                                                      | every Action Registry entry is `production_allowed:false`               | The blanket all-false claim was retired after the owner-approved, compose-only `gmail.renewal_notice.draft_create` entry became the sole allowlisted external action. No system-of-record write or send became executable.                                                                                                                                                           |
| DEMO-FIRESTORE-PROJECT-ID-SAFETY | F-DEMO-FIRESTORE-GUARD   | 2026-07-13 | `scripts/demo-firestore.mjs`; `scripts/seed-demo-records.mjs`; `docs/implement.md`                                                                             | a project ID is sufficient for demo Firestore reset                     | Demo mutation now requires a reachable loopback emulator before Admin initialization; a production-looking project id is only a local namespace and can never authorize live fallback.                                                                                                                                                                                               |
| MAINT-PHOTO-UNGATED-CONTROL      | F-MAINT-PHOTO            | 2026-07-13 | `components/maintenance/MaintenanceCapture.tsx`; `app/api/maintenance/photo/route.ts`; `docs/feature-suites/maintenance-intake.md`                             | Add/take-photo is available whenever Drive is configured                | Photo selection and POST execution now share the committed Action Registry decision. The false entry removes the input and refuses before body/store work; the historical Drive capability remains dormant.                                                                                                                                                                          |
| GMAIL-HUB-SIMULATOR-FINAL        | F-GMAIL-LIVE-DIRECTION   | 2026-07-13 | `docs/products/gmail-inbox-zero.md`; `AGENTS.md`; `docs/feature-suites/gmail-hub.md`; `docs/facts.md`                                                          | request NO Gmail READ scope this cycle                                  | S15 remains historical evidence, but the owner now directs the final Gmail Hub toward bounded per-user live Gmail with exact-confirmation send. The simulator remains only a separate fallback.                                                                                                                                                                                      |
| GMAIL-PER-USER-REPRESENTATIONAL  | F-GMAIL-LIVE-PER-USER    | 2026-07-13 | `AGENTS.md`; `docs/products/gmail-inbox-zero.md`; `docs/facts.md`                                                                                              | per-user domain-wide (console overhaul Slice F, representational only)  | The per-user model now has a local S19 runtime and UI; all live permissions/actions remain gated.                                                                                                                                                                                                                                                                                    |
| GMAIL-CREATEDRAFT-ONLY-RUNTIME   | F-GMAIL-RUNTIME-S19      | 2026-07-13 | `docs/integration-architecture.md`; `docs/products/gmail-inbox-zero.md`; `docs/facts.md`                                                                       | exposes ONLY `createDraft`                                              | The shared runtime now includes separately gated explicit-send/read/sync methods. The executable renewal-notice route remains draft-only and cannot invoke the new send method.                                                                                                                                                                                                      |
| GMAIL-S19-ALL-ACTIONS-FALSE      | F-GMAIL-LIVE-PER-USER    | 2026-07-13 | `docs/feature-suites/gmail-live-per-user.md`; `docs/integration-architecture.md`; `docs/loop-state.md`; `docs/facts.md`                                        | all new S19 Gmail actions remain `production_allowed:false`             | The owner-approved bounded self-mailbox proof succeeded, so `gmail.mailbox.read` is now the only promoted S19 action. Send, reply, draft, label, watch provisioning, and deployment remain gated.                                                                                                                                                                                    |
| GMAIL-S19-WRITES-DISABLED        | F-GMAIL-ACTIONS-ACTIVE   | 2026-07-13 | `docs/feature-suites/gmail-live-per-user.md`; `docs/integration-architecture.md`; `docs/environment-handoff.md`; `docs/loop-state.md`; `docs/facts.md`         | send, reply, draft, label, Pub/Sub, and deployment remain disabled      | The owner explicitly approved and activated all five separately governed Gmail Inbox 0 actions plus Pub/Sub/watch and deployment. Exact confirmation remains required for send/reply.                                                                                                                                                                                                |
| GMAIL-S19-SELF-PILOT             | F-GMAIL-LIVE-ACTIVE      | 2026-07-13 | `AGENTS.md`; `.env.example`; `docs/feature-suites/gmail-live-per-user.md`; `docs/products/gmail-inbox-zero.md`; `docs/environment-handoff.md`; `docs/facts.md` | configured self-only pilot / `GMAIL_PILOT_USERS`                        | The rollout allowlist is removed. Server-verified `pmikcmetro.com` users operate only their own mailbox; production proof remains self-addressed.                                                                                                                                                                                                                                    |
| GMAIL-S19-TWO-SCOPES             | F-GMAIL-RUNTIME-ACTIVE   | 2026-07-13 | `lib/gmail-runtime/dwd-token.ts`; `docs/feature-suites/gmail-live-per-user.md`; `docs/integration-architecture.md`; `docs/facts.md`                            | Gmail DWD permits only readonly and compose                             | The exact runtime allowlist now also includes `gmail.labels` and `gmail.modify` for bounded user-label application.                                                                                                                                                                                                                                                                  |

| GMAIL-NOTIFICATION-ACCESS-GATE | F-GMAIL-PROD-ACTIVE | 2026-07-13 | `lib/notifications/families.ts`; `components/gmail-hub/GmailHubHome.tsx`; `docs/feature-suites/unified-console-and-attention.md`; `docs/facts.md` | Gmail-dependent notification families are unavailable because mailbox access is waiting | S19 activated per-user mailbox access and authenticated push. The two product-specific reply-event families remain unavailable only because their source-backed classification rules are not configured; access is no longer their blocker. |
| GMAIL-GENERAL-INBOX-PRODUCT | F-GMAIL-WORKFLOW-BOUNDARY | 2026-07-14 | `AGENTS.md`; `docs/north-star.md`; `docs/plan.md`; `docs/products/gmail-inbox-zero.md`; `docs/feature-suites/gmail-live-per-user.md` | per-user live mailbox product | Proven per-user transport remains, but the end-user product starts from an authorized renewal/maintenance entity or linked-reply attention and cannot browse a general inbox. |
| GMAIL-FIVE-GENERIC-ACTIONS | F-EXTERNAL-ACTION-GATE | 2026-07-14 | `docs/facts.md`; `docs/integration-architecture.md`; `lib/integrations/action-registry-seed.ts` | five Inbox 0 actions are Approved for Execution | Generic new-message send is Disabled; workflow read/draft/reply/label remain separately governed; renewal and maintenance notice drafts are Planned. |
| GMAIL-LEGACY-EVENT-SENDER | F-GMAIL-SEND-CONTROL | 2026-07-14 | `lib/notifications/approval.ts`; `docs/auth-identity-and-access-strategy.md`; `docs/client-checklist.md` | event-driven approval Gmail delivery | The legacy notification sender is hard-disabled even when configured. In-app is the default; any future email notification lane needs a separate approved human-confirmed design. |
| KB-TWO-ROLE-SUMMARY | F-RBAC-SUBUSERS | 2026-07-14 | `docs/plan.md`; `docs/products/pmi-kc-kb.md`; `lib/auth/roles.ts` | internal V1 has only product-facing Admin and User roles | Active guidance now names the implemented `Editor`, `Approver`, and `Admin` capability tiers plus optional renewals/maintenance scopes. Dan and Josiah may remain the only initial operators without inventing a separate `User` tier. |
| V1-AUDIT-PENDING-OWNER | F-V1-AUDIT-ROUND1 | 2026-07-14 | `docs/v1-readiness-audit-2026-07-14.html`; `docs/v1-readiness-audit-round-2-2026-07-14.html`; `docs/plan.md` | every primary tab and release owner still awaits initial alignment | Round 1 approved the tab visions, internal release boundary, workflow-only Communications direction, operating owners, AI exclusions, and safe defaults. Only the exact execution/publication/vendor/retention/template/live-data semantics isolated in Round 2 remain open. |
| V1-AUDIT-ROUND2-OPEN | F-V1-AUDIT-ROUND2 | 2026-07-14 | `docs/v1-readiness-audit-round-2-2026-07-14.html`; `docs/v1-readiness-audit-round-3-2026-07-14.html`; `docs/plan.md` | the Round 1 internal/no-non-Gmail-write contract and Round 2-is-open guidance | Round 2 makes the vendor portal and named workflow external writes V1 product requirements. The final Round 3 now isolates only the exact action matrix and the remaining execution, provider, containment, retention, artifact, Console, and release semantics. |
| V1-AUDIT-ROUND3-OPEN | F-V1-AUDIT-ROUND3 | 2026-07-14 | `docs/plan.md`; `docs/loop-state.md`; `docs/client-checklist.md`; `docs/research-backlog.md` | Round 3 must | R01–R09 are answered. Active guidance now routes implementation through the final facts, S20–S27, and the V1 gap implementation program instead of asking the owner to classify the same actions again. |
| V1-EDITOR-PREPARE-ONLY | F-V1-EXECUTION-AUTHORITY-BUILT | 2026-07-14 | `AGENTS.md`; `docs/plan.md`; `docs/products/pmi-kc-kb.md`; `docs/feature-suites/execution-authority.md`; `lib/auth/roles.ts` | internal Editors to prepare actions for later approval | S20 now ships the local authority foundation and Editor exact-confirmed workflow-send capability: enabled Low/Medium executes directly; consequential High routes to Admin. Registry-closed S25/S26 actions remain non-executable for technical reasons, not because Editor is universally prepare-only. |
| EDITOR-CONTENT-APPROVAL-DEFAULT | F-TRUSTED-PUBLICATION-BUILT | 2026-07-14 | `docs/plan.md`; `docs/product-definition-gap-plan.md`; `docs/feature-suites/trusted-publication.md`; `components/workflows/ProcessDefinitionDetailClient.tsx` | validation-passing Editor content still waits for approval | S21 now routes the active process/source publication surfaces through the automated trust boundary and creates no routine content Approval Queue item. |
| PRODUCTION-FIXTURE-FALLBACK | F-CONSOLE-LIVE-BOUNDARY-BUILT | 2026-07-14 | `lib/console/environment.ts`; `lib/console/live-data.ts`; `components/console/ConsoleView.tsx`; `scripts/preflight-production-cutover.mjs` | production Console may silently use sample/demo data or a browser-selected fixture mode | S23 derives mode on the server. Ordinary production constructs only the live-provider boundary and names source failures; fixtures are guarded to local/emulator/test or explicitly named non-production test deployments with a persistent badge, and the production cutover rejects that test-deployment marker. |
| MANAGED-DOMAIN-ONLY-END-USERS | F-VENDOR-PORTAL-BUILT | 2026-07-14 | `AGENTS.md`; `docs/auth-identity-and-access-strategy.md`; `docs/feature-suites/vendor-portal-and-mailbox.md`; `lib/vendor/auth.ts` | future external Vendor Firebase/mailbox principal | S22 now implements the separate local Vendor identity/OAuth boundary; staff/cloud/DWD identities remain managed-domain-only and all live Vendor setup remains gated. |
| GMAIL-RETENTION-OPEN | F-COMMUNICATIONS-POLICY-BUILT | 2026-07-14 | `docs/plan.md`; `docs/client-checklist.md`; `docs/research-backlog.md`; `docs/feature-suites/communications-policy.md` | retention is undecided or S24 cleanup/legal hold is unbuilt | R06 periods and the S24 local policy/worker/hold boundary are complete; only separately authorized live TTL/scheduler activation remains. |
| WORKFLOW-TEMPLATES-UNAPPROVED | F-COMMUNICATIONS-POLICY-BUILT | 2026-07-14 | `docs/plan.md`; `docs/products/gmail-inbox-zero.md`; `docs/feature-suites/communications-policy.md` | v1.0 artifacts and governed AI reply are unbuilt | Exactly three immutable v1.0 artifacts plus transient source-backed `workflow-reply:v1.0` are Local green; S25/S26 still own authoritative runtime mappings and action execution. |
| LEASE-EXTERNAL-EXECUTION-LATER | F-LEASE-V1-EXECUTION-BOUNDARY | 2026-07-14 | `docs/plan.md`; `docs/products/lease-renewal-agent.md`; `docs/feature-suites/lease-renewal-execution.md`; `lib/lease-renewal/execution` | non-Gmail renewal writes are later work | Every R02 group is represented in the local S25 execution graph and fake adapters; real provider evidence/promotion remains release-gated per action. |
| MAINTENANCE-EXTERNAL-EXECUTION-LATER | F-MAINTENANCE-V1-EXECUTION-BOUNDARY | 2026-07-14 | `docs/plan.md`; `docs/products/pmi-kc-kb.md`; `docs/feature-suites/maintenance-execution.md`; `lib/maintenance/execution` | maintenance external writes are later work | Every R03 group is represented in the local S26 execution graph and fake adapters; real provider evidence/promotion remains release-gated per action. |
| V1-INTERNAL-BEFORE-WRITES | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-14 | `docs/north-star.md`; `docs/plan.md`; `docs/products/README.md`; `docs/v1-pre-release-report-2026-07-14.md` | production launch should happen before external write paths are added | S27 now makes the candidate visibly pre-V1 and rejects final V1 until every required action, Vendor portal, browser/operations proof, and both named acceptances pass. |
| EXTERNAL-EXECUTION-BARE-APPROVER-UID | F-EXTERNAL-EXECUTION-AUTHORITY-BOUNDARY | 2026-07-14 | `lib/external-execution/types.ts`; `lib/external-execution/orchestrator.ts`; `tests/unit/external-execution-authority.test.ts` | a nonempty `approvedByUid` field is sufficient local proof of High authority | External orchestration now requires S20's exact-preview, role-aware, reasoned approval and revalidates it with scope immediately before the claim; the former bare uid marker no longer exists. |
| PUBLICATION-UNBOUNDED-BODY | F-TRUSTED-PUBLICATION-BUILT | 2026-07-14 | `lib/publication/content.ts`; `lib/publication/service.ts`; `docs/feature-suites/trusted-publication.md` | publication buffers the whole upload before enforcing size | The S21 intake now enforces declared and actual limits while streaming, stores hash-verified Firestore-sized chunks, and reuses an immutable content reference for rollback. |
| COMMUNICATIONS-FULL-SCAN-CLEANUP | F-COMMUNICATIONS-POLICY-BUILT | 2026-07-14 | `lib/gmail-hub/retention-store.ts`; `docs/feature-suites/communications-policy.md` | communications cleanup scans each full collection | S24 cleanup now uses bounded indexed expiry queries and transactional candidate rechecks; legal hold nulls both `expires_at` and `expires_at_ms` and the local worker is emulator-only. |
| GENERIC-FAKE-V1-ACCEPTANCE | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-14 | `lib/release/fake-acceptance.ts`; `docs/v1-pre-release-report-2026-07-14.md` | generic fake adapter acceptance is sufficient for release | The local harness now traverses the real S22 domain services and every typed S25/S26 provider adapter, but the manifest permanently rejects local/synthetic evidence as a final-V1 substitute. |
| CUTOVER-NOTIFICATION-EMAIL-RESIDUAL | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-14 | `docs/client-production-cutover.md`; `docs/v1-monitoring-and-rollback-plan-2026-07-14.md` | approval email remains a cutover dependency | V1 approval delivery is in-app; cutover fixtures keep notification email off and do not turn the hard-disabled legacy sender into a residual requirement. |
| ROLLBACK-CLOUD-RUN-SERVICE-DELETE | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-14 | `docs/client-production-cutover.md`; `docs/v1-monitoring-and-rollback-plan-2026-07-14.md` | delete the Cloud Run service during rollback | S27 rollback requires a captured prior revision and restores traffic with `update-traffic`; service deletion is not an accepted rollback path. |
| CUTOVER-CONFLICT-PINNED-COMMANDS | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-15 | `docs/status.md`; `docs/loop-state.md`; `docs/client-production-cutover.md`; `docs/feature-suites/v1-release-acceptance.md` | mismatched cutover commands remain pinned | A malformed or conflicting project/identity/topic/audience/location input now suppresses every setup/corpus/deploy/rollback command; a canonical project value in the report is context, not an executable fallback. |
| CORPUS-BLOCKED-COMMAND-PREVIEW | F-V1-RELEASE-READINESS-BOUNDARY | 2026-07-15 | `scripts/source-corpus-manifest.mjs`; `docs/client-production-cutover.md` | blocked corpus plans still print executable command arrays | Source-corpus planning now emits commands only when every entry is production-ready and all command inputs are strict, non-placeholder, and workspace-contained. |
| PREV1-UNTIL-EVERY-PROVIDER | F-WORKING-APP-V1 | 2026-07-15 | `docs/north-star.md`; `docs/plan.md`; `docs/products/README.md`; `docs/autonomous-agent-runner.md`; `docs/ai-execution-workflow.md` | intermediate deployments are pre-V1 | A stable production app with complete isolated Test workflows is V1. Live-provider activation is tracked per action and does not rename the app. |
| PROVIDERS-BLOCK-APP-V1 | F-WORKING-APP-V1 | 2026-07-15 | `docs/plan.md`; `docs/north-star.md`; `docs/products/README.md` | real provider contracts/mappings/proofs still block final V1 | A missing provider contract blocks that Live action only. The Test adapter and app workflow may still be complete and accepted. |
| CONSOLE-LIVE-ONLY-PRODUCTION | F-PRODUCTION-DUAL-DATA-LANES | 2026-07-15 | `docs/north-star.md`; `docs/plan.md`; `docs/implement.md`; `docs/products/README.md` | ordinary production constructs only the live-provider boundary | Production carries separate Live and visibly labeled Test projections simultaneously; no fallback or browser mode switch exists. |
| TEST-EVIDENCE-NEVER-CLOSES-APP | F-WORKING-APP-V1 | 2026-07-15 | `docs/autonomous-agent-runner.md`; `docs/ai-execution-workflow.md`; `docs/plan.md` | a local receipt never closes an external evidence row | Production Test receipts close application-workflow evidence while remaining structurally ineligible for Live-provider evidence. |
| TTL-SCHEDULER-RELEASE-GATE | F-RETENTION-SAFE-DEFAULT | 2026-07-15 | `docs/plan.md`; `docs/implement.md`; `docs/ai-execution-workflow.md`; `docs/products/README.md` | production indexes/held-record migration/TTL/scheduler remain separate S24 gates | Bounded on-demand cleanup and bodyless storage are the V1 safe default; automatic TTL/scheduling are optional operations improvements. |
| SOURCE-SCANNER-AS-V1-GATE | F-WORKING-APP-V1 | 2026-07-15 | `docs/feature-suites/trusted-publication.md`; `docs/feature-suites/README.md`; `docs/pmi-kc-v1-working-app-state-2026-07-15.html` | a new production root and scanner must activate before the application can be V1 | Existing approved sources and the deployed publication trust boundary support Working V1. A new root/scanner/import connection activates independently; Test publication never substitutes for a Live scan. |
| NAMED-SIGNOFF-V1-GATE | F-WORKING-APP-V1 | 2026-07-15 | `docs/plan.md`; `docs/ai-execution-workflow.md`; `docs/implement.md`; `docs/products/README.md` | named Dan/Josiah acceptance | Named signoff remains visible metadata but cannot convert a working app into Pre-V1. |
| PRODUCTION-TEST-PROVIDER-BLANKET-BAN | F-EXTERNAL-EXECUTION-DATA-LANE | 2026-07-15 | `docs/autonomous-agent-runner.md`; `docs/ai-execution-workflow.md`; `docs/plan.md`; `docs/implement.md` | production must reject fake providers | Production permits only the branded, isolated, no-client Test workspace; Live orchestration continues to reject every Test adapter/reference. |
| UNDOCUMENTED-PROVIDER-BLOCKS-V1 | F-WORKING-APP-V1 | 2026-07-15 | `docs/north-star.md`; `docs/plan.md`; `docs/products/README.md` | an undocumented provider contract blocks final V1 | It blocks only activation of that Live provider action; the working Test workflow remains V1-complete. |
| GMAIL-FOUR-WORKFLOW-ACTIONS | F-GMAIL-ACTIONS-ACTIVE | 2026-07-15 | `docs/facts.md`; `docs/integration-architecture.md`; `docs/environment-handoff.md` | linked read, draft, exact reply, and label are all production-enabled | The unused draft route lacks the exact-confirmation/idempotency/reconciliation UI ledger and is production-closed before Gmail construction. Linked read, exact-confirmed reply, and approved label remain enabled. |
| V1-BROWSER-ROLLBACK-LOCAL-ONLY | F-V1-BROWSER-ROLLBACK | 2026-07-15 | `docs/facts.md`; `docs/evidence/working-app-v1-production-2026-07-15.md`; `docs/v1-tab-browser-acceptance-plan-2026-07-14.md`; `docs/v1-monitoring-and-rollback-plan-2026-07-14.md` | browser evidence is local-only and production browser/rollback remain gated | The final serving revision passed delayed direct signed-in desktop/375px production browser acceptance. Its captured-predecessor traffic rollback/restore rehearsal also passed the public auth boundaries and existing signed-in Console, while the earlier `f02112d / 00025-mhw` rollback is preserved as historical evidence. |
| F-CURRENT-SERVING-CHECKPOINT-2026-07-15 | F-CURRENT-SERVING-CHECKPOINT-2026-07-22 | 2026-07-18 | `docs/facts.md`; `docs/loop-state.md`; `docs/plan.md`; `docs/status.md` | Cloud Run currently serves commit `38ebcf530e3fe193547806bace91246ccea20c0b` | The active serving checkpoint advanced through the fully validated audit-remediation deployments; the 2026-07-15 revision remains historical evidence only. |
| F-CURRENT-SERVING-CHECKPOINT-2026-07-18 | F-CURRENT-SERVING-CHECKPOINT-2026-07-22 | 2026-07-19 | `docs/facts.md`; `docs/loop-state.md`; `docs/status.md` | sha256:39e807a8e8365c881a6443757de2b62c78b668d51f8aba04b3c20a0f552a82b2 | The 2026-07-19 owner-approved deploy advanced the serving checkpoint to c87f54d (F-SEND-AUTHORIZED go-live + activated renewal-notice draft gate); the prior f6d5ddb / rmrqntfvs revision is retained as the rollback target and historical evidence. |
| F-CURRENT-SERVING-CHECKPOINT-2026-07-19 | F-CURRENT-SERVING-CHECKPOINT-2026-07-22 | 2026-07-21 | `docs/facts.md`; `docs/loop-state.md`; `docs/plan.md`; `docs/status.md` | c87f54d02185507fe47280093a6af4da49ae9656 | The 2026-07-21 owner-authorized redeploy advanced the serving checkpoint to f4330ec (completed v1 remediation + governance/QA doc realignment) on revision rmruogj57 at 100%; a same-day owner-authorized QA-fix redeploy then advanced the same 2026-07-21 checkpoint further to 8243b88 (the browser-QA pass: P3.2 sign-in copy + P6.3 High-risk approval reason) on revision rmrv6motb, so rmruogj57 became the retained rollback target and rmrsg73yg / ead5da5 dropped out of the rollback slot; the 2026-07-19 c87f54d / rmrrv992z go-live revision is historical evidence only. |
| F-CURRENT-SERVING-CHECKPOINT-2026-07-21 | F-CURRENT-SERVING-CHECKPOINT-2026-07-22 | 2026-07-22 | `docs/facts.md`; `docs/loop-state.md`; `docs/status.md` | 8243b88 / rmrv6motb | The 2026-07-22 owner-authorized unattended overnight-cycle deploy advanced the serving checkpoint to 7663cec on revision rmrwmk2kn at 100%; rmrwc70pc (the prior same-day Sheet write-back deploy) became the retained rollback target and rmruogj57 dropped out of the rollback slot; the 2026-07-21 8243b88 / rmrv6motb revision is historical evidence only. |
| F-WRITE-GATE | F-SEND-AUTHORIZED | 2026-07-19 | `docs/facts.md`; `docs/integration-architecture.md`; `lib/integrations/action-registry-seed.ts` | only production_allowed:true entries are three workflow-targeted Gmail actions | Owner granted send/draft permissions across the board (2026-07-19) and directed a ship-to-live posture; the three-executable-Gmail-actions ceiling and the preview-only-until-cutover default are retired. Per-action production_allowed activation is now a routine reviewed change gated only by a genuine external dependency, with the human-confirmed-send, secrets-in-Secret-Manager, SoR-contract, no-sample-send, and managed-identity invariants retained. |

## Open Questions

The `Q-` rows above, surfaced together so a reader sees the unknowns at a glance. Detail and history
live in `docs/research-backlog.md` and `docs/status.md`; resolve a question by flipping its row to a
`Verified` fact (with evidence + date) or recording why it stays open.

- **Q-CUTOVER-POSTURE** — OWNER DECISION (surfaced 2026-07-19): dictated UI/UX spec §B ("remove test/simulation/demo mode; make everything sendable; make it prod") conflicts with the standing governance that deliberately KEEPS the isolated Test lane, the simulation-only run surfaces, and the admin SimulatedEmailChain demo as safety features (AGENTS.md Conventions; Locked Safety "no autonomous/bulk send"). The 2026-07-19 adversarial audit confirmed these are code-only governance defaults (not external dependencies) that §B would remove — but removing them contradicts a repeatedly-ratified safety posture, so it was NOT done unilaterally. Owner to choose: keep the Test lane, or execute the §B production-only cutover (which would also drop the stale "V1 application" banner, the four "nothing here can send" strings, and the hardcoded "Dan" labels).
- **Q-ABC-1** — define "ABC" (owner). Nothing should rely on an assumed meaning. (Still open.)
- **Q-V1-EDITOR-EXECUTION / Q-V1-ENDTOEND-BOUNDARY** — RESOLVED by the working-app model. S20/S25/S26 define the exact Live contracts; persistent production Test workflows prove app execution, while each real provider action activates independently.
- **Q-SOURCE-PUBLISH-BOUNDARY** — RESOLVED by R05; S21 defines the automated trust boundary.
- **Q-VENDOR-PORTAL-IDENTITY** — RESOLVED by R04/S22: Admin-provisioned Vendor identity, verified email, TOTP, assigned-ticket authorization, and isolated Test mailbox are application V1; each real Vendor's OAuth mailbox is activated independently.
- **Q-CONSOLE-REAL-DATA** — RESOLVED by R08/S23: production renders bounded Live data and clearly labeled, isolated, persisted Test workflow state together.
- **Q-GMAIL-RETENTION / Q-GMAIL-RENEWAL-ARTIFACTS / Q-GMAIL-MAINT-ARTIFACTS** — RESOLVED by R06/R07/S24: exact bodyless retention/legal hold, the three v1.0 base artifacts, and human-confirmed exact reply policy are implemented; automatic TTL/Scheduler operation is optional.
- **Q-PREC-1** — RESOLVED (owner 2026-07-01): per-case manual override via the resolve flow, Admin-approved for High-severity, reason mandatory + logged, never auto-applied.
- **Q-WRITEBACK-METHOD** — RESOLVED (owner 2026-07-01): append-only proposal column first; graduate to cell-anchored compare-and-set after Phase-1 accuracy is proven; RentVine-first deferred (OQ-RV-1).
- **Q-MAINT-STORAGE** — RESOLVED: Drive sync live + verified (F-DRIVE-DWD); only the prod Cloud Run MAINTENANCE_PHOTO_DRIVE_FOLDER_ID env remains (deploy step), now enforced by the cutover preflight.
- **Q-MAINT-PHOTO-INDEX** — RESOLVED (owner 2026-07-01): binding rule — tenant photos never enter an indexed corpus; SOPs index from a separate approved low-sensitivity source only.
- **D1-APPROVAL-REASON** — RESOLVED (owner 2026-07-10): in the S14 mobile approval redesign, a tapped `reason_code` satisfies the mandatory-reason rule for Low/Medium severity (stamped verbatim to the audit record); free-text reason stays REQUIRED for High/Blocked and any manual override. Scopes but does not remove Q-PREC-1.
- **D2-ATTENTION-IA** — RESOLVED (owner 2026-07-10): Console stays the at-a-glance home; Notifications becomes a superset time-ordered hub the same signals ALSO flow into (ADD, not MOVE), backed by one shared gather so counts never diverge (S17).
- **D3-GMAIL-HUB** — SUPERSEDED 2026-07-13 by F-GMAIL-LIVE-DIRECTION. S15's pasted/sanitized app-plane remains a fallback; the final-state direction is bounded per-user live Gmail with exact-confirmation send.
- **D4-OVERHAUL-CADENCE** — RESOLVED (owner 2026-07-10): the 2026-07-10 cycle delivers the runner-agnostic governance foundation + specs S14–S18 only, then STOPS for owner review before any pillar implementation.
- **Q-RBAC-SCOPE-MODEL** — RESOLVED for managed staff by Round 1 safe-default approval: keep the orthogonal `scopes` claim and absent=all-spaces behavior. The new vendor-worker identity/mailbox is separate (`Q-VENDOR-PORTAL-IDENTITY`).
- **Q-RBAC-1** — RESOLVED by Round 1 safe-default approval: scope-denied users land on their primary in-scope space.
- **Q-DAN-REVIEW-CADENCE** — RESOLVED by Round 1 safe-default approval: weekly, renewals-only for V1.
