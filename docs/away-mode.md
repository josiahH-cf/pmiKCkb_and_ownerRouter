# Away-mode compatibility marker

`AWAY_MODE_STATUS: INACTIVE`

Away mode has been inactive since 2026-06-15 and grants no authority. This minimal file remains only
because the protected local budget guard reads the machine marker. Current authority and safety live
in `AGENTS.md`; do not restore the former remote queue or use Git history as operating guidance.
