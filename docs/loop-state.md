# Loop state

Last updated: 2026-09-10. Read AGENTS.md, docs/facts.md and docs/open-blockers.md first.

## Current objective

Finish S113 release after actual backend verification and adversarial closure. Do not stop at an
unpromoted candidate. Preserve supplied work and private evidence. No new scope or customer effects.

Initial S113 delivery is pushed on main as 00836a82b26aedf4d439fec6280c54ab7548cdd4 (217 files).
Exact CI 34525701558 passed all five jobs: 6,460 unit tests, four existing skips, 201 backend tests.
The first backend lane had one mounted-journey timeout; its unchanged rerun passed. No assertion changed.
Candidate pmi-kc-app-rmtvz9r9f-10904b929fad passed deployment, exact version/configuration, smoke
and domains, but failed browser assurance. No promotion or observation passed.

## Current corrections and verification

F1-F5 and normal S106/S34 continuation are implemented. All initial 21 review findings are closed.
Seven compiled browser checks passed, including the full desk and 42-step guide. Backend journeys
use actual controls/routes/Firestore/claims/receipts/readbacks with deterministic external adapters.
Staff completion remains distinct from provider verification; no fake production completion is seeded.

Three browser-release findings are corrected in the working tree:

- Legacy predecessor selector now checks presence before its fallback; candidate stays strict.
- Only canceled same-origin Next.js page prefetch GETs are excluded from request failures.
  Navigation, API, other errors, mutations and late diagnostics remain fatal.
- RentVine and Sheet durable status reads use GET with unchanged authorization/environment checks,
  strict parameters, signed actor-bound Sheet context header and private/no-store responses.
  Actual mutation paths remain exact-confirmed POST; GET cannot select an effect operation.

The owner approved the exact blocked predecessor My Work POST /api/work {action:reconcile}
exception. Version 4 receipts retain failed_known_legacy_defect and exact blocked evidence.
The latest actual predecessor diagnostic passes all 13 landmarks, with only that one successfully
aborted request and its matching browser errors. Candidate/post-promotion still require zero attempts.
No business request was dispatched. Never ask again for this exact exception or an Editor profile.

Initial affected verification: nine files / 123 PASS and typecheck PASS in
`temp/s113-release-corrections-r1.log`. Canonical R2 passed clean install, format, lint and types;
full units had 6,486 PASS and two inventory failures for the newly added GET methods/header updates.
Both exact inventories are corrected under existing read authority. R3 passes all 6,488 unit tests,
201 backend tests, policy checks and the production build. All 25 review findings are closed.
A final exception own-property edge case failed first, was corrected and passes its direct regression.
Review evidence: docs/evidence/s113-implementation-review-2026-09-10.md.

## Authority and resource inputs

Required insurance, renewal information and seven legal-form location fields accept blanks.
Only an output requiring a real verified resource waits. No placeholder becomes customer/legal content.
The supplied six-page private PDF pack remains in docs/client_docs/renewal-template-source-2026-09-10/.
Both exact owner and tenant v2 publications were published/read back approved at 20:08:16-17 UTC
September 10. Old review-only versions remain unchanged. No Gmail draft/send ran.

S106/S34 normal packet controls, S20 queue/ledger, exact S21 bytes and own-receipt recovery pass backend
acceptance. Actual forms/catalog/mappings, managed connection/selection and closed exact keys remain
separate gates. No signature API or legal content is invented. S100/S36 remain outside this scope.

## Authentication and release

Approved WSL CLI/ADC refresh passed again. Native Node 22.23.2 and Linux SDK use the existing store.
Owner profile: /home/josiah/pmi-assurance/owner-admin, authenticated on canonical and 00836a8 candidate.
Admin-only assurance is authorized; Editor is not_run and backend role restrictions remain intact.
No account, IAM or claim changed. The separate 24-hour authentication longevity proof is unverified.

Serving: d243911cb20ffb01773072c0e27c723648eeea34 / pmi-kc-app-rmtkmhj1z-8855e4c6dbfb,
https://pmi-kc-app-kq6wuvpiva-uc.a.run.app, 100% traffic. This is the captured predecessor.
Current checkpoint: /home/josiah/.local/state/pmi-kc-release/checkpoint.json, SHA 00836a8, assurance.
The older 6e77d18 checkpoint was archived exactly as superseded, without a PASS claim.
The watcher is stopped to serialize this release; restore the existing watcher after final acceptance.

## Exact continuation

1. Finish canonical verification, actual GET status backend readbacks and affected adversarial review.
2. Record results, format/check current docs and extract/reverse/clean-base-check the correction patch.
   Commit/push only the green correction scope; no protected auth/action activation changes are added.
3. Require green exact-main-SHA CI. Use temp/s113-release-current.sh with the exact SHA and --initialize;
   it archives the exact 00836a8 assurance checkpoint, then uses the real driver and gates.
4. Complete new candidate/configuration/domain checks, Admin on both origins, source reconciliation,
   version 4 receipt, exact promotion and 300,000 ms observation. Finish promotion and serving readback.
5. Read actual serving APIs and backend state; apply only the prepared two-key Sheet descriptive
   metadata correction with backup/CAS/readback, preserving all 48 entries and 16 open flags.
6. Update current docs, downstream suite status and meeting PDF to verified serving truth, push
   documentation-only closure and restore the existing watcher. No docs-only Cloud Run deployment.
