# 2026-07-13 Localhost Interactive Audit Remediation

This is the implementation and verification companion to
`docs/local-app-interactive-audit-checklist-2026-07-13.json`. The audit remains immutable evidence of the original
failures. Update this document as remediation evidence is produced; do not rewrite the audit observations.

## Safety Envelope

- Verification is local only: `FIRESTORE_EMULATOR_HOST=127.0.0.1:8080`, `LOCAL_DEMO_AUTH=true`,
  `ASK_DEMO_MODE=true`, `MODEL_PROVIDER=local`, and `IMAGE_STORE=stub` are set in the process environment.
- No production Firestore inspection or cleanup, external send, RentVine/Sheet write, Firebase claim mutation,
  live Drive upload, deployment, or other external mutation is authorized.
- `google_drive.maintenance_photo.store` remains `production_allowed:false`. The only permitted outcome here is
  enforced refusal plus a future-gate preview contract.
- QA-007 containment does not authorize incident cleanup. An owner must separately inspect and authorize any
  narrowly scoped response to suspected synthetic production fixtures.

## Traceability Plan

| Finding | Root cause                                                                                                                                 | Behavioral contract                                                                                                                                                   | Server enforcement                                                                                                              | UI change                                                                                   | Primary files                                                                          | Tests                                                                                                                                 | Docs                                                             | Dependencies                             | Safe verification                                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| QA-001  | Recorder callbacks update the button/transcript but expose no typed lifecycle state.                                                       | Explicit permission, recording, stopping, processing, success/no-speech/error announcements; preserve typed prompt and never auto-submit.                             | Existing STT provider fence remains; no new persistence/API scope.                                                              | One restrained live region, visible state, predictable focus return, append result.         | `AskForm.tsx`, `useAudioRecorder.ts`                                                   | Hook/component lifecycle, rapid actions, denial, empty/error, unmount; browser local-stub flow.                                       | Ask/voice facts and status.                                      | After safety waves.                      | Stub recorder/STT only; desktop and phone DOM/status checks.                                               |
| QA-002  | High/Blocked resolution calls `window.confirm`, which is unreliable in embedded browsers.                                                  | Required reason followed by an accessible DOM dialog; cancel preserves state; confirm submits once and returns focus.                                                 | Existing resolution route/capability/append-only checks stay unchanged.                                                         | Focus-trapped dialog with Escape, Cancel/Continue, pending state, and focus restoration.    | `flag-actions.tsx`, renewal run client, dialog styles                                  | Empty reason, cancel zero calls, keyboard/focus, double confirm, failure/stale, ordinary-browser and in-app flows.                    | Renewal suite/product status.                                    | QA-007 safe fixtures first.              | Emulator-only High fixture; assert one app-plane audit and zero external writes.                           |
| QA-003  | Notifications gathers events/standing conditions but omits the shared needs-decision inbox used by Console.                                | One canonical value-free attention projection; event, standing, and backlog lanes stay distinct; no false all-clear.                                                  | Scope-filtered gatherer returns whitelisted counts/links only.                                                                  | Decision lane in full hub; event-empty copy is local to the event lane.                     | `lib/notifications/hub.ts`, `app/notifications/page.tsx`, attention/approval gatherers | Same-fixture Console/hub count invariants, serialization negatives, empty-event/nonempty-decision rendering.                          | Unified attention suite, facts/status.                           | QA-007 fixtures.                         | Emulator decisions; compare rendered counts before/after an app-plane resolution.                          |
| QA-004  | Exclusions compare untrusted free text to exact display labels.                                                                            | Canonical category IDs; aliases and obvious excluded intent in category/subject/facts refuse before any model construction.                                           | Deterministic normalization/classification at route boundary; unknown IDs fail closed; model call count zero.                   | Select canonical IDs and render stable refusal guidance; no Send/mailbox behavior.          | Gmail constants/drafts/anticipatory route/composer                                     | Table-driven casing/punctuation/plural/Unicode/alias and mixed-content tests; route no-model assertions; browser bypass reproduction. | Gmail suite/product facts/status.                                | Containment wave first.                  | Local model spy only; assert identical refusal for canonical and alias inputs.                             |
| QA-005  | Gmail grid/form children retain min-content widths at narrow viewports.                                                                    | All content fits 320/375/390/400px; long text wraps; no global clipping.                                                                                              | None.                                                                                                                           | `min-width:0`, `max-width:100%`, track/form constraints on actual children.                 | Gmail components, `app/globals.css`                                                    | Responsive browser measurements with longest labels/previews and focus.                                                               | Gmail suite/status.                                              | QA-004 UI stable first.                  | Local pasted synthetic text; `scrollWidth <= clientWidth`.                                                 |
| QA-006  | Projection exposes a static process definition ID without reconciling the definitions loaded for this user.                                | Start only when definition exists, is scoped/startable, and user can start; otherwise Open the space; stale deletion recovers inline.                                 | Existing route remains fail-closed; view model derives availability/permission.                                                 | Fallback link and actionable stale state; pending click dedupe.                             | Console view/anticipated components/projection                                         | Missing/out-of-scope/retired/view-only fallback, one start, stale 404 recovery, duplicate pending.                                    | Process auto-initiation suite/facts/status.                      | QA-007 safe emulator.                    | Fresh emulator both with and without a local definition; simulation-only run.                              |
| QA-007  | Demo scripts can initialize Admin Firestore without a verified process-level emulator target.                                              | One pre-Admin guard accepts only reachable loopback targets, prints non-secret target metadata, and never offers `--live`.                                            | Central host parser/reachability guard used by seed/reset/operator/shared helper before Admin import/init; child env preserved. | CLI messages only.                                                                          | `scripts/demo-firestore.mjs`, seed/reset/operator scripts                              | Parser/reachability unit tests; child-process proof of pre-init refusal; local integration; repeated/interrupted idempotence.         | Implement/environment handoff/facts/status; owner incident note. | First.                                   | Disposable loopback listener/emulator; production Admin path mocked unreachable; no live credentials/read. |
| QA-008  | Expanded queue grid/disclosure children use min-content width beyond the phone track.                                                      | Collapsed and every tab fit 320–400px; labels wrap; only tab strip may scroll locally.                                                                                | None.                                                                                                                           | Constrain shell, disclosure, rows, links, chips, nested panels without global hiding.       | Approval components, `app/globals.css`                                                 | Responsive browser measurements for all tabs/long labels; tab keyboard/local-scroll check.                                            | Approval Queue mobile suite/status.                              | After queue fixtures are safely local.   | Emulator/synthetic queue; `scrollWidth <= clientWidth` per expanded view.                                  |
| QA-009  | Blockers are advisory on the client and ticket schema defaults required fields to empty/null.                                              | Blocked preview stays available; create requires trimmed issue plus explicitly selected verified unit; selection invalidates on edit; one request.                    | Tight schema/readiness assertion before any Firestore call; stable 400; zero ticket/activity/notification on invalid input.     | Disabled Create control tied to guidance; clear/edit invalidates selection; pending dedupe. | Maintenance capture/draft/ticket route/store                                           | Client readiness/selection/dedupe; route/schema no-write spies; emulator valid/invalid persistence.                                   | Maintenance suite/product facts/status.                          | QA-007 fixture safety.                   | Emulator-only synthetic unit; document counts before/after; no RentVine.                                   |
| QA-010  | Property history route exists but renewal record/run view models render no safe canonical link.                                            | Authorized record links always use canonical property; run/flag links only when unambiguous; explicit unavailable state; origin-aware return.                         | Existing renewals scope/Admin checks remain; derivation rejects ambiguous/propertyless mapping.                                 | Named history link and safe return navigation; empty/unavailable/long-content states.       | Renewal record/run pages/client, property repository                                   | Canonical encoding, ambiguity negatives, auth/scope, empty/multiple/long, browser record/run/back/phone.                              | Renewal suite/product facts/status.                              | QA-007 safe fixtures; can follow QA-002. | Synthetic emulator records only; assert no link for ambiguity and preserved authorization.                 |
| QA-011  | Photo route constructs the configured store and uploads without consulting the closed Action Registry entry; local demo may resolve Drive. | Closed action exposes no file input and returns typed refusal before body/store/client work; local demo cannot select Drive; future open state requires safe preview. | Canonical readiness lookup is first; configuration forces stub for local demo; no Drive client construction while closed.       | Explicit unavailable copy and future filename/MIME/safe-folder preview contract.            | Photo route, maintenance capture, image store/config/action readiness                  | Route order/no-client spies, closed UI, local-demo store fence, preview contract; no live upload test.                                | Integration/maintenance/environment/facts/status.                | First, alongside QA-007.                 | Crafted local POST while closed plus injected spies; assert zero body/store/Drive calls.                   |

## Wave Status

1. **Critical containment (QA-007, QA-011):** code, focused tests, and safe emulator/browser evidence complete.
2. **Central safety/truth boundaries (QA-004, QA-003):** code, focused tests, and direct browser evidence complete.
3. **Workflow integrity (QA-009, QA-006, QA-002, QA-010):** code, focused tests, and direct browser/emulator evidence complete.
4. **Accessibility/responsive quality (QA-001, QA-005, QA-008):** code, focused tests, and direct browser evidence complete.
5. **Full regression and interactive re-audit:** complete.

## Verification Record

Focused and full automated checks plus direct browser evidence will be recorded here at each completed boundary.
Source inspection or an automated assertion alone is never recorded as an interactive pass.

- Critical containment focused run: 8 files / 78 tests passed; `npm run typecheck` passed. Proven by tests:
  non-local/unreachable seed paths stop before Admin initialization, a `.env.local` loopback target is propagated,
  the closed photo route does not read JSON or construct a store, the closed UI has no file input, and local-demo
  config resolves the stub. Interactive status remains pending until the final safe localhost run.
- Shared safety/truth focused run: 10 files / 80 tests passed; `npm run typecheck` passed. Proven by tests:
  canonical/alias/Unicode-width exclusions and excluded subject/fact intent construct no provider and call no
  model; the full hub uses the shared inbox total, strips smuggled values/reasons/addresses, and retains decisions
  when its event log is empty. Interactive status remains pending until the final safe localhost run.
- Workflow-integrity focused runs passed for maintenance capture/ticket routes and persistence, anticipated-work
  rendering/stale recovery, the High-resolution dialog, property attribution/navigation, and value-free renewal
  board sentinels; `npm run typecheck` passed. Proven: invalid tickets stop before the writer, a changed unit
  invalidates a ready draft, stale/missing/retired process definitions fall back safely, rapid starts/resolutions
  are deduplicated, dialog cancel preserves the operator input and restores focus, and ambiguous/property-less
  flags receive no history destination.
- Accessibility/responsive focused runs passed: 4 dictation files / 21 tests and 7 UI/CSS files / 31 tests;
  `npm run typecheck` passed. Proven: permission/recording/stopping/processing/success/no-speech/error lifecycle,
  typed-question preservation, focus return, rapid permission suppression, unmount stream cleanup, actual Gmail
  form/preview constraints, actual Approval Queue disclosure/row constraints, and no document-level overflow
  hiding.
- Safe emulator exercise passed with `FIRESTORE_EMULATOR_HOST=127.0.0.1:8080` and
  `FIREBASE_PROJECT_ID=pmi-kc-kb-prod`: `npm run demo:reset` printed the verified loopback target before Admin
  initialization and produced deterministic counts (`total=24`, `created=24`, `updated=0`). The project id was
  only the emulator namespace; no ADC credential or production Firestore path was used.
- Direct in-app-browser re-audit passed against `http://localhost:3100` with local demo auth, local model mode,
  stub speech/image storage, and the same loopback emulator. Gmail hard exclusions refused before drafting; the
  Console and full Notifications hub both showed the same seven-decision backlog while the empty bell now says
  only `No unread event notifications.`; anticipated cards exposed only valid space links or no action; a blocked
  Maintenance preview kept Create disabled, editing the unit invalidated the draft, and the closed photo action
  exposed no file input.
- Direct real-browser keyboard evidence passed for the High renewal dialog: `aria-modal=true`, initial focus on
  Cancel, Shift+Tab/Tab focus wrap, Escape close, preserved reason, and focus returned to Resolve. Confirm was not
  activated, so no app-plane decision was recorded. The canonical property-history link loaded the Admin route and
  returned to the exact originating flag.
- Responsive browser measurements passed at 320, 375, 390, and 400 CSS pixels for Gmail Hub and every Approval
  Queue tab: in every case `documentElement.scrollWidth === documentElement.clientWidth`; the queue tab strip alone
  retained local `overflow-x:auto`. A 260-character synthetic draft fact also remained contained at 320 px. The
  authenticated Chrome cross-check also confirmed the actual Gmail Hub and Approval Queue routes/headings at the
  task's exact 1440×1000 desktop and 390×844 phone viewports; all four exact-size route measurements had
  `scrollWidth === clientWidth`. A deterministic Chrome runner mounted the real `AskForm` with temporary local
  microphone/recorder/transcription stubs and directly observed permission, recording, stopping, processing,
  appended-transcript, no-speech, and error announcements. It also confirmed typed-text preservation, stopped
  media tracks, and focus return to the re-enabled Dictate button after all three terminal outcomes. This pass
  exposed and fixed a real timing bug: focus had previously been attempted while the button was still disabled;
  it now returns on the next frame. The temporary route and harness were deleted after verification. Component and
  hook tests retain denial, rapid-request suppression, and unmount coverage without a real microphone grant.
  Browser error logs were empty.
- Full deterministic verification passed: `bash scripts/verify.sh` completed the clean install, formatting, lint
  (zero errors), typecheck, 219 test files / 1,548 tests, router boundary, falsification, context freshness, spec
  traceability, redaction, and production Next.js build. `npm run test:e2e:core` separately passed 7 files / 31
  tests with the 3 emulator-only files / 18 tests skipped by that command as designed. `git diff --check` passed.
  The clean install continues to report the repository's existing 11 dependency advisories (1 low, 7 moderate,
  3 high); this slice did not alter dependency versions.
