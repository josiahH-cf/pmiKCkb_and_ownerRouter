export function ReleaseStageBanner() {
  return (
    <div className="environment-banner" role="status">
      V1 application · Live and Test records are labeled by data mode · provider status
      and signoffs are advisory
    </div>
  );
}
